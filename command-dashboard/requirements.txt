fastapi==0.136.0
uvicorn[standard]==0.30.0
python-multipart==0.0.26
structlog>=24.1.0
# Backup DR Drill (#41) — Fernet AES-128-CBC for encrypted backups (E-4 唯一允許新 dep)
cryptography>=43.0.0
# P2-02 (#102) — CoT XML XXE-safe 解析。維護者 Christian Heimes（CPython core），PSF/MIT，非中國。
# 純 Python、無 C extension、無傳遞相依。Wave 2 訂閱層另加 pytak[with-takproto]。
defusedxml==0.7.1
# P2-02 Wave 2 (#106) — TAK :8089 CoT 串流訂閱（mTLS + 分幀 + v0/v1 協商）。
# pytak/takproto 維護者 Greg Albrecht / Sensors & Signals LLC（美國），Apache-2.0/MIT，非中國。
# pytak 核心零必填相依；with_takproto extra 拉 takproto（protobuf，v1 用）。
pytak[with_takproto]==7.3.10
# P2-11 (#138) — Marti REST HTTP client（aiohttp）。pytak 7.x 不再把 aiohttp 列為
# 必填傳遞相依，需明確聲明。維護者 aio-libs（歐美開源社群），Apache-2.0，非中國。
aiohttp>=3.9
# P1-12a (#227) — FIDO2/CTAP2 hmac-secret（scripts/keymgmt 用，app runtime 不 import）。
# 維護者 Yubico AB（瑞典），BSD-2-Clause，非中國。lazy import：無金鑰的
# dev / CI 場景走 ICS_MASTER_KEY fallback 或 mock，不觸發此套件。
fido2==2.2.0
# P1-12c (#229) — SQLCipher at-rest 加密 driver（模組名 sqlcipher3；-binary 變體含 bundled
# SQLCipher，免系統 libsqlcipher）。維護者 Charles Leifer / coleifer（美國），SQLCipher 本體
# Zetetic LLC（美國），BSD-style，非中國。app runtime 僅「加密模式」lazy import（core.database）。
# ⚠ Windows 無 wheel（已實測）→ platform marker 排除，本機跑明文模式；Linux/CI（ubuntu）才裝、
# 跑加密整合測試（tests/integration/test_db_encryption.py，sqlcipher3 缺則 importorskip）。
sqlcipher3-binary==0.6.0; sys_platform != "win32"
# #318 — 層2 真撤銷：ICS 直連 TAK Server postgres 寫 certificate 表（hash+revocation_date）→
# TAK x509checkRevocation 在 :8089 串流層踢掉撤銷證（reality check 證 CRL 只擋 :8443 不擋串流）。
# 維護者 Tony Locke（英國），BSD-3-Clause，**純 Python、無 C extension/libpq 系統依賴**，非中國。
# 僅 services/tak_revocation 用（admin 撤銷時一次性 INSERT/UPDATE，低頻）；未配置 TAK_DB_* 則不 import。
pg8000==1.31.2
