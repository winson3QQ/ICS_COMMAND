# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
.env

# Database
*.db
*.db-shm
*.db-wal
*.db.gz
data/backups/
# P1-12b（#228）：runtime 備份目錄（整包 .tar.gz.enc / pre-restore / DB-only .db.gz.enc）。
# 原 `data/backups/`（上行）錨定 repo root，match 不到 command-dashboard/data/backups/
# → .db.gz.enc / .tar.gz.enc 漏網。補正確路徑（user-data 邊界紅線，不入版控）。
command-dashboard/data/backups/

# P1-13 (issue #27)：map_config runtime 檔在 data/，user data 邊界，不入版控；
# factory default 走 static/map_config.seed.json（tracked）+ startup ensure() 兜底
command-dashboard/data/map_config.json
command-dashboard/data/map_config.json.tmp

# P1-10d 地基（#60/#66）：event_taxonomy runtime 檔在 data/，同 user data 邊界
command-dashboard/data/event_taxonomy.json
command-dashboard/data/event_taxonomy.json.tmp

# Node
node_modules/
dist/
.cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.claude/settings.json
.claude/settings.local.json
.claude/todos/
.claude/projects/

# Wireshark captures
wireshark/

# TLS 憑證（含私鑰，不可提交）
certs/
*.pem

# Token / secret 防誤提交
*.token
.codeberg-token
.github-token
*-token
.netrc

# 臨時指令檔
claude_code_instructions.md

# pytest 產生的暫存與報告
command-dashboard/.coverage
command-dashboard/.pytest_cache/
command-dashboard/tests/reports/

# #419：release SBOM 烤入用副本（build artifact；gen_release_sbom.sh 於 release 產、
# Dockerfile COPY 進映像）。版本化紀錄走 repo 根 sbom/releases/（committed），此副本不入 git。
command-dashboard/sbom/current.cdx.json

# 截圖（badge 資產除外）
command-dashboard/static/*.png
!command-dashboard/static/badge.png
!command-dashboard/static/臂章後製版.png
command-dashboard/static/tiles/
# 使用者上傳的底圖（#286 H1：runtime 寫入物，server 端 uuid 命名；勿入 git / snapshot）
command-dashboard/static/map_upload_*

# 本地暫存 / 實驗區（mockup、抓取腳本、中間檔；保留品已搬進 static/）
_scratch/

# 本機 TAK dev 啟動器（機器專屬憑證路徑，不進版控）
dev_tak_mac.sh

server.log
server.log.err

# runtime DB 備份（P1-12b 自動備份，不入 git；user-data 邊界）
deploy/prod/_db_backups/
deploy/prod/_dbbackup/
