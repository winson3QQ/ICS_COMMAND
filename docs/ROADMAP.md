# ICS_Command Roadmap

由 [ICS_DMAS](https://github.com/winson3QQ/ICS_DMAS) 拆分而來的指揮部單體版本。本 ROADMAP 為**單一 SoT**，不再維護獨立的 `matrix.md`；compliance 對照以 inline 註記方式融入各 phase 的 Definition of Done。

## 🧭 整體概念導覽 + 文件地圖（TAK 工作 START HERE）

> 給「靠 ROADMAP 定位」的接手者：**先讀這一段拿到整體概念 + 知道每件事 SoT 在哪，再往下找工作**。（2026-06-09 漸進重構建立，避免檢討成果散落、session 漏讀。）

**全局架構（一句）**：COP（共同作戰圖）＝ 多來源（`tak` / `pi-node`／回流 PWA / `waveink` / `manual`）經 **`cop_service` 單一正規化層** 落地 `cop_entities`（COP 單一 SoT）。**TAK 只是來源之一**，任何功能**不得硬依賴它**（掛掉走三階降階；見 strategy §2）。

**指揮循環對照**（19 情境深挖按此組織）：
| OODA / 軍用循環 | 對應情境群 |
|---|---|
| 感知 (Observe) | 基礎(BFT/COP/多機構) + 感知(Recon/UAV/IMINT/SDR) |
| 計劃·決策 (Orient/Decide) | 軍用計劃流程 + 指揮決策(OODA Decide) |
| 行動 (Act) | 動作/應變(SAR/颱風/野火/結構火災/HazMat/設施/MCI) + 高威脅(RTF) |
| 評估 (回饋) | 計劃驗證(TTX) + 評估(AAR) |

**文件地圖（誰是哪件事的 SoT）**：
| 想知道 | 看哪份 |
|---|---|
| **做什麼**（work, 開 issue 源頭）| **本檔 ROADMAP** |
| 為何要這功能（操作面 / 19 情境 / why）| [`docs/reference/tak-use-cases-config.md`](reference/tak-use-cases-config.md) |
| 怎麼沿 TAK 軸做（降階 / 能力↔API 矩陣 / CoT 生命週期 / server-authoritative invariant）| [`docs/roadmap/tak-integration-strategy.md`](roadmap/tak-integration-strategy.md) |
| 安全 / 威脅（TAK 信任邊界 §8）| [`docs/compliance/threat_model.md`](compliance/threat_model.md) |
| 符號 / 2525 / NAPSG（§6 LOCKED）| `command-dashboard/docs/design/classification-crosswalk.md` |
| COP 分層 / 資料模型（事件·標記·決策·下行）| [`docs/design/cop-event-layering.md`](design/cop-event-layering.md) |
| marker/event 解耦：操作 / 呈現 / 緣起（P2-33 必讀）| [`docs/design/cop-marker-event-decoupling.md`](design/cop-marker-event-decoupling.md) |
| TAK 部署 / 憑證 ops | [`deploy/tak-server/README.md`](../deploy/tak-server/README.md) |
| TAK API 契約 | `docs/reference/takserver-5.7-openapispec.json` |

> **⚠️ TAK 工作必讀（不漏讀）**：動任何 **P2 / TAK item 前**，先讀 strategy（**§1 invariant** / **§1.1 server-authoritative** / **§4b CoT 生命週期** / **§4 能力↔API 矩陣**）+ 對應 **threat_model §8** + use-cases 的 why。**Phase 2 DoD 的「降階紀律」為強制**（每 item 宣告 Tier + fallback + 斷線負向測試）。

## 狀態 marker 約定

每個 item 完成時在 row 開頭加 ✅，並寫入 `(#PR, commit hash)` 供 audit。維護由 [`docs/PROCESS.md`](PROCESS.md) step 8 強制（**merge 同時必勾 ROADMAP**，不是事後想到才補）。

- ✅ = 完成 + merged
- ⏳ = 進行中（branch 已開）
- 🚧 = blocked（PR 留 `ESCALATE` 升級）
- 無 marker = pending

跨機器 / 跨 session 新接手者：跑 `python3 scripts/roadmap_issue_sync.py` 拿到「ROADMAP item ↔ GitHub issue」對照（Layer 3 status report 規劃中）。

---

## 願景

在指揮部儀表板上建構**共同作戰圖（COP, Common Operational Picture）**，作為事件管理體系（ICS）下的決策中樞。COP 透過兩個外部來源餵入：

1. **TAK Server**（官方 [TAK-Product-Center/Server](https://github.com/TAK-Product-Center/Server)，Apache 2.0）——標準化態勢圖、人員 / 載具位置、CoT 事件
2. **WaveInk**（[winson3QQ/WaveInk](https://codeberg.org/winson3QQ/WaveInk)）——SDR 多頻 PTT 錄音 + Breeze ASR 中文 STT，將無線電通聯轉成結構化事件

兩者皆透過 `services/cop_service.py` 單一正規化層落地，**不開新 table**——遵循 ICS_DMAS 既定架構決策（多來源 → 單一 COP 模型）。

---

## Phase 1 — Command Dashboard 基底重構（Re-construct）

**目標**：拆分後的 Command 單體能獨立運作、測試 green、文件對齊；改造原 PWA 殘留依賴為**通用上游節點介面**（保留未來 Medical / Shelter PWA 回流的可能性），並把地圖 UX 升級為 P2 TAK 整合的基底。

### Scope

| Item | 說明 |
|---|---|
| **——— 【基礎 ① 後端清整 + COP 正規化】拆分後可獨立運作的地基 ———** | — |
| ✅ P1-01 | 盤點 18 routers / 18 repos / 7 services，移除真正的孤兒 import（**不刪除 federation infra**，見 P1-04） — 完成於 [#2](https://github.com/winson3QQ/ICS_COMMAND/pull/2) `05049e2`（2026-05-25）|
| ✅ P1-04 | **保留並重新定位** `pi_batch_repo`、`pi_node_repo`、`sync_repo`（三 Pass 對齊）為「**上游節點 federation 介面**」——架構上已是通用設計（`sync_repo._unit_to_node()` 已參數化 shelter/medical/forward/security）。命名不改，文件補充說明：未來 Medical / Shelter PWA 重新對接、或 ICS_Command 變成多 Pi 站台中樞時，這層直接用。**僅清理**真正死掉的測試與 import — 在 [P1-01 #2](https://github.com/winson3QQ/ICS_COMMAND/pull/2) `05049e2` 盤點時順手完成（3 檔 docstring 已含 P1-04 + Wave 7+ 錨點；pyflakes clean；每個 export 都有 ≥1 caller；無孤兒 test）— retroactively 標記於 2026-05-26 |
| ✅ P1-05 | `routers/manual.py` 確認仍能手動建立 COP entity（最小可用 baseline）— direct repo call 驗證 `create_manual_record` + `get_manual_records` 整鏈通 — 完成於 [#10](https://github.com/winson3QQ/ICS_COMMAND/pull/10) `f0555cf`（2026-05-26）|
| ✅ P1-03 | `services/cop_service.py` 正規化層 schema 凍結 v1（`source: enum[manual, pi-node, tak, waveink]` 必填欄位，預留 `pi-node` 不關門）。3 新表 (cop_entities/tracks/links) + events ALTER lat/lon；Pydantic 模型 + 4 normalize_* stub + repo CRUD + 35 contract tests；11 個 end-state scenario 壓測（含 TTX）— 完成於 [#16](https://github.com/winson3QQ/ICS_COMMAND/pull/16) `ba5f942`（2026-05-26）|
| **——— 【基礎 ② ingress 路由】通用上游節點介面 ———** | — |
| ✅ P1-02 | `routers/ingress.py`（自 `pi_push.py` 改名）為通用 ingress 路由集合；目前承載 pi-node（P3 將加 waveink，TAK 留在 `routers/tak.py`）。新主路徑 `/api/ingress/pi-node/{unit_id}`；舊 `/api/pi-push/{unit_id}` 以同 handler 雙裝飾器保留為別名（ICS_DMAS Pi client 向後相容）— 完成於 [#12](https://github.com/winson3QQ/ICS_COMMAND/pull/12) `9253c88`（2026-05-26）|
| ✅ P1-02b | **`server/sync.js` 遷移到新 ingress 主路徑** `/api/ingress/pi-node/{unit_id}`：Wave 4 piPush 三處（heartbeat / 正常 push / replay）統一改用新路徑；抽 const 集中化。別名 `/api/pi-push/` 在 ingress.py 雙裝飾器仍保留為長期 fallback — 完成於 [#14](https://github.com/winson3QQ/ICS_COMMAND/pull/14) `c3c2034`（2026-05-26）|
| **——— 【基礎 ③ 即時 COP 同步】多人即時共享（樂觀鎖 + WS）———** | — |
| ✅ **P1-15** | **多人即時共享 COP（[issue #29](https://github.com/winson3QQ/ICS_COMMAND/issues/29)）**：`cop_entities` 作為單一正規化 COP 儲存兼**同步邊界**——per-entity `version_clock` 樂觀鎖 + WebSocket 廣播 + 前端 per-uid LWW merge，解「畫了/釘了別人不 reload 即時看到、拖移/結案即時同步」痛點。**即時管線（done）**：PR-A 樂觀鎖 + soft-delete [#33](https://github.com/winson3QQ/ICS_COMMAND/pull/33) `e2a73c4`；PR-B `/api/cop/*` REST + If-Match [#35](https://github.com/winson3QQ/ICS_COMMAND/pull/35) `c2b4d3d`；PR-D WebSocket hub（`--workers 1` pin）[#36](https://github.com/winson3QQ/ICS_COMMAND/pull/36) `415b80c`；PR-E 前端 `cop_stream.js` [#37](https://github.com/winson3QQ/ICS_COMMAND/pull/37) `8ca2d34`。（PR-C normalize_manual 跳過，被 PR-B POST 涵蓋。）**cutover（map 物件從 map_config blob → cop_entities，done）**：PR-G1a route+polygon [#38](https://github.com/winson3QQ/ICS_COMMAND/pull/38) `022fb24`；PR-G1b event 位置圖釘 [#39](https://github.com/winson3QQ/ICS_COMMAND/pull/39) `86e389c`（事件記錄仍在 events 表、流程不變；reset 一併清 cop + 廣播 resync）。分類用 `attributes.kind`（route/polygon/event）+ CoT 相容 `type`（為 P2-04 TAK 預留）；不做 migration、從 0 開始；節點（icon='pin'）固定不 cutover。**收尾（done）**：**PR-H** [#41](https://github.com/winson3QQ/ICS_COMMAND/pull/41) —— 退役 PR-E ＋標記 MVP（cop_stream 改純資料層、移除自建 marker / ◉COP / ＋標記）+ **移除流向（flow）功能**（與 route 重疊、cutover 後連不到事件；route 才是有向移動路徑的正解）+ 規格書/ROADMAP 收尾 + close #29。**G2 descoped**：flow 移除（非 cutover）；infra 與節點真正缺的是「placement UI」（MapLibre 改寫時遺失的空 stub），與即時同步無關 → 另開 [issue #40](https://github.com/winson3QQ/ICS_COMMAND/issues/40)（補建立 UI 時順便做成 cop_entity + 綁 exercise）。**TAK 對齊**：cutover 時 type/source/origin_node_id 已對齊 CoT，雙向 TAK 實作留 P2-04 |
| **——— 【基礎 ④ 測試 / 規格 / 部署】CI + 規格 framework + start/deploy ———** | — |
| ✅ P1-06 | `tests/` 全綠 + **CI workflow 落地**（`.github/workflows/test.yml` + `.forgejo/workflows/test.yml`，跑 pyflakes + pytest + doc_sync_check）— 完成於 [#10](https://github.com/winson3QQ/ICS_COMMAND/pull/10) `f0555cf`（2026-05-26）|
| ✅ P1-07 | `docs/指揮部儀表板設計規格.md` v3.0 **framework header**（拆分 delta + incremental rewrite policy；v2.2 既有內容保留，後續隨 P1-03/P1-10/P2/P3 PR 配對段落重寫）— 完成於 [#10](https://github.com/winson3QQ/ICS_COMMAND/pull/10) `f0555cf`（2026-05-26）|
| ✅ P1-08 | `start_mac.sh` 已純化（Stage 1 完成）；補 `start_pi.sh` 與 systemd unit drop-in。**順手修 venv shebang detection（P1-01 dogfood 發現）+ systemd EnvironmentFile pattern + ics-backup paths 同步** — 完成於 [#6](https://github.com/winson3QQ/ICS_COMMAND/pull/6) `918b675`（2026-05-26）|
| ✅ P1-09 | `deploy/`（Stage 1 未帶）補回必要部分：nginx reverse proxy + TLS（去掉 PWA server block，保留 ingress 通用路由）。**順手修 7 個 code-review + 2 個 security-review findings**（DB path / ProtectHome / nginx install / Host injection / XFF spoofing / PII scrub gap / map_config world-writable…）；**ROADMAP P1-12 新增**（dogfood 衍生：統一 key management + at-rest 加密 + backup GUI）— 完成於 [#8](https://github.com/winson3QQ/ICS_COMMAND/pull/8) `a6ea334`（2026-05-26）|
| **——— 【應用 ⑤ 地圖 UX + UI】MapLibre 全換 + 移除 PWA + 響應式觸控 ———** | — |
| ✅ P1-10 | **地圖 UX baseline 升級**：見下方〈P1-10 細項展開〉。改採 **MapLibre GL JS + PMTiles + dark ops theme + SVG marker + 等寬字體**，全替換 Leaflet（不走 `leaflet-maplibre-gl` 折衷路線，避免 P2 二次手術），預埋 P2 TAK + MIL-STD-2525 渲染接點。<br>**[2026-06-06 reality check 補帳]**：子項 P1-10a/b/c/d/e/g/h 全 ✅（10f clustering descoped；10a-2 DS token migration 為**獨立延後 backlog**，非本項 blocker），對應 commit 全在歷史（`1d46025`/`d3e9386`~`418a65a`/`9ccf190`~`fe84e6e`/`d8c13ab`/`79561a7`/`f1a3f3f`）。**code 驗證**：Leaflet 已拆淨（`static/lib/` 無 leaflet.js、map.js 無 `L.*` 呼叫，殘留僅 `_leafletMap`/`refreshLeafletMarkers` 歷史變數名）、MapLibre+PMTiles 2 套 style（dark/muted-day）+ EntityLayer/SDF baking/setFeatureState/text-halo/symbol-sort-key/LOD 全落地、CSP enforce 預設開。**DoD 6/7 達成**（FOUC 由使用者親驗，見 p1-map-ux.md）；唯 **Lighthouse@Pi500 為 standing residual**（無 Pi 實機）。子項各走獨立 PR、未對單一 GitHub issue（故 status 腳本顯示 no-issue，非缺漏）。 |
| ✅ P1-11 | **Commander Dashboard UI 移除 PWA-specific 元素**：見下方〈P1-11 範圍〉。配合 P1-04 backend federation infra 保留決策，UI 層拿掉「收容/醫療」固定二元呈現，**改為 Option B 整刪**（dogfood 中決議；P2 TAK / P3 WaveInk 接入再重蓋）。源於 P1-01 dogfood 跑 dashboard 時的截圖盤點 — 完成於 [#4](https://github.com/winson3QQ/ICS_COMMAND/pull/4) `23dee14`（2026-05-26）|
| ✅ P1-18 | **Dashboard 響應式 layout + 觸控支援（[issue #165](https://github.com/winson3QQ/ICS_COMMAND/issues/165)）**：主操作儀表板在 iPhone/iPad 無法正常使用——layout 固定不縮放、觸控目標過小、地圖長按無 touch 支援。**三層修正**：① **CSS 響應式**：`@media (pointer:coarse)` 觸控目標 ≥44px（`.map-btn`/`.map-tab`/`csel-btn`/MGRS）；`@media (max-width:900px)` Zone-A 壓縮、右側 panel 改 **overlay 模式**（`position:absolute + transform:translateX(100%)`，不壓縮地圖寬度）；`@media (max-width:600px)` 全寬。② **行動面板切換按鈕**：`#panel-toggle-btn`（44×44px 圓形，桌機 `display:none`，≤900px 才顯示；`data-action="toggleMobilePanel"` dispatch 切換 `.mobile-open`；點 panel 外自動收合，guard 用 `window.matchMedia` 非 `getComputedStyle`）。③ **MapLibre 觸控長按**：`touchstart/touchmove/touchend/touchcancel` + 10px slop + 650ms timer；`_touchActive` 旗標延遲 400ms 清除，攔截瀏覽器補發的 synthetic mousedown（防 lpTimer 雙路徑干擾）；全 `{ passive:true }` 不干擾 MapLibre pan/pinch。**⚠️ `settings-panel` hidden offset bug fix**：`min(-340px,-90vw)` → `max(-340px,-90vw)`（min() 回傳最負值，隱藏時偏移過遠）。**WCAG 2.1 AA SC 2.5.5 觸控目標 ≥44px**（P2-20 iPad AAR 頁面基礎）。`cmd-v1.1.0`。<br>**[2026-06-07 完成 — [#166](https://github.com/winson3QQ/ICS_COMMAND/pull/166) `c20da6e`+`7ce0420`+`17706c2`（issue #165）]**：feat + `fix(deps): aiohttp` + code-review 5 項修正；vitest 247 passed（8 pre-existing collection error 與本 PR 無關）。**Human verify 待做**：使用者需在實機 iPhone/iPad 以 Chrome DevTools 或直連驗觸控目標、overlay panel 開收、地圖長按事件（§8 驗收腳本見 PR #166 body）。 |
| **——— 【應用 ⑥ COP 資料治理】user-data 邊界 / 演習 scoping / 放置 / 永久設施 ———** | — |
| ✅ P1-13 | **`map_config.json` seed/runtime 分離（user-data boundary 建立，P1-12 prep）**：現況 `command-dashboard/static/map_config.json` 被 git tracked 又被 server runtime 寫入，造成 (a) 工作樹永遠 dirty、(b) 切 branch 洗掉 user zone/route、(c) snapshot script 把 user data 當 code commit、(d) 上線後場域 / 演習資料活在版控（違反 user data 邊界）。改為 `static/map_config.seed.json`（tracked，factory default）+ `data/map_config.json`（gitignored，runtime），讀寫對稱走 `GET/POST /api/map_config`，startup hook + migration script 兜底既有部署。CLAUDE.md 加「User Data 邊界」紅線段。順帶擴張 P1-12b scope（三層 backup 觸發 + Restore）並新增 P1-14（Exercise data scoping）+ P1-12b+14 bundling 決策。源於 issue #24 dogfood 副發現 — 完成於 [#28](https://github.com/winson3QQ/ICS_COMMAND/pull/28) `f4775d9`（2026-05-29，含 post-review code-review 2 HIGH + 1 MEDIUM + 2 LOW finding fixes）|
| ✅ P1-14 | **Exercise data scoping — events / map_config 加 exercise 綁定**（2026-05-28 dogfood 衍生）：當前 events / map_config / exercises 三者孤立沒外鍵關聯，導致「fresh deploy 後 map 復原成 seed 但 events 表還留上場演習的 8 筆」這種資訊孤兒狀態（dogfood 親見）。修法：(a) events 表加 `exercise_id` 外鍵 + `created_at` < exercise.started_at 的 backfill 策略；(b) map_config.json 的 `evt_*` zone 在 archive 時跟著 exercise 一起歸檔（搬到 backup tarball，不留在 live data/）；(c) GET endpoints 預設只回**當前 active exercise** 的 events / zones（admin override 可看歷史）；(d) UI 加「演習名稱」chip 在 header 強提示 user「現在在看哪場」。需先有 P1-12b 才能驗 reset 流程；建議 P1-12b 完成後接著做。**[2026-06-03 reality check — 上方 (a)-(d) 描述已過時，以本段為準]**：盤 code 後發現**資料模型 + repo + active 機制 + reset + AAR 報告幾乎全已建（ICS_DMAS C0 繼承）**，**不是「三者孤立沒 FK」**：① `exercises` 表有 `status`(setup/active/archived) + `mutex_locked`（單一 active）；② `events`/`cop_entities`(+索引)/audit/decisions… **皆已有 `exercise_id` FK**；③ `exercise_repo` 有 CRUD + `update_exercise_status`(mutex) + **`get_active_exercise()`**；④ `event_repo`/`cop_entity_repo`/`cop.py` create+list **已參數化 `exercise_id` 過濾**；⑤ admin **`/reset-exercise`** 已存在（清演習表 + `cop_entities WHERE exercise_id`）；⑥ **AAR 報告已存在**（`aar_entries` 表 + ai `/report/{exercise_id}` post-exercise report + ML export）。**真正缺口 = scoping「休眠」**：`get_active_exercise()` 幾乎沒被呼叫、**前端 dashboard 完全無演習 UI**（無 active 選擇器/header chip/`/api/exercise` 呼叫）→ 建立/查 events/cop 都不帶 exercise_id → **全部 `exercise_id=NULL` 未 scoped**（孤兒 bug 根因）。**故 P1-14 實為 wiring + 前端 UI**（非建模型）：(1) 前端演習管理 UI（建立/啟動/歸檔）+ active 選擇器 + header「演習名稱」chip；(2) create 預設 `exercise_id=get_active_exercise()`；(3) GET 預設過濾 active（admin override）；(4) backfill NULL。**無硬卡 P1-12**（reset endpoint 已在；只有 reset-to-factory *完整驗證* 鬆散想要 P1-12b backup）→ **現在就能做、且直接解鎖 P1-16 綁定**（cop create 帶 active id 即綁）。**AAR 修正**：報告級 AAR 已備；只剩**視覺逐格時間軸 scrub** = Wave 6（`snapshot_repo` 也已在）。**Out of scope 排除**：跨演習 cross-reference / 演習 fork / 多演習並行（Wave 6+）。<br>**[2026-06-03 v1 完成 — [#91](https://github.com/winson3QQ/ICS_COMMAND/pull/91) `34b61cd`（issue #89）]**：① 後端 `resolve_scope(session, requested)` 安全閘 + `NULL_SCOPE` 三態過濾（int 精確／NULL 實戰池／None 不過濾）套 6 repo（events/cop/decisions/manual/dashboard/audit）；**strict isolation**（無 active→實戰 NULL 池非「看全部」，歷史需 `COMMAND_ROLES` override）；create 由 server 蓋 active `exercise_id`（不信任 client），cop create 綁 active（= P1-16 接點）。② 演習硬刪（級聯白名單表·sysadmin-only·非 active·同連線 status 再確認防 TOCTOU）。③ **跨 session 即時**：`cop_hub.broadcast_all` 於 activate/archive/delete 廣播 `exercise_switched` 給**所有**連線（不過濾 scope）→ 各 client 重對帳 map/面板/chip（cop_stream → DOM `exercise:switched` → main.js refresh）。④ 前端演習管理面板 + active chip（文字「演習／」「實戰／」「無進行中場次」），退役 ttx-toggle/session_type。⑤ Review LOW 收尾：`GET /api/cop/entities/{uid}` by-uid scope gate（非指揮層跨場讀→404）、`reset-exercise` 補清 `exercise_kpis`。測試：test_exercise_scoping（scoping/strict/role-gate/delete/by-uid）+ test_cop_ws（operator 收廣播）+ test_realtime_hub + 前端 cop_stream/exercises 全綠（216 vitest + pytest）；`/code-review`＋`/security-review` 無 HIGH/MED 新破口。**解鎖 P1-16 綁定**。<br>**[follow-up [#93](https://github.com/winson3QQ/ICS_COMMAND/issues/93) 完成 — [#96](https://github.com/winson3QQ/ICS_COMMAND/pull/96) `156eb65`]**：稽核日誌綁 active session（**Model B**）—— `audit()` 未明傳 exercise_id → 自動戳當前 active session（演習/實戰），**全部 audit（含 decision/ttx/pi-sync/系統層）**綁該場，AAR 可查/回放完整時間軸；`exercise_*` lifecycle audit 例外保 NULL（cascade durability，review #93-1）。另補 **COP 操作 audit**（create/update/delete，原本完全沒記；update 全 kind 含 event → 捕捉 QRF/事件移動軌跡；best-effort 不擋即時同步）。顯示/TAK 不受影響（audit 正交）。2026-06-04 P1-16 follow-up dogfood 衍生。<br>**[follow-up #93 收尾完成 — [#97](https://github.com/winson3QQ/ICS_COMMAND/pull/97) `f9ef5af`]**：① 稽核日誌 UX 具體化（`cop_entity_*` 依 `detail.kind` 顯「新增/移動/刪除＋節點/路線/範圍/設施/圖釘」取代泛稱「標繪」、target 顯 callsign、`_escAudit` 跳脫、session 事件納入「帳號」篩選）；② 後端 abandoned session（切帳號/關頁→token 不再被用）逾時登出補 `SESSION_EXPIRED` audit、idle cutoff `14h→min(IDLE,SESSION)=15分`（與 `check_session` 同閾值）、`main.py` lifespan 加每 5 分週期 session 清理；③ 補 DoD 缺測 2（cleanup audit 留痕 + idle cutoff 回歸守門）；④ code-review/security-review 修正（shutdown `_cleanup_task` cancel 後 await 回收、`DELETE ... RETURNING` 去並發重複 audit、audit 失敗 `log.warning`、`_escAudit` 一致化 operator/fallback），**無 HIGH 以上破口**。pytest 607 passed/1 skipped。UI 最終驗（§8 Step 4）待人工瀏覽器驗（presentational）。<br>**[2026-06-15 #265 修正 — WS scope 凍結（#258 β-1 dogfood 衍生）]**：P1-14 的 `exercise_switched` 廣播只靠 client 重連對帳，但 WS 連線 scope 在 handshake 當下凍結（`_Conn.exercise_id` resolve 一次定死）→ 切換 active 演習後新場 entity 被 `wants()` 濾掉、需硬重整才 render。**主修（server 權威）**：`_Conn.follows_active` 旗標 + `cop_hub.rescope_active(new_scope)`，activate/archive 時就地把跟隨 active 的連線重綁新 scope（不靠 client 重連、不動 COMMAND_ROLES 顯式 pin 歷史場的連線）。**前端硬化**：`cop_stream` onclose identity guard（被取代的舊 socket 不誤排重連）+ `stop()` 重置 backoff + `_refreshAfterExerciseSwitch` 改就地 `resync()`（不清快取、消雙 socket race）。**真機 ATAK dogfood /verify PASS**（切換不重開 WS、新場 entity 即時 render）。測試：`test_realtime_hub` rescope +2、`cop_stream.test` reconnect/onclose +2；pytest 1018 passed、vitest 331、`/code-review`＋`/security-review` 0 finding（isolation 邊界經驗證保持）。`backend-v2.5.1`/`frontend-v1.4.1`。dogfood 另挖出感測層 doctrine 缺口（group + 參與編組兩層）→ [#267](https://github.com/winson3QQ/ICS_COMMAND/issues/267)。<br>**[2026-06-29 ✅ #267 完成 — 感測層 scope 重構，取代本 P1-14 的 insert-time exercise 綁定（`backend-v2.27.1` / `frontend-v1.19.0`）]**：doctrine（使用者拍板）entity 屬某場 = 唯一 active 場 × producer CN 在 roster × ts 在活躍窗（**純乙**：空 roster=沒人、不 auto-capture，可用性靠 UI）。**Slices 1+2**（PR [#444](https://github.com/winson3QQ/ICS_COMMAND/pull/444)：m036 exercise_active_intervals + m037 exercise_roster + repo/schema 地基）→ **3+4**（PR [#445](https://github.com/winson3QQ/ICS_COMMAND/pull/445)，`backend-v2.27.0`/`frontend-v1.19.0`：純乙 scope 解析【ingest 接縫覆寫、fail-closed】+ roster 變動即時 restamp + `POST /api/admin/exercises/{id}/roster/add-connected` + 名冊 UI「X 連線 / Y 在場」計數＋空場警示橫幅＋「加入全部連線」一鍵）→ **5 + 部署後兩 bug 修**（PR [#446](https://github.com/winson3QQ/ICS_COMMAND/pull/446)，`backend-v2.27.1`：activate/archive 自動 `restamp_all_tak_entities`【pre-rostered 啟動即歸場 / 結束釋放回待命】；軌跡 denormalize exercise_id【m038】解「演習刪不掉＝audit append-only×FK 死結」【m039 拆 audit→exercises FK】＋「iTAK 結束消失＝entity 凍結 archived 場」；m040 一次性收斂既有殘留）。獨立 security+code review pass（抓並修 1 HIGH regression：timeline/kpi AAR reader 漏改）、live DB 副本 dry-run、部署 prod 驗證（schema_version 40、兩 bug 端到端證實修好）。教訓收 memory [[dev-prod-append-only-test-gap]]。 |
| ✅ P1-16 | **節點 / 設施 placement UI（[issue #40](https://github.com/winson3QQ/ICS_COMMAND/issues/40)）**：P1-10b（Leaflet→MapLibre 改寫）把節點/設施放置 UI 變成空 stub（`map.js` 的 `_openInfraForm` / `_startInfraPlace` / `_saveInfraPosition` / `_cancelNodePlace` 皆空）——**功能 regression**。每場演習地點不同，5 常駐編組（收容/醫療/指揮/前進/安全）+ 臨時設施（醫院/收容所/急救站/警局/消防）位置需逐場設定；現況節點只能 `map_config.seed.json` 寫死座標（換地點全錯）、設施完全無法新增。修法：(a) 工具列「放置節點 / 放置設施」→ 點地圖放置 → 拖移 / 刪除；(b) 落地為 **cop_entity**（`attributes.kind='zone'`（節點）/ `'infra'`（設施））→ 沿用 P1-15（#29）即時同步管線（cop_stream / EntityLayer / `copEntityTo*` adapter）+ 綁定當前 active exercise；(c) 設施類型沿用既有 `INFRA_TYPES`（醫院/收容所/警局/消防/公用 + 圖示/字母）。**依賴**：P1-15（#29 即時管線，done）；**對接**：P1-14（exercise data scoping — 放置物件綁 exercise）。源於 P1-15 PR-H G2 descope。<br>**[2026-06-03 reality check + A-Full 定案]**：空 stub regression 已確認（`_openInfraForm`/`_startInfraPlace`/`_saveInfraPosition`/`_cancelNodePlace` 皆 `{}`，刪除可用、放置 no-op；節點+設施現皆活在 map_config blob，非 cop_entity）。**決定走 A-Full（exercise 綁定 + 可回放做 AAR；不綁無法複盤）**。釐清出**三層資料模型**：① **永久設施**（真醫院/消防/警局/診所）= 公開資料底圖層、**不隨演習** → 另立 **P1-17**；② **5 常駐節點**（指揮/收容/醫療/前進/安全）**現為 `map_config.seed.json` 寫死座標（錯——每場地點不同）→ 需 cutover 成 exercise-scoped cop_entity**（**修正 P1-15「節點 icon='pin' 固定不 cutover」決定**）；③ **臨時節點/物件** = exercise-scoped cop_entity。②③ 落 `cop_entity + attributes.kind(zone/infra) + exercise_id`，沿用 P1-15 即時管線；因帶 `version_clock`（P1-15 已有）**為 Wave 6 AAR 逐格回放鋪資料**（snapshot_repo = P2-06 預埋、回放 UI = Wave 6）。**硬依賴：P1-14**（active-exercise 綁定機制）→ **P1-16 A-Full 不可早於 P1-14**。建議順序：P1-14（FK/scoping）→ P1-16 A-Full（P1-17 永久設施層可並行/後做）。<br>**[2026-06-03 v1 完成 — [#92](https://github.com/winson3QQ/ICS_COMMAND/pull/92) `59c7c3d`（issue #40）]**：① **節點(kind='zone')** on-demand：工具列選 5 編組 → 點地圖放 → cop_entity（P1-14 自動綁 active/實戰）+ 拖移 + 刪除；adapter `copEntityToZone` + `_allRenderedZones` cutover 改讀 cop entities；收容=避難所屋頂象形、醫療=白十字、其餘 abbr。② **設施(kind='infra')** on-demand：5 類（醫院/收容所/警局/消防/公用）放+刪（拖移 follow-up）；`copEntityToInfra` + `_renderInfra` cutover + `_scheduleCopRender` 即時重繪。③ **cutover**：移除 `map_config.seed.json` 寫死 5 zones（出廠開機無預設節點、全靠 on-demand；改正「每場地點不同」regression）。④ **RBAC**：前端 `canUseRealModeControls` gate UI + **後端** cop create/delete 對 kind∈{zone,infra} 加 `COMMAND_ROLES`（operator/observer 無法繞 API 建/刪；security review HIGH-1 落實）。⑤ 附帶：移除 vestigial「指揮部設定」+ 區段改名「站內地圖」、歸檔鈕只對 active、稽核 exercise_* 中文短標、放置 banner 重複 id。測試：copEntityToZone/Infra vitest + `TestNodeInfraRBAC`（後端 4 測），223 vitest + pytest 全綠；`/code-review`＋`/security-review` 通過（HIGH-1/MED-1/LOW 全修）。**follow-up（2026-06-03 記 → 2026-06-04 全收）**：✅ ① **設施拖移**（`_syncEventDragHandles` 納 infra + drag callback 按 kind 分支 click→`_onInfraClick`／per-frame→`_renderInfra`；dragend 泛型）；✅ ② **放置流程參數化**（`_nodePlaceState`+`_infraPlaceState` 合一 `_placeState`，共用 `_startPlace`/`_cancelPlace`/`_placeAt`，export 留 thin wrapper、main.js 不動；消 ~50 行 + 根除互斥隱患）—— ①② 完成於 [#94](https://github.com/winson3QQ/ICS_COMMAND/pull/94) `93d3d65`（in-browser 驗過）。✅ 死碼（`btn-node-place` toggle、map.js `showZoneDetail` 死刪除塊）已清 `7ed5e45`。**P1-16 follow-up 全清。**|
| ✅ P1-17 | **永久設施公開資料底圖層（[issue #88](https://github.com/winson3QQ/ICS_COMMAND/issues/88)，2026-06-03 reality check #1 衍生）**：真實醫院 / 消防 / 警局 / 診所等**永久公共設施**為**基準參考層、不隨演習走**——與 P1-16 的 exercise-scoped 節點/臨時物件分離（三層資料模型見 P1-16）。**動機**：這些是現實常駐基礎設施、跨演習不變；不該被 P1-16 的放置 UI 逐場手放、也不該被演習 reset 清掉。**資料來源**：台灣**政府開放資料**（醫療院所 / 消防分隊 / 警察機關位置等 dataset；NCDR/NFA 圖資生態）——**每個來源須逐一確認非中國供應鏈**（CLAUDE.md），離線打包匯入。**落地**：匯入為**唯讀基準圖層**（非 cop_entity、非 map_config user-data、不受 exercise scoping / reset 影響），工具列可切換顯示。**與 P1-16 分工**：P1-16 = 隨演習走的節點/臨時物件（放/搬/刪/綁 exercise）；P1-17 = 永久設施基準（匯入、唯讀、常駐）。**順序**：可獨立於 P1-16，建議 P1-16 後或並行；主要工作量在 open-data 匯入 pipeline + 供應鏈逐源確認。<br>**[2026-06-03 v1 完成 — [#90](https://github.com/winson3QQ/ICS_COMMAND/pull/90) `9614f7f`（醫療 → v1.1）]**：① **3 源上線**——避難收容處所(NCDR/內政部 #73242)/消防(消防署 #5969)/警察(警政署 #5958)，共 **8318 筆**；維護者腳本 `command-dashboard/scripts/import_facilities.py`（curl 下載→正規化→`static/facilities.seed.json`）。② **供應鏈全過**：來源皆台灣政府（非中國）、政府開放授權第1版（CC BY 4.0 相容）；`pyproj`（警政 TWD97 TM2→WGS84，MIT/NOAA/OSGeo 非中國）**僅維護者匯入用、不進 runtime requirements**。③ **PII 剝除**（管理人姓名/電話不入 seed）。④ **資料品質普查**：每筆「縣市↔座標」bbox 一致性檢查，抓掉 78 筆來源座標錯誤（海上點/placeholder/跨縣市），全 log 留痕。⑤ **前端**：自管 `facilities` clustered source（MapLibre `cluster:true`＝P1-10f 技術，**scope 限 facilities**）＋彩色圓＋110(警)/119(消)數字徽＋⌂避難所象形＋hover 名稱 tooltip；獨立層不碰 zones/infra/polygons/routes，不依賴 EntityLayer。⑥ **後端**：唯讀 `GET /api/facilities` + `facilities_store`（只讀 static seed）。⑦ **決策**：seed **tracked**（固定常駐、factory-default baseline，符 User Data 邊界；非 user-data）；普查用 **county-bbox**（零依賴、有 log，未升 point-in-polygon）。⑧ **命名**：程式名 `facilities`＝UI「**公共設施**」，**≠** 既有 `infra`（UI「設施」，P1-16 演習臨時物件）。⑨ **醫療（醫院＋診所）→ v1.1**：bulk 名單皆「有地址無座標」、需 TGOS geocode key，故延後。測試：Python 13 + JS（facilities 10）全綠；`/code-review`（7 修正）+ `/security-review`（0 finding）通過。<br>**[2026-06-03 close]**：v1 baseline（3 源 8318 筆）交付，本項標 ✅ 結案（[#88](https://github.com/winson3QQ/ICS_COMMAND/issues/88) 已關）。**唯一延後 = v1.1 醫療**（醫院+診所，卡 TGOS geocode key 外部依賴）；屬增強非阻塞，續做時另開 issue 追蹤 |
| **——— 【安全 ⑦（橫切·pending）】統一 key + LUKS/SQLCipher + Backup GUI ———** | — |
| ⏳ P1-12 | **統一 key management + At-rest 加密 + Backup GUI**：FIDO2-derived 主密鑰 + HKDF 衍生子鑰，統一 backup encryption + live DB at-rest encryption（SQLCipher）+ 未來簽章用途。詳見下方〈P1-12 範圍〉。<br>**[2026-06-08 同機部署修正 — threat_model.md §8.4]**：TAK Server 與 ICS 同機 → TAK PostgreSQL 同碟明文使「只 SQLCipher 加密 ICS DB」不一致 → **LUKS 整碟為主控必備、SQLCipher 退內層**；**P1-12a key 階層須加 `disk-v1` child = LUKS unlock key**（一次 FIDO2 unlock 開機碟+app+backup，勿碎裂託管）。動工前先訂部署 at-rest 策略。<br>**[2026-06-12 動工]**：at-rest 策略已拍板（threat_model §8.4 v0.6：LUKS 主控 [#231](https://github.com/winson3QQ/ICS_COMMAND/issues/231)・SQLCipher 退內層・`disk-v1` child 入 12a・manned C2 形態）。umbrella [#226](https://github.com/winson3QQ/ICS_COMMAND/issues/226)：12a [#227](https://github.com/winson3QQ/ICS_COMMAND/issues/227) → 12b [#228](https://github.com/winson3QQ/ICS_COMMAND/issues/228) → 12c [#229](https://github.com/winson3QQ/ICS_COMMAND/issues/229) 序列；硬體驗收尾巴 carve-out → [#230](https://github.com/winson3QQ/ICS_COMMAND/issues/230)。規格 reality-check 修訂見 [`p1-key-management.md`](roadmap/p1-key-management.md)。<br>**[2026-06-21 12a/12b merged]**：✅ 12a 金鑰基建（[#227](https://github.com/winson3QQ/ICS_COMMAND/issues/227) / PR [#234](https://github.com/winson3QQ/ICS_COMMAND/pull/234)）+ ✅ 12b 整包備份/還原 GUI（[#228](https://github.com/winson3QQ/ICS_COMMAND/issues/228) / PR [#235](https://github.com/winson3QQ/ICS_COMMAND/pull/235)）併入 main，含 code/security review + Windows/Mac 雙平台測試。**umbrella 仍 ⏳**：12c SQLCipher（[#229](https://github.com/winson3QQ/ICS_COMMAND/issues/229)）未做、硬體驗收（[#230](https://github.com/winson3QQ/ICS_COMMAND/issues/230)）待實體金鑰。<br>**[2026-06-21 12c code-complete（待 merge/CI）]**：✅ 12c SQLCipher code 落地（[#229](https://github.com/winson3QQ/ICS_COMMAND/issues/229)）：`get_conn` driver 抽象（`ICS_DB_ENCRYPTED` 明文/SQLCipher 雙路）+ 明文→加密 migration（`scripts/migrate_encrypt_db.py`）+ restore re-encrypt（補「備份產物恆明文 vs 加密 live DB」正確性洞）+ CI ubuntu 加密整合測試（`sqlcipher3-binary` 0.6.0，Windows 無 wheel→marker 排除、本機明文）。**default-off**。**策略定位（§8.4）：SQLCipher = L2 內層縱深、非 at-rest 主控**——同機部署完整 at-rest 需整碟加密（[#231](https://github.com/winson3QQ/ICS_COMMAND/issues/231) LUKS/BitLocker）；**是否真開 + 交付模式待 #231 前置定案**。 |

### Definition of Done

- [ ] `pytest` + `npm test` 全綠，coverage 不低於拆分前
- [ ] 無 import error / dead route（`uvicorn --reload` 啟動 clean，無 warning）
- [ ] `services/cop_service.py` 有 contract test 鎖定 source enum（含 `pi-node` 預留）
- [ ] `pi_*_repo` + `sync_repo` 通過「federation 介面相容性」測試（模擬非 PWA 上游節點推送）
- [ ] MapLibre GL JS 已上線、24/7 主題切換可運作、entity layer 抽象介面已定義
- [ ] 規格書 v3.0 merged
- [ ] **Tag**：`command-v2.2.0`（原規劃 1.0.0；後端因 #24/#26 RBAC/deploy 早期已躍 2.x，故版號階梯 rebase 到 2.x 線）

### Compliance touchpoints

- **ASVS V14 / NIST SSDF PW.7**：測試覆蓋（DoD #1）
- **ISO 25010 可靠性 / 可維護性**：federation 介面契約測試（DoD #4）
- **供應鏈** (CLAUDE.md)：
  - MapLibre GL JS — BSD-3，AWS/Meta/MapTiler/Felt 共同維護，非中國
  - PMTiles / Protomaps — BSD-3，maintainer Brandon Liu（US/台美籍），非中國
  - milsymbol — MIT，瑞典
  - JetBrains Mono / Noto Sans TC — 皆 OFL，離線打包
- **Design System provenance**：vendor 自 [WaveInk DESIGN.md + colors_and_type.css](https://codeberg.org/winson3QQ/WaveInk)，commit hash 釘版於 `docs/design/POLICY.md`；兩處 divergence（MIL-STD-2525 token、JetBrains Mono webfont）明文記錄
- 證據路徑：`command-dashboard/tests/reports/p1-baseline.html`、PR# + commit hash 寫進 commit message

> **設計細節**：P1-10 子項（a-h）完成紀錄、視覺等級 ladder（T0→T3）、為 P2 鋪路成果、P1-11 Tier 1-3 分解 → [`docs/roadmap/p1-map-ux.md`](roadmap/p1-map-ux.md)
> **Key management 規格**：P1-12 FIDO2/HKDF 架構、三子項 12a/b/c、Bundling 決策 → [`docs/roadmap/p1-key-management.md`](roadmap/p1-key-management.md)


---

## Phase 2 — TAK Server 整合（COP 第一個外部來源）

**目標**：部署官方 TAK Server，完整接通 TAK 雙向介面——上行（CoT 位置/事件 + GeoChat + MEDEVAC + shape 幾何）流入 COP，下行（Mission API 指令下達）推送現場 ATAK；充分利用 Marti REST API（在線人員、Mission、DataSync、EXCHECK、影像串流）；地圖渲染採 MIL-STD-2525 符號（含 planned/actual 指令圖層）。

> **開發策略（主軸 + 降階）**：TAK 整合怎麼沿 TAK 軸開發、能力↔API↔ICS 對應矩陣、**TAK Server 掛掉時的三階降階模型**、連線/信心兩軸狀態表示（來源 chips）→ [`docs/roadmap/tak-integration-strategy.md`](roadmap/tak-integration-strategy.md)。情境面「為何要這功能」見 [`docs/reference/tak-use-cases-config.md`](reference/tak-use-cases-config.md)（19 情境深挖，按 OODA 循環組織）。

> **[2026-06-09 漸進重構]** 本 Phase 自此依上述兩份（19 情境 use-cases + TAK 整合策略）**漸進重構**——注入既有 ROADMAP 缺的維度，**不拆 P2-01~25 層結構（歷史留痕）**：① 操作面 grounding ＝上方連結；② 情境檢討衍生 → 見〈**跨來源 / 新增 feature（SC）**〉第一級區塊（Phase 3 後，與各 phase 平行）；③ 降階紀律 → 入 Definition of Done；④ 活躍 code（#173/#176/#177）→ P2-14 / P2-26 連結。**檢討花的力氣一律留痕，不蒸發**（即使 later/不做也記）。

> **[Reality check 2026-06-04 — [#98](https://github.com/winson3QQ/ICS_COMMAND/issues/98)]**：盤 main `0db382f` 實際 code。**TAK 上行鏈路（P2-01 部署 / P2-02 `tak_service.py` CoT XML 解析 + 8089/9000 訂閱 / P2-08 XXE 測試）全 greenfield、零實作、XML lib 未選**；但 **P2-04 的下游落地層已被 P1 完整建好且刻意對齊 CoT**——`CoPEntity` v1 凍結（CoT 欄位對齊 + `source="tak"` 預留，P1-03/#15）、`cop_entity_repo`（version_clock CAS + stale 過濾 + TAK 風 soft-delete，P1-15/#29）；`routers/tak.py` schema + RBAC 已掛但 handler 是 stub、`cop_service.normalize_cot` 是 `NotImplementedError`。**關鍵路徑＝ P2-01+P2-02（重活、無 C0 繼承）→ P2-03 wire + P2-04 normalize（輕，下游全建）**。P2-05 可並行（設計 crosswalk §6/§8 已 LOCKED、管線預建；milsymbol 未 vendored / 2525 框零 / `POLY-ROUTE_TYPES` 仍寫死 hex；**疊加非全換**；MGRS #56 完全獨立可先做）。**動工前 3 個 drift（詳 #98）**：① **P2-06 指向錯層** — `snapshot_repo` 是 unit-KPI 聚合非 per-entity COP 時間軸，回放路徑（`cop_entity_tracks`／新表）須先重新確認；② **step-ca 憑證現 24h 且 federation 缺 peer cert** — 90 天 patch 與 client/peer cert profile + TAK Java keystore 信任 SOP 須提早於 C3-B；③ **CSP wasm** — milsymbol 若走 wasm fallback 需補 `wasm-unsafe-eval`（P1-10h 刻意未預放）。供應鏈現況乾淨；三相依 license 已圈定，lock 前逐一驗來源。

### Scope

| Item | 說明 |
|---|---|
| **——— 【基礎 ① 上行管線：CoT → COP → 上圖】純後端 + 渲染，已全通（P2-10 真機驗收）———** | — |
| ✅ P2-01 | 部署官方 TAK Server（Docker compose，Java/Spring + Postgres）；產出 `deploy/tak-server/` 部署文件與 cert 設定 SOP。**[完成 — [#100](https://github.com/winson3QQ/ICS_COMMAND/pull/100) `35ed3d8`（issue #99）]**：`deploy/tak-server/`（compose + `.env.example` + `pki/issue-tak-certs.sh` step-ca→JKS + README）；TAK Server 5.7（官方 Docker 包不含 compose/.env，本目錄補）；M1 原生 arm64 build。**實機 boot dogfood（M1/16GB）**：DB tier 通（initdb+SchemaManager+cot/martiuser healthy）、Ignite 叢集 ACTIVE、**CoT streaming `:8089` 開**（P2-02 接點解鎖）；過程抓並修 3 整合 bug（volume chown / CoreConfig 密碼 / TAKIgniteConfig race）。web/API `:8443` 初次未起（api JVM 活但 Tomcat 不綁）→ **[#101](https://github.com/winson3QQ/ICS_COMMAND/issues/101) 已解（2026-06-05）**：兩根因皆憑證問題（非 Ignite 脆弱性）——① server 憑證須 RSA 非 EC（jwkSource bean 寫死 RSAPublicKey，`14b5373`）；② 缺 `fed-truststore.jks`（messaging 無條件部署 distributed-federation-manager Ignite service，缺檔→SSLContext 失敗→server node 掛→client 全斷線，PR #104 `fa38200`）。**實機 VERIFY-PASS**：messaging/api Started、:8443 綁定、mutual-TLS+admin RBAC 通、web GUI（Metrics Dashboard）可登入。`command-v2.2.1`。 |
| ✅ P2-02 | `services/tak_service.py`：CoT XML 解析（規格相容，禁止自創欄位）；TAK Server 推播訂閱（TCP/SSL 8089 或 federation port 9000）。**[Wave 0+1 — [#103](https://github.com/winson3QQ/ICS_COMMAND/pull/103) `d5deaad`（#102）]**：`parse_cot_xml()` XXE-safe（defusedxml forbid_dtd/entities/external）+ `CoTEventIn` 移 `schemas/tak.py` 補 CoT 必填 start/how/version（修 ↔CoPEntity contract gap）+ 時間戳 normalize 秒精度 Z（對齊 stale 字典序）+ detail 抽 callsign/remarks/結構化 dict。**[Wave 2 — [#109](https://github.com/winson3QQ/ICS_COMMAND/pull/109) `e7358fb`（#106）]**：`subscribe()` pytak[with-takproto] mTLS 連 :8089、`readuntil(</event>)` 分幀、濾 `t-x-takp-v` TakControl、斷線退避重連（只在收資料後重置）、只呼叫 `ingest_cot_event`（#105，不碰 cop_service.py）。reality-check 實測定案：8089 預設 **v0 明文 CoT XML**（v1 protobuf 選配）、強制 **fullchain client cert**（leaf+intermediate，非 user enrollment）。TLS **fail-closed**（無 cafile 須顯式 opt-in）。25 測試。/code-review（W1 2 + W2 2 修正）、/security-review（W1 0 + W2 1 TLS fail-closed）。**活 server 端到端閉環實測 PASS**：推 CoT → subscribe → 真 ingest 落 `cop_entities`。<br>**剩 follow-up（非 P2-02 scope）**：① **lifespan wiring** — 把 `subscribe()` 在 app 啟動跑成背景 task → **P2-03**（routers/tak.py wire + app 整合）；② client cert 正式簽發＝擴充 `issue-tak-certs.sh` 出 client fullchain（現只簽 server/fed-truststore）。 |
| ✅ P2-04 | `cop_service.normalize_cot(event)` — CoT → COP entity 映射（type / uid / time / stale / lat / lon → COP `entity` + `track`）。**[完成 — [#108](https://github.com/winson3QQ/ICS_COMMAND/pull/108) `28490cf`（issue #105）]**：`normalize_cot(CoTEventIn)->CoPEntity`（純函式，欄位直通 + source='tak' + detail→attributes + track 安全取值）+ **`ingest_cot_event` 共用接縫**（normalize→upsert version_clock CAS→cop_hub 廣播；out-of-order/重送守門；create 綁 active 場）由 P2-02 W2(#106)/P2-03(#107) 共同呼叫。13 新測試 + code/security review 過（0 HIGH）。track 逐格時間軸寫入留 P2-06。 |
| ✅ P2-03 | `routers/tak.py`：從 stub 升級為真實 endpoint（接 TAK Server federation push + REST 查詢）；schema 已存在於 Stage 1 帶過來的 stub。**[完成 — [#112](https://github.com/winson3QQ/ICS_COMMAND/pull/112) `01ab9b6`（issue #107）]**：main.py lifespan 依 `TAK_ENABLED` 啟動 `subscribe()` 背景 task（啟動失敗永不擋 app 開機）+ `core/config.py` TAK_* env + `routers/tak.py` async ingest endpoint（RBAC/CAS/skip）+ `issue-tak-certs.sh` Section 5 離線簽 fullchain client cert。11 測試、/code-review 1 修正、/security-review 0 finding。**活 app 端到端 + 視覺驗收 PASS**：起 app(TAK_ENABLED) → 推 4 種 affiliation CoT 到新竹 → P2-05 milsymbol 渲染成 2525 符號（友軍藍矩形/敵軍紅菱/中立綠方/不明黃四葉）即時上圖。<br>**＋ P2-02 W2(#106) 移交的 production 整合 follow-up**（subscribe() 已實作+活 server 端到端實測過，但「在 app 真正跑起來」屬本項）：① **lifespan wiring** — main.py FastAPI lifespan 把 `tak_service.subscribe()` launch 成背景 task（startup）+ shutdown cancel；沒這步 CoT 不會在 production 流入。② **client cert 正式簽發** — 擴充 `deploy/tak-server/pki/issue-tak-certs.sh` 出 COP subscriber 的 **fullchain** client cert（leaf+intermediate；現只簽 server/truststore/fed-truststore）。③ **config plumbing** — `core/config.py` 加 env-driven `cot_url` + cert 路徑餵 `build_subscribe_config`。憑證區分見 [`deploy/tak-server/README.md`](../deploy/tak-server/README.md)「:8089 streaming 憑證 ≠ :8443 web」段（:8089 只需 fullchain CA-trusted cert，**不需** UserManager enroll；enroll 是 :8443 web UI 才要）。 |
| ✅ P2-11b | **CoPEntity schema 擴充**：(1) `source` enum 加 `"command"`（P1-03 解凍）；(2) `planned: bool`（空心/實心 2525 框，P2-13）；(3) `simulated: bool`（合成注入標記，`how="h-g-i-g-o"`，P2-19）。**分階段 B 完成**。<br>**[planned/simulated — [#143](https://github.com/winson3QQ/ICS_COMMAND/pull/143)（issue #140）]**：零風險 `ADD COLUMN`（`_m017` INTEGER 0/1 + repo int↔bool、schema bool default False）。<br>**[source `'command'` — [#144](https://github.com/winson3QQ/ICS_COMMAND/pull/144)（issue #141）]**：SQLite **CHECK 不可 ALTER** → 整表 rebuild。`legacy_alter_table` 實測無效（3.50 仍改寫 child FK）→ 改 **`foreign_keys=OFF` + DROP old + rename new**（避 child FK cascade 刪光 tracks/links），自含 commit/autocommit/BEGIN-COMMIT dance 繞 transaction 內 PRAGMA 無效，**動態重建所有原 index**（讀 sqlite_master，含 m015 team_color）。`_m018` up/down 共用 `_rebuild`、idempotent。`/code-review` 抓真 bug（#1 team_color index 漏建 → 動態重建；#4 ROLLBACK 遮蔽 → began 旗標）+ `/security-review` 0 finding。rebuild 安全網測試（資料完整 + cascade 運作）+ 常數==Literal 防漂移。<br>**[security wiring 守則]** P2-13/P2-19 wiring 時 `planned`/`simulated` 不可被 untrusted CoT 偽造（`_TAK_UPDATE_FIELDS` 已排除、normalize_cot 不設）。**解鎖 P2-13（planned）/ P2-19（simulated）** |
| ✅ P2-05 | 前端 `static/js/map.js` 加 MIL-STD-2525 符號渲染（用 [milsymbol](https://github.com/spatialillusions/milsymbol) JS lib，MIT，非中國維護）。**MGRS grid 一併於本項實作**（zoom-adaptive 密度、淡灰底 + 強調 100km / 10km 分層、label 避讓）——P1-10 已預埋 MapLibre Symbol Layer 接點。**affiliation-aware 渲染模型**（敵我=2525 框 / 類型=NAPSG 象形 / severity=halo；情境表 A–E 由 cot_type 前綴分流；建立流程 type-first）依 [`classification-crosswalk.md`](../command-dashboard/docs/design/classification-crosswalk.md) §6。**＋ route/polygon（線/面）符號對齊 MIL-STD-2525 Tactical Graphics（control measures：route≈axis-of-advance、polygon≈area-control）+ 走 design token**——現況為 app 自訂寫死 hex（`POLY_TYPES`/`ROUTE_TYPES`），非標準、違 POLICY hex doctrine，正規化留本項，見 [`classification-crosswalk.md`](../command-dashboard/docs/design/classification-crosswalk.md) §8 <br>**[進度 2026-06-05]**：拆三子塊 — ✅ (b) hex→token 調色盤對齊（[#111](https://github.com/winson3QQ/ICS_COMMAND/pull/111) `16fb5b2`）、✅ (a) MGRS 多 zone+GZD+1-2-5 精度（[#113](https://github.com/winson3QQ/ICS_COMMAND/pull/113) `ad14bd3`，closes #56）；✅ (c) milsymbol 2525 affiliation 框（[#115](https://github.com/winson3QQ/ICS_COMMAND/pull/115) `07d59e7`，closes #110）。**P2-05 完成**；`cmd-v1.0.1`。框內 function 細分（兵種圖）/ TAK 單位全鏈視覺（配 P2-03）為後續。 |
| ✅ P2-10 | **地基層驗收 + E2E 上行測試**：地基層（P2-06a～P2-11b）完成後的整合驗收里程碑。涵蓋：(1) 真實 ATAK/iTAK 推 CoT → 地圖 < 5s 更新（**human verify**）；(2) shape 幾何渲染（route/polygon **human verify**）；(3) mock TAK integration tests；(4) CoT 內容層驗證（座標越界拒絕、callsign 字元白名單、type prefix 白名單——任何 ATAK 裝置可推 CoT，**此為最後一道防線**）；(5) XXE 防護驗測（`defusedxml`）；(6) GeoChat 路由負向（不進 `cop_entities`）。**全通後才進 P2-12/P2-13**。<br>**[進度 2026-06-07]** 純後端項先行：✅ (4) 內容層 type/callsign 白名單（[#157](https://github.com/winson3QQ/ICS_COMMAND/issues/157)，`CoTEventIn` field_validator，:8089+REST 兩路徑；座標越界 P2-04 已擋）；(5) XXE 已於 P2-02 `defusedxml`、(6) GeoChat 負向已於 P2-07 落地。<br>**[2026-06-08 驗收完成 — 真機 iTAK(iOS) dogfood，[PR #167](https://github.com/winson3QQ/ICS_COMMAND/pull/167)]**：TAK Server 5.7（Windows Docker, mTLS）上線，iTAK `曙豐-3QQ-CH1` → :8089 → ICS subscribe → `cop_entities` → 地圖。✅ (1) 真機 CoT < 5s 上圖（位置/2525 marker、`version_clock` live 遞增、真 GPS lat/lon）；✅ (2) shape 幾何 human-verify（矩形 `u-d-r`/多邊形 `u-d-f`/圓 `u-d-c-c`→48-gon、顏色 ARGB、solid/dashed/dotted 三筆觸）；✅ (3) mock 整合測試齊（`test_tak_subscribe`/`test_tak_events`/`test_cop_router`/`test_cop_ws`）。dogfood 連帶修：#159/#4 矩形封閉、#5 顏色、#134 圓、#160/#161 軟 stale、how-based 生命週期、t-x-d-d 刪除、TAK 圖形唯讀、iPad 觸控/版面（PR #167 三 commit；`command-v2.4.0`/`cmd-v1.2.1`）。**上行 E2E 全通 → 解鎖 P2-12/P2-13**。**殘留（不擋上行驗收）**：iTAK 對 dropped marker/drawn shape 刪除不送串流信號 → **可靠刪除生命週期 deferred P2-14**（已留痕）；持久化資安 MEDIUM 同掛 P2-14。<br>**[2026-06-08 #161 後續更正]**：上文「#160/#161 軟 stale、how-based 生命週期」描述的是 WIP（`0ba8fde`）的 **last-heard / how-based 舊解，經 reality check（真機 iTAK + 活 server wire/Marti 雙證）推翻**——改對齊 **TAK 原生 honor `stale` + honor `<archive/>`**（archived 持久豁免 stale、非 archived 過 stale 移除）：[#174](https://github.com/winson3QQ/ICS_COMMAND/pull/174) `f3db03e`，`command-v2.4.1`/`cmd-v1.2.2`，#161 closed。設計動機（CoT 為何如此）見 `docs/reference/` MITRE CoT Developer Guide + memory `tak-streaming-archive-stale-vs-mission`。衍生 [#173](https://github.com/winson3QQ/ICS_COMMAND/issues/173)（ICS reconnect 漏靜態標記，streaming 不重播 → 需 Marti 權威 resync → P2-14）。<br>**[2026-06-13 #236 後續修正：callsign 白名單過嚴 → 單位隱形]**：iTAK 預設名「iTAK: A1」（帶 `:`）原本整筆 CoT 驗證失敗被丟棄 → 單位在 ICS COP 隱形（連帶位置/類型/GeoChat 全沒，非僅名字）。修法 **A+B**：(A) `:` 入 callsign 白名單；(B) **改淨化非丟棄**——`_sanitize_callsign` strip 非白名單字元、`str|None` 歸一、截 128，保留 marker（位置+類型是 COP 命脈）；`CoTEventIn`＋`DownlinkCommandIn` 同步。對齊感測層底線「marker 不消失」(#213)；危險字元仍 strip（真 XSS 防線在 sink：textContent／出向 quoteattr／SQL 參數化）。 |
| **——— 【基礎 ② 後端資料萃取：上行 CoT → 結構化（各 feature 後端面）】純後端，自動化測試驗收 ———** | — |
| ✅ P2-06a | **CoT 軌跡寫入接線**（`insert_cop_track` 零 caller bug 修復）：`cop_entity_repo.insert_cop_track()` 定義存在但整個 codebase 零 caller → `cop_entity_tracks` 表一直是空的。修法：`ingest_cot_event` 每次 upsert 後同步寫一筆軌跡點。抽樣策略：per-entity 最短間隔 5s（ATAK 最短 2s push，不節流則爆量）；帶 active `exercise_id`（無演習 = NULL）。清理：exercise 刪除 cascade；archive 保留。**P2-20 AAR 回放的唯一資料基礎，不做則 AAR 無資料可播**。<br>**[完成 — [#121](https://github.com/winson3QQ/ICS_COMMAND/pull/121) `7dd5d7d`（issue #120）]**：`ingest_cot_event` create/update/reinsert 三點接 `_record_track` + per-uid 5s 抽樣（config `TRACK_MIN_INTERVAL_S` 可覆寫）。**設計 B（取代上文「帶 active exercise_id」）**：tracks **不存** exercise_id，靠 uid JOIN cop_entities 取得（SoT 單一不反正規化）；exercise 刪除靠既有 `uid ON DELETE CASCADE`。<br>**review follow-up（本收尾 PR）**：修 `_within_min_interval` aware−naive 相減 TypeError（REST push time 未正規化 → 軌跡靜默漏寫）、抽 `_helpers.iso_to_dt` 共用（消第 4 份 fromisoformat idiom）、抽樣間隔 configurable。`/code-review`（1 真 bug 已修）+ `/security-review`（0 finding）；測試 9 + ingest 回歸全綠。 |
| ✅ P2-06b | **軌跡查詢 API**：`GET /api/exercises/{id}/tracks?uid=&from=&to=` → 回傳該演習所有 entity 位置時間序列（`{uid, t, lat, lon, hae}`，按 t 升序，分頁）。**RBAC**：`COMMAND_ROLES`（軌跡含人員位置 PII，非 READ_ROLES 可見）。P2-20 回放前端的資料來源。<br>**[完成 — issue #123]**：repo `list_tracks_by_exercise`（設計 B `JOIN cop_entities`，不存 exercise_id）+ router `GET /api/exercises/{id}/tracks`（回**全欄位** `{uid,t,lat,lon,hae,heading_deg,speed_mps}`，t 升序，limit/offset 分頁，`from/to` 經 `iso_utc` 正規化）。**RBAC 零額外工**：自動繼承中央 gate（`role_enum.py:118` `/api/exercises/*` 非 DELETE → COMMAND_ROLES）。涵蓋演習(ttx)+實戰(real)；limit 服務端上限 5000（對齊 RT-L4）。**附 P2-06a NULL gating**：reality check 釐清 NULL=「非演習也非實戰」（非「實戰池」，實戰是 type='real' 有 id）→ `_record_track` 在 `exercise_id=NULL` 時不寫軌跡（entity 照常 upsert），並正名 code 註解。測試：repo 6 + router 6（含 operator/observer 403、404、ttx+real）+ NULL gating，全綠；`/code-review` 修 5 項（ORDER BY `t.id` tiebreak 防分頁漏/重、`iso_utc` date-only 補當天起訖、docstring filesort 正名、抽 `tests/api/conftest.py` 共用 role fixtures、clamp 註解去假對齊）+ 跨場軌跡 misattribution 開 [#125](https://github.com/winson3QQ/ICS_COMMAND/issues/125) follow-up；`/security-review` 0 finding<br>**[2026-06-29 ✅ #125 解決 — 軌跡 denormalize（PR [#446](https://github.com/winson3QQ/ICS_COMMAND/pull/446)，m038，`backend-v2.27.1`；隨 #267 收尾）]**：根因＝上述「設計 B（tracks 不存 exercise_id，靠 JOIN cop_entities）」→ 同 uid entity 跨場時，其歷史軌跡全歸 entity **當前** scope。修＝`cop_entity_tracks` 加自身 `exercise_id`（寫入時逐點凍結當下 entity scope ＋回填既有），`list_tracks_by_exercise` / timeline_service / kpi_service 全改查**軌跡自身**欄位（不再 JOIN entity 現值）→ 軌跡歸屬於寫入當下定讞，entity 之後改 scope（如 #267 archive→NULL）也不影響歷史歸場。**本段上方「設計 B JOIN」描述自此被 m038 取代。**回歸測試：entity 重 stamp 回 NULL 後 AAR timeline/KPI 仍正確取得該場軌跡。 |
| ✅ P2-06c | **CoT `<__group>` / `<status>` 小隊欄位提取**（前端小隊模組基礎）：資料**已進** `attributes`（`_extract_detail` 通用收 + `normalize_cot` 整包），本項為**提取成一等欄位** `team_color` / `role` / `battery`。<br>**[完成 — issue #126，設計 A]**：reality check 定案走 **CoPEntity 欄位 + DB column**（取代原 PR [#119](https://github.com/winson3QQ/ICS_COMMAND/pull/119) 述的 attributes 鍵 B）—— 對齊 `callsign`/`remarks` 提取先例，且 P2-06d `GROUP BY team_color` 走 column + `idx_cop_entities_team_color`（B 的 `json_extract` 無 index）。`_extract_squad` 從 `__group.name`→team_color（`title()` 標準化、**不強限 enum** 免丟 Orange/Teal 等 ATAK 真實色）/`__group.role`/`status.battery`（→int 0-100 防 garbage）；migration `_m015` 加 3 column + index；`_TAK_UPDATE_FIELDS` 納入（battery 隨 update 刷新）；原 `__group`/`status` 仍保留 attributes（CoT 忠實）。測試：normalize 提取/標準化/garbage/真 fixture + ingest update 刷新，回歸全綠。**解鎖 P2-06d** |
| ✅ P2-06d | **小隊聚合 API** `GET /api/cop/squads?exercise_id=`（[#119](https://github.com/winson3QQ/ICS_COMMAND/pull/119)）：按 `team_color` 分組聚合，回 `[{team_color, total, online, offline, avg_battery, centroid_lat, centroid_lon}]`。online/offline 以 `stale` 判定；centroid 後端算；同端點服務 live（active id）+ AAR（歷史 id）。**RBAC**：`READ_ROLES`（態勢摘要非 PII 軌跡）。**依賴**：P2-06c。<br>**[reality check]** 後端聚合 API + `dashboard_service.build_dashboard()` 整合 `tak_squads`（修補 dashboard 對 TAK 盲視 —— 屬實：`build_dashboard` 現無任何 cop_entities/TAK 引用）為本項交付；但原 PR 列的消費方 **UI（右側欄 TAK Presence 彙總列、zone-c 摘要）與 P2-12「UI 面板統一落地」重疊** → UI 落點併入 P2-12 設計，本項只交付後端。<br>**[完成 — [#130](https://github.com/winson3QQ/ICS_COMMAND/pull/130)（issue #128）]**：repo `aggregate_squads`（單句 SQL GROUP BY team_color → total/online/offline/avg_battery/centroid，**未分隊 NULL 組保留**）+ router `GET /api/cop/squads`（resolve_scope 三態安全閘，RBAC 自動 READ_ROLES）+ `build_dashboard` 加 `tak_squads`。total 含 stale → offline 反映「該隊有幾顆但失聯」。測試 16；`/code-review`+`/security-review` 乾淨（SQL 參數化 / IDOR resolve_scope 擋 / RBAC 正確）。背景 agent 併行實作 |
| ✅ P2-07 | **GeoChat 誤路由修正—後端**（reality check 2026-06-05 bug）：CoT type `b-t-f` 存進 `cop_entities`，行為錯誤。修法（純後端）：(1) `tak_service._consume_cot` type prefix 路由（`b-t-f` 不進 `ingest_cot_event`）；(2) 新 `services/chat_service.py` + `chats` 表（`sender_uid` / `callsign` / `message` / `group` / `lat` / `lon` / `time` / `exercise_id`，對齊 ICS-214 Unit Log）；(3) 後端 `html.escape()` 處理 `message`（XSS 防線在後端）。測試：`b-t-f` 不得進 `cop_entities`（負向）；XSS 負向（`<script>` 不得原樣輸出）；exercise scoping 對齊。**通聯記錄面板 UI → P2-12 統一落地**。<br>**[完成 — [#131](https://github.com/winson3QQ/ICS_COMMAND/pull/131)（issue #129）]**：**分流點實放 `ingest_cot_event` 共用接縫**（取代上文「`_consume_cot`」—— reality check 發現 REST push 路徑也漏，共用接縫一處擋 :8089 串流 + REST push 兩條）：`type.startswith("b-t-f")` → `chat_service.ingest_chat` + return。新 `chat_service`/`chat_repo`/`schemas/chat` + `chats` 表（`_m016`）；message 存前 `html.escape`（XSS 後端防線）；exercise_id 綁 active 場；只交付後端。測試 7（不進 cop_entities / 進 chats / escaped / scoping / 真 fixture / 非 b-t-f 不誤擋 / group 不取 groupOwner）；`/code-review`（修 group 誤取 groupOwner 布林旗標）+ `/security-review`（0）。並行於 P2-06d<br>**[2026-06-13 #248 收條雜訊修正]**：GeoChat 收條/ack（`type=b-t-f` 但 `<remarks>` 空、uid=訊息 GUID、無 `__chat`）原被當空 chat 寫入 → 前端「直接」頻道冒空白列（Windows dogfood 實證）。`ingest_chat` 加空訊息守門：`remarks` 去空白為空 → 回 None 跳過、不寫不廣播（非空照常）。測 +5（None/空/純空白→跳過、非空照進）。**doctrine 拍板 A（2026-07-02 定案，#248 / #397 已 close）**：DM 經 Marti resync 入 COP 即可（live 串流僅廣播是 TAK 路由特性）；**出向 DM 已完整**（#216：指揮部點地圖 marker → 底部切「→ 私訊」→ `<marti><dest>` 投遞，可主動私訊任一單位）→ 私下溝通通道已足。即時全收 DM（B）+ ICS presence 自報（#397）**決定不做**：牴觸「非 GPS 裝置不自報 SA」（#214）＋ DM 複製/縮 resync 週期的 PII/RBAC 代價 vs 效益低。 |
| ✅ P2-08 | **CoT `<shape>` 幾何萃取 + `services/geometry_service.py`**（純後端）：ATAK `<shape>` 幾何埋在 `attributes` 被忽略，地圖只渲染 `<point>` 單點。修法：(1) 抽 `services/geometry_service.py`（CoT `<shape>` + DataSync GeoJSON 統一解析入口，P2-14 共用，避免重工）；(2) `CoTEventIn` 加選填 `geometry` 欄位；(3) `normalize_cot` 依幾何類型映射到 `kind='route'`（polyline）/ `kind='polygon'`（polygon），沿用 P1-15 管線。<br>**[完成 — [#133](https://github.com/winson3QQ/ICS_COMMAND/pull/133)（issue #132）]**：新 `geometry_service`（re-parse CoT `<shape>`/`<link>` → GeoJSON，**不污染 `_extract_detail`** 既有一層提取；`geojson_to_vertices` 轉前端格式；P2-14 DataSync 共用入口）+ `CoTEventIn.geometry` + `normalize_cot` shape 分支（kind=route/polygon + vertices，**沿用 P1-16 格式零前端改動**；lat/lon **沿用 CoT `<point>`** 忠實對位）。**circle/ellipse 先不支援** → follow-up [#134](https://github.com/winson3QQ/ICS_COMMAND/issues/134)。`/code-review`+`/security-review`：修 garbage geometry→REST 5xx（`geojson_to_vertices` 防禦不可信座標）+ 移除 centroid 覆寫（對位失準）+ Polygon ≥3 頂點；XXE 已防、無新注入面。測試 17 全綠 |
| ✅ P2-09 | **MEDEVAC 9-line 正規化—後端**（純後端）：CoT MEDEVAC 9-line 欄位（pickup / frequency / casualties…）埋在 `attributes` blob。修法：`normalize_cot` 偵測 → 萃取 9-line 進 `attributes` 結構化子物件；`severity` 設 critical。<br>**[完成 — [#137](https://github.com/winson3QQ/ICS_COMMAND/pull/137)（issue #135），設計 B]**：reality check 校正——9-line 其實**已在 attributes**（`_extract_detail` 通用收 `<_medevac_>` 屬性，同 `__group` 模式，非規格所述「埋在 blob 待解析」），本項實為**提取結構化摘要 + 設 severity**，對齊 P2-06c/P2-08 先例。**設計 B（進 attributes，不開新表）覆蓋 ROADMAP 內 TAK-F 紅隊 `medical_records` 分流建議**：9-line 是聚合後送態勢（傷亡數 by precedence / pickup 位置 / 通訊），**非個別病患 PII**（無姓名病史）→ 進 attributes 隨 COP 廣播、指揮部透明可見，符合單一 COP 不開新表紅線。`_extract_medevac`（precedence→int、casevac→bool、case-insensitive，原始 `_medevac_` 完整保留）+ `<_medevac_>` 偵測設 critical + **severity 單調升級**（ingest update 只升不降）。測試 normalize 7 + ingest 2；`/code-review`（cleanup + #135-3 severity 升級缺口修）+ `/security-review`（0 finding；pre-existing TAK ingest XSS gap → #136）。<br>**待驗證**：真機 ATAK CASEVAC type 字串 + `<_medevac_>` 屬性語意（precedence 數量 vs 布林）→ fixture 依 FreeTAKServer CoT domain 文件建構，待 P2-12 human verify 終驗（#135）。**MEDEVAC incident card UI → P2-12 統一落地** |
| **——— 【基礎 ③ Marti REST 主動查詢層（:8443）】下行 / DataSync / EXCHECK / presence 共用前置 ———** | — |
| ✅ P2-11 | **TAK Marti REST 抽象層 `services/tak_rest_client.py`**（純後端，**P2-12~P2-19 共用前置**）：指揮部「主動查」TAK Server :8443 Marti REST（vs :8089 被動收串流）。<br>**[完成 — [#139](https://github.com/winson3QQ/ICS_COMMAND/pull/139)（issue #138），⚠️ 規格更正：cert 非 OAuth2]**：原規格述「OAuth2 client credentials（JWT Bearer），非 enrolled cert」**前提錯誤** —— reality check 證實部署是官方 TAK Server 5.7-RELEASE-43（tak.gov），Marti :8443 走 **client cert（mTLS）**；OAuth2 是 OpenTAKServer（第三方）才有（SoT：`deploy/tak-server/README.md` + `issue-tak-certs.sh` 全 step-ca cert；見 memory `tak-server-marti-cert-not-oauth`）。改正：**複用 P2-03 既有 `TAK_CLIENT_CERT/KEY/CAFILE`**，砍掉 JWT 取得/refresh + OAuth2 SOP。交付 `build_marti_ssl_context`（fail-closed，無 CA 不裸奔）+ `TakRestClient`（`get_json`：rate-limit + 指數退避（cap 30s）+ 4xx 不重試 + 204 空 body；`poll` scheduler stop_event 軟停 + best-effort；`asyncio.Lock` 並發保護）+ `build_tak_rest_client` factory；config `TAK_MARTI_URL`；HTTP=aiohttp（pytak 已帶，非中國）。測試 15；`/code-review`（204/並發鎖/退避 cap/isawaitable）+ `/security-review`（0 finding，+ `_join_url` 杜絕 SSRF host override）。<br>**待真機（P2-10/P2-12）**：Marti `clientEndPoints` 端點格式 + :8443 cert 是否需綁 TAK user/role |
| **——— 【應用 ④ 雙向核心（下行 + DataSync）】直接建於 ③ Marti；需 Human Verification；P2-14 已提前解鎖（#173/#176，不受 TTX gate）———** | — |
| P2-13 | **Mission API 下行指令 + COP 指令圖層（Human Verification 里程碑）**：(1) `tak_rest_client.py` Mission CRUD（依賴 P2-11）；(2) 前端「下達指令」UI（選 entity → 指定 group → 推 Mission；**COMMAND_ROLES 限定按鈕**）；(3) MIL-STD-2525 空心框（`planned=True`）vs 實心框（`planned=False`）地圖視覺（P2-11b schema 基礎）。**RBAC**：COMMAND_ROLES 嚴格限（403 負向測試必通）。**Audit**：每次 push 強制寫 `MISSION_PUSH_TAK operator={uid} group={name} cop_entity_id={id} mission_id={id}`（指揮行為，**不得 best-effort**）。**E2E bidirectional 里程碑**：P2-10 + P2-13 → 真實 ATAK ↔ ICS Dashboard 雙向驗證<br>**[2026-06-09 A slice 落地 — [#179](https://github.com/winson3QQ/ICS_COMMAND/pull/179)（merge `5ee77a3`）]**：⚠️ **機制更正**——上文 (1)「`tak_rest_client.py` Mission CRUD」**非下行正解**：#176 reality check 實測官方 Marti **無 REST 廣播端點**、且 mission 寫入 owner-role **無狀態 REST 取不到**（memory `tak-marti-authz-model`）。**A slice = streaming-write 即時下行**：`POST /api/tak/downlink` → 建 CoT 寫進 :8089（與訂閱同一條雙向連線）→ server 廣播現場 ATAK，不需 mission/owner/admin（只要 truststore cert）。已交付：`services/tak_downlink`（build_command_cot + send_cot）+ endpoint（**COMMAND_ROLES + audit-first** `TAK_DOWNLINK`，DoD 不得 best-effort 守住）+ unit 6/api 7；`/security-review` 0、`/code-review`（stdlib-logger kwargs bug→structlog）。**端到端對活 iTAK 驗過**（production code path → 現場訂閱端實收）。**仍 pending（不勾 ✅）**：(2) 前端「下達指令」UI + (3) `planned` 空心框視覺 = 下個 slice；**持久/可靠刪除半**（reconnect 重播）明確歸 **P2-14**（live broadcast 無持久保證）；full ATAK↔Dashboard 雙向 human-verify 待前端。<br>**[2026-06-09 分層 doctrine 對齊]** 指令 = COP 感知層的「指令態」（`planned=true`，非觀察、非事件），見 [`cop-event-layering.md`](design/cop-event-layering.md)。**P2-13/P2-30 邊界**：下行為**共用基建**（`send_cot`+幾何序列化+audit+共享閘）的兩消費者——P2-13 發「指令」、P2-30 分享「既有感知標記」；**P2-13 下「線/區指令」依賴 P2-30 的幾何序列化器** → **定路線：先做 P2-30 共用基建，再回補 P2-13 B(寫 ICS COP)/C(UI)**。指令應可連結 originating event（#5，依賴 P2-27），B/C 設計勿堵死。 |
| P2-14 | **DataSync client**（ATAK Mission / Route / 照片，Marti REST `/missions/` + `/sync/`）：<br>**[2026-06-08 gate 解鎖]** 本項（尤其新增的 **(C) 權威 resync** 與 **(A) 可靠刪除**）與 **#161 / #173 為同一個 TAK 同步 feature**——reconnect 漏靜態標記、streaming 無可靠刪除是**當前 dogfood 就會遇到的痛**（非「實戰才需」）→ 自實戰延伸層**提前解鎖**，**不受 TTX Gateway(P2-22) 限制，可即刻動工**。建議承接 #161 之後做；完整 DataSync（照片/route 等）可分階段，先落 (C) resync + (A) 可靠刪除 + 安全網。工作：(1) `tak_rest_client.py` poll（依賴 P2-11）；(2) geometry 透過 `services/geometry_service.py`（P2-08 共用）轉 cop_entity；(3) 照片：reference-only URI（**永不 follow**——防 SSRF，OWASP A10；`datasync_service` 不得有任何 `requests.get(uri)` 呼叫）；(4) 新 `services/datasync_service.py`。**動工前 DataSync API reality check**<br>**[P2-10 dogfood 帶出的兩個需求（[PR #167](https://github.com/winson3QQ/ICS_COMMAND/pull/167)）]**：(A) **可靠刪除**——iTAK 對 dropped marker/drawn shape 刪除**不送任何串流信號**（無 `t-x-d-d`、無 past-stale 重送，實證），串流模型偵測不到 → 標繪/標記的刪除生命週期須改走此 server 權威通道（明確 create/update/delete 事件）。`t-x-d-d` 串流處理已落地（self-marker 或 Mission 來源刪除即時生效），但不涵蓋 ephemeral 標繪。(B) **🔒 資安（/security-review 補做，MEDIUM）**：`how=h-*`（人工標記/繪圖）改持久化後**豁免 stale → 無自動清理、無 entity 總量上限**；被盜用的 mTLS 場端憑證可 fire-and-forget 灌爆 COP/DB（限速 60/s 但時間拉長無界）。本 server 權威生命週期為正解；過渡期若需先擋，加「`how=h-*` 絕對最大壽命安全網」（`stale` 超過可調 `PERSIST_MAX_AGE`≈7d 即移除）或每來源 entity 上限 + 操作員「移出 COP」。<br>**[威脅模型補註]** `t-x-d-d` 刪除命令在 TAK 信任邊界內（持證 client）可刪任何 `source='tak'` entity（manual/command 受所有權守門保護）；屬 TAK 本質行為，補入 `docs/compliance/threat_model.md`（隨 P2-17）。<br>**[2026-06-08 #161 更正 + #173 補入]**：(A)(B) 的持久化訊號**不是 `how=h-*`，是 CoT `<archive/>`**（#161 reality check，[#174](https://github.com/winson3QQ/ICS_COMMAND/pull/174)）→ 安全網（B 的 `PERSIST_MAX_AGE`/總量上限/操作員「移出 COP」）改以 `archived` 為界。**新增 (C) 權威 resync**：實測此 server streaming **不對重連者重播靜態標記**（latestSA 只推 PLI）→ ICS 重啟/斷線會漏既有靜態 marker、與 server 分歧 → 須 Marti 主動拉權威全集對帳（同 poll 管線）→ [#173](https://github.com/winson3QQ/ICS_COMMAND/issues/173)。t-x-d-d 實測：清 live 畫面（ICS+client drop）但**不清 server archive**（需 server 端/mission 才徹底 purge）。<br>**[#176 reality check（另一 session 進行中）]** P2-14 Mission/DataSync 路徑成立性驗證 / #173 範圍修正 / 與 P2-13 下行**共用面協調**，由 [#176](https://github.com/winson3QQ/ICS_COMMAND/issues/176) 收斂。<br>**[2026-06-09 #177 L1 端到端實測補（[#178](https://github.com/winson3QQ/ICS_COMMAND/pull/178)，memory `tak-marti-authz-model`）]** 動工必讀：(C) resync 的 `GET /cot/sa?start=&end=&{l,b,r,t}` **已驗回真 CoT**（讀只需 truststore-trusted cert，毋須 register）；**寫入（可靠刪除/權威增刪）受 mission-role `MISSION_WRITE` 把關**——ICS 寫 cert 如何取得 owner role 是 P2-13/P2-14 寫路徑動工第一關（無狀態 REST creator 落 `defaultRole`，沒拿到）。<br>**📐 動工必讀**：CoT archive/stale/可靠刪除模型 → [strategy §4b](roadmap/tak-integration-strategy.md)（自 memory 收編）。<br>**[2026-06-11 ★ dogfood 定論 —— (A) 可靠刪除『做不到』，推翻「Mission 為正解」前提（真機 iTAK iOS + 活 5.7，[#194 comment](https://github.com/winson3QQ/ICS_COMMAND/issues/194#issuecomment-4677048174)）]**：跑完整 mission 刪除傳播 dogfood（乾淨隔離：mission 通道送達確認、無 streaming 混淆）。**所有 server 端刪除信號 iTAK 地圖都不清**：mission `REMOVE_CONTENT`（iTAK 收到 REMOVE 訊息但地圖 marker 不消失）、t-x-d-d（含 mission dest）、**DELETE 整個 mission（任務包整個沒了 marker 還在）**、（前次）streaming t-x-d-d/stale/墓碑。→ **iTAK(iOS) 對 server 任何刪除信號都不移除地圖 marker，只能裝置本機刪 = client 硬限制，非 ICS 可解**。**直接推翻上文 (A)「改走 server 權威通道（明確 delete 事件）」與「Mission 來源刪除即時生效」**——Mission 也救不了。**P2-14 重切**：**(A) 可靠刪除 → 降為『client 限制·不可解·operators 本機刪』，不建 Mission delete 子系統（會白做）**；**P2-14 收斂以 (C) 權威 resync（#194，`/cot/sa` 已敲定可行）為主**。連帶發現：`PUT/DELETE missions/{n}/contents` **必帶 `creatorUid`** 否則 200-no-op（memory `tak-marti-authz-model` 已補）。<br>**[2026-06-11 ✅ (C) 權威 resync 落地 — [PR #210](https://github.com/winson3QQ/ICS_COMMAND/pull/210) squash `32ddcdd`，Closes #194]**：拉 `GET /Marti/api/cot/sa` 的 `<events>` 快照逐筆走既有 `ingest_cot_event` 補進 cop_entities（normalize/場域歸屬/CAS/廣播沿用）。新 `services/tak_resync.py`（`build_window` 小窗避 400 gotcha / `resync_once` **只 upsert 不刪** / `run_resync` / `resync_on_connect`）+ `tak_service.parse_cot_events`（拆 `<events>`，抽 `_event_from_element` 與 `parse_cot_xml` 共用、XXE 同防）+ `tak_rest_client.get_text`（raw body）+ `POST /api/tak/resync`（COMMAND_ROLES + audit-first，未配置 422 / 拉取·解析失敗 503）+ `tak_runtime.start` 後 fire-and-forget resync（補 #173）。**reality check 落地**（活 5.7 實測）：`/cot/sa` 回 `<events>` XML 集合非 JSON、大窗 400 gotcha→小窗、bbox 省略、只 upsert（測試鎖死）。`/verify`（temp DB ingested=1 + 活 app on-connect ingested=0 冪等）/ `/code-review`（CoTParseError→503、resync task 留 ref）/ `/security-review` 0。全套 960 passed。**仍 pending（P2-14 未整體完成）**：(A) 可靠刪除 + **full DataSync = ⚠️ blocked on ATAK 驗證**（→ [#211](https://github.com/winson3QQ/ICS_COMMAND/issues/211)）—— **「(A) 不可解 / DataSync 低價值」結論全來自 iTAK(iOS) dogfood，ATAK(Android) 從未測；ATAK 歷來對 Mission/DataSync 支援最完整，很可能 honor server 刪除信號 + 渲染 → 結論待 ATAK dogfood 確認或推翻才定案，不做半套**；**(B) 安全網**（archived 持久 entity 無上限 → `PERSIST_MAX_AGE`/hard-delete，**已併 [#207](https://github.com/winson3QQ/ICS_COMMAND/issues/207) retention sweep**；reality check 校正：非 resync 造成、metric 用 `received_at` 齡非 stale、hide-vs-reclaim 待拍板）；**[已知限制]** 中途 :8089 斷線重連不自動 resync（只覆蓋 app 啟動/手動 toggle，subscribe 內部重連不觸發 → 手動端點補）；resync **不回填軌跡**（newest-first SA，舊點被順序守門擋，僅收斂最新位置）。<br>**[2026-07-05 ★ 照片 dogfood 帶出 Mission 平面正解 → 開 [#506](https://github.com/winson3QQ/ICS_COMMAND/issues/506)]**：#503 上行照片真機 dogfood 實證 **ATAK 分享 marker 附件走 Data Sync/Mission，非單純 Enterprise Sync 上傳**——無 mission 則附件留手機（file store 恆 0、CoT detail 無 attachment 參照、裝置只 `GET /Marti/sync/missionquery` 從不 `POST upload`）。⇒ 印證 Mission/DataSync 是照片/附件的**正解通道**、非可選。**盤點**：ICS 目前 Mission 平面**零消費**（stream 成熟、Enterprise Sync 讀側半成品 #503、Mission 全缺）。**系統性建置計畫 M0-M4 收斂於 #506**：M0 讀取地基 `tak_missions.py`→M1 消費進 COP（附件 hash 掛 entity＝可靠連結，取代 #503 錯用的 uid-search）→M2 照片 pivot→M3 上傳（下行；真機定 `/Marti/sync/upload` 格式：無多餘 hash 參數、檔名純 ASCII）→M4 可靠刪除。**#211 待的 ATAK dogfood 部分進行**：真機 ATAK 現已打到 8443（原 WG 未轉發 Marti API 埠是根因，已修 mTLS 保護，見 memory `tak-wg-containerized-vpn`），但 ATAK 端建 Data Sync feed 仍卡「頻道發現」（未解）。見 memory `tak-mission-datasync-plane` / `tak-filestore-image-uplink`。<br>**[2026-07-05 ✅ #506 M0+M1 落地（**merged 0bc4484，backend-v2.37.0 / frontend-v1.28.0，部署中**）]**：Mission 平面**從零到能消費** + **地點型雙向照片一條龍**（#503 上行 uid-search 顯示 + M3 下行上傳，reality-check 破解格式、翻案「device 牆」；#503 已關）。code+security review 兩輪（修 M4 over-delete、上傳 magic-byte、下載 per-hash faction gating）。原以下 M0/M1 細節：M0 `services/tak_missions.py` 讀取地基（list/get/cot/changes + name 白名單防注入）；M1a `sync_mission_once`（mission `/cot` → 既有 ingest 縫，複用 resync）；M1 wiring（`TAK_MISSION_FEEDS` config 清單 + `run_mission_sync` + `POST /api/tak/mission-sync` COMMAND_ROLES+audit）；M1 poll（on-connect + 週期雙軌，掛 tak_runtime）。**code+security review PASS**（無 bug/漏洞）。reality-check 定調（throwaway mission 已清）：`/cot`=`<events>` 直接 parse、只含真投遞進 mission 的 CoT、WRITE cert 可建、普通 delete(creator) 可清、deepDelete 要 admin。**未竟**：M1b 附件連結 / M2 照片 pivot / M3 上傳 = 卡「有 attachment 的真 mission」（device）；M4 可靠刪除 = 下個 reality check。合併時補 backend MINOR bump + tag。<br>**[2026-07-05 ✅ M4 可靠刪除落地 + review 修正]**：mission 內容移除 → 軟刪 ICS **自身 COP** 的 cop_entity（server-authoritative）——**給了 P2-14 串流做不到的可靠刪除**（client 端 iTAK 不 honor 是 client 硬限制、非此可解）。reality-check（throwaway mission，已清）：`/changes` 發 REMOVE_CONTENT 帶 contentUid、最終一致性 ~1-3s。**code/security review 抓 over-delete 並修**：改 `/cot`-diff（只 diff 真進 mission /cot 的 marker、不碰 :8089 串流 live entity），刪除核心抽 `cop_service.soft_delete_tak_entity`（t-x-d-d 與 M4 共用、source=tak 守門）。**Mission 平面凡不吃真機的（讀 M0 / 消費 M1 / 刪 M4）全完成**；剩 M1b 附件連結 / M2 照片 / M3 上傳硬卡真機（要 ATAK 附圖進真 mission）。<br>**[2026-07-06 ★ #507 照片橋根因翻案 + ICS TAK 身分模型「兩面一軸」]**：二輪 dogfood **直查 prod DB `resource` 表**推翻 07-05「照片非走 Enterprise Sync／P2P 牆」判斷——**現場照片確實上 server Enterprise Sync**（3QQ-atak4 17 檔／3Q-itak3 10 檔，含先前一直 404 的 hash），ICS 抓 404 真因＝**group 隔離**：`ics-marti-read` 不在任何 TAK 群（不在 UserAuthenticationFile）→ 只讀 public → 現場 blue 群檔 404（對照 `ics-cot` 在 blue 實測抓得到 10.6MB JPG＝坐實）。⇒ **非架構牆、是可修權限**。落地「兩面一軸」消費半身（`services/tak_identity` infra 證群歸屬**宣告式 SoT** + registrar 多群 register + 編排 reconcile/apply〔開機 best-effort 警示 + sysadmin `POST /tak/infra-groups/reconcile` 一鍵對帳 + 面板消費半身漂移顯示〕）+ **presence beacon**（ICS 自報 SA〔a-f-G-U-C，誠實不送偽造遙測 #214〕現身現場 Contacts，下行定址，預設 OFF opt-in）。四輪 code/security review PASS（修 anon_exempt 誤豁免 read/write）。**backend-v2.38.0 / frontend-v1.29.0**。連帶更正 memory `tak-mission-datasync-plane`（M2「讀 mission 拿照片」前提作廢，照片走 fileshare→ES 非 mission）/`tak-filestore-image-uplink`（P2P 牆→group gate）。照片橋端到端實機驗待部署後。<br>**[2026-07-06/07 ✅ #508/#509/#509-P2/#509-P3 現場照片『雙向』端到端上 prod + 真機實測通]**：**上行**＝#508 CoT 種類分流器（denylist 攔 `b-f-t-*`/`t-x-*` 非實體家族，不誤存成 marker）+ #509 附件模型（`b-f-t-r`→抓 mission-package zip→ingest 內含 marker + 掛照片，backend-v2.39.0）+ #509-P2 **主動輪詢橋**（補 ATAK 不廣播 `b-f-t-r` 之漏——真機定讞 ATAK 照片有上 Enterprise Sync 但通告定址不到 ICS；改週期 `GET /Marti/api/sync/search` 撈 `missionpackage`；hotfix 2.40.1 **不濾 tool** 才收得到 `tool=private` 的新 ATAK 分享）。**下行『顯示到現場』**＝#509-P3（真機抓包定讞：現場 client 顯示照片要**推 mission-package zip 非裸 jpg**；`build_mission_package` + `build_fileshare_cot`〔senderUrl 用裝置面對位址、純 `<fileshare>` 無 peerHosted/ackrequest〕 + `POST /api/tak/downlink/photo`〔COMMAND+faction+magic-byte+audit〕；甲=為既有 marker 補照片／乙=ICS 自建 marker 配圖〔非 TAK 來源情報〕同端點；`GET /api/tak/clients` 收件人挑選 + 前端「📡 推照片到現場」UI〔廣播/點對點多選〕。backend-v2.41.0→.2 / frontend-v1.30.0）。**echo hotfix 2.41.2**：ICS 下行廣播的 `b-f-t-r` 會回自己 subscriber → `handle_fileshare` 跳過 `senderUid=ICS-CMD` 防自我 re-ingest 污染 COP（對齊 #509-P2 輪詢跳過 `creatorUid=ICS-CMD`）。**上下行真機實測皆通**。詳見 memory `tak-filestore-image-uplink`。未竟小改：乙的 UI 未接（後端已支援）；`_seen_hashes` in-memory（重啟重掃，storage 去重無害）；TAK server 資料無 retention（見 memory `tak-server-data-lifecycle-unmanaged`）。 |
| **——— 【應用 ⑤ 前端面板 + 連線 UX】需 Human Verification；P2-12 = MEDEVAC / 小隊 / 通聯 / chips(SC4) 共同前端面 ———** | — |
| P2-12 | **新 UI 面板統一落地（Human Verification 里程碑）**：P2-07/09 後端已備，本項統一設計所有新 panel——**一次 layout 設計決策（整合缺口 #5）** 涵蓋以下三面板，統一 human verify：(1) **TAK 在線人員面板**（`tak_rest_client` poll clientEndPoints/contacts，30s）；(2) **通聯記錄面板**（`chats` 表，前端 `textContent` 禁 `innerHTML`，可點位置跳地圖）；(3) **MEDEVAC incident card**（P2-09 後端，9-line 格式化，severity=critical pulse）。**RBAC**：以上三面板均 `READ_ROLES`。**cop_hub None 廣播不洩漏業務 COP 欄位**（presence 廣播只帶 callsign/UID/online-status）。整合缺口 #4/6 確認（resolve_scope bypass + cop_stream.js null 廣播守門）。<br>**[P2-09 連動依賴]**：(a) MEDEVAC fixture 真機 ATAK CASEVAC 抓包驗證 type 字串 + 屬性語意（[#135](https://github.com/winson3QQ/ICS_COMMAND/issues/135)）；(b) **TAK ingest path server-side XSS escape**（[#136](https://github.com/winson3QQ/ICS_COMMAND/issues/136)）—— MEDEVAC incident card / 通聯面板渲染 `medevac.*`/chat 前須先落地後端 escape（pre-existing gap，P2-09 `/security-review` 發現）<br>**[2026-06-11 panel (2) 通聯記錄面板落地 — [PR #223](https://github.com/winson3QQ/ICS_COMMAND/pull/223)（#213 b1）]**：右欄「事件追蹤 ｜ 通聯」tab + 單一時間流 + `[room]` 標籤 + 動態 filter chips（≤1 房間隱藏）+ 7s poll + 未讀紅圈；**TAK 停用隱藏 tab**（`cop.js` 派 `tak:conn-state`，同源 `takConnState`）。`GET /api/chat`（`chat_repo.list_chats` newest-first 三態 scope + `chat_service.build_chat_feed` limit+1 探截斷，`resolve_scope` 綁場，**READ_ROLES 含 observer**、跨場 PII 由 resolve_scope 守門；POST 預留 COMMAND_ROLES 給 #216）。**(b)/#136 要求達成**：message 已 P2-07 `html.escape`，前端 `decodeChatMessage` 還原後**經 `textContent` 落 DOM（無 innerHTML）**。`/code-review`（修 `_range_bound` 漏 `iso_utc` 的 date-only `to` 邊界漏秒）+ `/security-review` 0；後端 973 / 前端 292 / doc_sync 綠。**真機 iTAK dogfood 端到端 PASS**（iTAK→TAK Server→ICS 訂閱→chats→通聯面板）；連帶修 2 bug（`_resizeEvtList` 隱藏時誤量釘 50px、chips 單房間撞 TAK「All Chat Rooms」）。**仍 pending（P2-12 未整體完成）**：panel (1) TAK 在線人員 + (3) MEDEVAC card + human-verify 里程碑；#213 b2 即時 WS / b3 marker 連結 / 欄位盤點(#193) / O/C 限可見後置。<br>**[2026-06-12 panel (2) b2 即時 WS 推播 — [PR #225](https://github.com/winson3QQ/ICS_COMMAND/pull/225)（#213 b2）]**：通聯入 `chats` 即 `cop_hub.broadcast({op:chat,...}, exercise_id)` 推 `/ws/updates` 訂閱者（**重用既有 CopHub、多工 `chat` op、不開新 socket**）→ 面板即時更新；poll 7s→**30s safety-net**（斷線/重連對帳）。`ingest_chat` async 化；前端 `cop_stream.js` `case "chat"` 派 `chat:new` → `chat_panel._onLiveChat`（`mergeLiveChat` 去重 + 依 (t,id) 排序對齊 poll）。`/code-review` 修 3（sorted insert / chips 只在房間集合變時重建 / `LIVE_CAP=200` 對齊 poll 窗）+ `/security-review` **0**（廣播 scope 與 entity 同源 server-derived `exercise_id`+`resolve_scope`+strict `wants`；`textContent` sink；無新端點，與 b1 `/api/chat` 政策一致）。後端全綠 / 前端 299 / doc_sync 綠。零衝突（#224 未碰 b2 任一檔）。即時體感 human verify 待實機。<br>**[2026-06-12 panel (2) b3-1 點 marker→過濾通聯 — [PR #233](https://github.com/winson3QQ/ICS_COMMAND/pull/233)（#213 b3-1，⏳待 human verify 後 merge）]**：點地圖 TAK 單位 marker → 右欄通聯切到該單位（by-sender 過濾）。`map.js` `tak-units-icon` click → 派 `map:unitSelected {uid,callsign}`；`chat_panel.js` `parseSenderUid`（`GeoChat.<uid>.<room>.<id>`→uid，b2 dogfood 實證）+ `filterChatsBySender`（**uid 精準比對**）+ 可清除「篩 \<callsign\> ✕」chip。`/code-review` 修 2（callsign fallback 跨單位誤混→uid-only；dotted uid 邊界註記）+ `/security-review` **0**（client-side 過濾已授權 `_chats`、textContent sink、無新端點）。前端 305 / doc_sync 綠。**b3 仍 pending**：b3-2 點訊息→跳地圖 highlight（+無座標 fallback by-sender）。<br>**[2026-06-12 panel (2) b3-2 點訊息→跳地圖 — [PR #233](https://github.com/winson3QQ/ICS_COMMAND/pull/233)（#213 b3-2，⏳待 human verify）]**：點通聯訊息 → 地圖飛到發訊單位 + 點位 pulse（`map.js` 監聽 `chat:locateSender` → `pickLocateCoords`（entity 現位 / chat 自帶座標）→ `flyTo` + `_pulseAt` 暫態 circle 環，點符號專用不動 zones pulse）；**無座標或地圖未就緒 → 派 `map:senderNotLocated` → fallback by-sender 過濾**。`/code-review` 修 2（無 map 也派 fallback；`pickLocateCoords` 抽 `coord_tools.js` +6 測）+ `/security-review` **0**（座標純數字、textContent 不變、無新資料）。前端 311 / doc_sync 綠。<br>**[2026-06-12 b3 dogfood UI 細修 + human verify PASS]**：訊息定位改**長按**（對齊事件卡長按 pointer 機制）；highlight 從 pulse 圈改**對話泡泡 💬**（`maplibregl.Marker` 內層 span 動畫避定位 transform 衝突、anchor 右上）。`/code-review`（整分支複審，0 阻斷；長按全域 handler 與事件卡互不干擾、無 timer/marker leak）+ `/security-review` **0**（client-side、textContent、無新端點）。**真機 Mac+iTAK dogfood 全 PASS**：b3-1 點 marker→過濾、b3-2 長按訊息→跳地圖+泡泡、無座標 fallback。**dogfood 副發現**：reset-db/reset-exercise 漏清 chats 表 → 另開 [#237](https://github.com/winson3QQ/ICS_COMMAND/issues/237)（獨立，**✅ 2026-06-22 修：reset-db 全清 chats + reset-exercise 清演習場域 chats；AAR 乾淨起點 + PII 隨 reset 清；`backend-v2.13.2`**）。**#213 a/b1/b2/b3 全落地 + 驗收**（待 merge #233）；**仍後置**：欄位盤點(#193) / O/C 識別與限可見 / chat 斷線 backfill。<br>**[2026-06-12 marker 詳情統一（欄位盤點 #193）— [PR #238](https://github.com/winson3QQ/ICS_COMMAND/pull/238)]**：marker 互動統一為**點=詳情、長按=篩通聯**（感測層操作一致，不分誰放）。TAK 單位 click → `_openTakUnitDetail`（唯讀 modal：敵我態 / type(2525 grammar) / callsign / 座標(MGRS+經緯) / 備註——對齊 iTAK marker tap；TAK 為外部來源故唯讀，所有 READ_ROLES 可看）。長按分流走 `maplibre_core` onLongPress 帶**螢幕 point** → `queryRenderedFeatures` 命中 `tak-units-icon` → 派 `map:unitSelected`（篩通聯），命中空地 → 建立對話框。`_suppressMarkerClick` 旗標抑制「長按放開後補發的 click」（對齊事件卡 `_evtCardDidHighlight`；下次按下/輕點重置）。`/code-review` 修 2（座標列守門改 lat/lon 成對才渲染、避 lat-only TypeError；`_MARKER_LONGPRESS_LAYERS` 收斂為 TAK-only——contact 由拖曳 handle 短路且 `_onContactClick` 不認旗標，列入反致詳情+篩通聯雙觸發）+ `/security-review` **0**（modal title 走 `textContent` sink〔cop.js〕、body 全 `_escapeHtml`/`_coordValueHTML`、座標純數字、無新端點）。前端 311 / doc_sync 綠。**真機 Mac+iTAK dogfood human verify PASS**：短按詳情、長按翻頁到通聯、放開不誤彈詳情。**仍後置**：contact 長按=篩通聯 / O/C 識別與限可見 / chat 斷線 backfill。<br>**[2026-06-13 #213c marker 詳情欄位補全 — 入向顯示忠實度]**：`_openTakUnitDetail` 補上已收進 attributes/頂層卻沒顯示的欄位——平台＋裝置（CoT `<takv>`：iTAK·iPhone / ATAK-CIV·SAMSUNG SM-G9810）、來源單位（`<link>` parent_callsign；#217 實證反映「最後廣播者」非必原始建立者）、小隊（`<__group>` team_color·role）、電量（battery）、狀態（archived 持久）。資料早已 ingest（本 dogfood provenance 之謎的解），純前端 surface；空欄自動隱藏、外部值全 `_escapeHtml`（XSS 防線在 sink）。eslint + vitest 315 綠；**真機 iPad dogfood human verify PASS**（3QQaTak→ATAK-CIV、3QQ itak→iTAK·30%、atakF1→來源單位 3QQaTak＋持久）。declutter/刪除一致性歸 #217（不在本項）。<br>**[2026-06-13 #250 通聯面板 UX 三修（dogfood）]**：(1) **reload 通聯延遲** — `onEnterDashboard` 登入後立即補一次 chat poll（原通聯只靠自身 30s interval，事件 `poll()` 登入即跑 → 通聯比事件晚出）；(2) **房間 locale 收斂** — `All Chat Rooms`（iTAK 英）/`所有聊天室`（ATAK 中）收斂為單一「廣播」標籤且**併入「全部」**（不另立房 chip、不撞「全部」）；(3) **已讀水位持久化** — `_lastSeenId` 改存 `localStorage`（原 module 變數每 reload 歸 0 → 重開 session 全變未讀）。純前端、eslint+vitest 317 綠；iPad dogfood 驗 (1)(2) PASS、(3) 驗中。<br>**[2026-07-03 #463 公測回報：operator 可發通聯 — [PR #466](https://github.com/winson3QQ/ICS_COMMAND/pull/466)]**：公測回報 operator 需能看+發訊息。「看」早已開放（`GET /api/chat`=READ_ROLES）；本次補「發」——`POST /api/tak/chat` 由 COMMAND_ROLES 放寬 **WRITE_ROLES**（role_enum 窄洞、比照 #180 share；端點本已 audit-first + 發話者身分 server 端決定，問責不減）+ 前端 compose 對 operator 顯示（`auth.js canSendChat` 收斂第 3 份 inline role 清單）；observer 維持唯讀。**review 撤過度設計**：依「operator 比照 commander」撤掉一度誤補的 DM faction 檢查——commander 改動前即無此檢查、TAK 靠 `<dest callsign>` 投遞而 callsign 非可靠 faction 鍵（ICS 層擋不牢、crafted callsign 可繞），**出向跨陣營隔離權威邊界＝TAK #344 group 隔離**（留誠實註解 + `test_operator_dm_not_faction_gated_at_ics` 鎖「刻意不擋」防再犯）。`/code-review`（high，先誤補 DM 檢查、經使用者「照 commander」裁示後撤）；後端 pytest 全綠 / vitest 377 / ruff+eslint clean。**backend-v2.28.1**（RBAC 行為改變）/ **frontend-v1.21.1**（compose 顯隱；rebase 於 #464 v1.21.0 之上）。 |
| ✅ P2-23 | **ICS↔TAK 連線狀態顯示（[issue #163](https://github.com/winson3QQ/ICS_COMMAND/issues/163)，2026-06-07 P2-10 dogfood 衍生）**：TAK subscribe(:8089) 在 lifespan 背景 task 跑，但**前端完全看不出連上沒/斷了沒**（dogfood 時要看 server log 才知道）。落地：(1) 後端暴露 TAK 連線健康——subscribe socket 狀態 + **last CoT timestamp** + Marti(:8443) 可達性 →（`/api/status` 擴充或新 `/api/tak/health`）；(2) 前端 header **TAK 連線指示燈**（對齊既有 `status-lamp` 機制：綠＝連線收串流／黃＝連上但無流量／紅＝斷線／灰＝未啟用 `TAK_ENABLED=false`）。**RBAC**：狀態唯讀 `READ_ROLES`，不洩漏業務 COP 欄位（只報連線 meta）。依賴 P2-03（subscribe 已落地）。**[完成 — [#169](https://github.com/winson3QQ/ICS_COMMAND/pull/169) `879f4ee`]**：狀態燈四態（灰/紅/黃/綠）+ `GET /api/tak/status`（last_cot_age server 算、不信 client 時鐘）；cop.js 每 5s 輪詢。**真 iTAK 端到端驗收 PASS**（燈紅→綠、iTAK 單位 + CoT 幾何上圖）。**[衍生 deploy 修補 — [#170](https://github.com/winson3QQ/ICS_COMMAND/issues/170)/[#171](https://github.com/winson3QQ/ICS_COMMAND/pull/171) `75819fb`]**：dogfood 真機 onboarding 撞兩關（truststore 缺 intermediate / server SAN 缺裝置 IP），把手動憑證修補固化進 `issue-tak-certs.sh`（雙層 CA truststore + `TAK_EXTRA_SANS` 參數化，prod=FQDN doctrine）。 |
| ✅ P2-24 | **TAK 連結 runtime 開關（[issue #164](https://github.com/winson3QQ/ICS_COMMAND/issues/164)，2026-06-07 P2-10 dogfood 衍生）**：現況 TAK 連線由啟動時 `TAK_ENABLED` env 決定、改需**重啟 process**；指揮部應能 **runtime 啟用/停用** subscribe，不重啟。落地：(1) `SYSADMIN_ONLY` 限定開關 `POST /api/tak/connection {enabled}`；(2) 後端能 start/stop subscribe 背景 task（不重啟 process）；(3) 狀態持久化（重啟後維持選擇）。**RBAC 決策（2026-06-08）= `SYSADMIN_ONLY`**：關掉 TAK＝整 COP 態勢全斷、blast radius 最大 → 比照演習刪除等「破壞性最高」動作鎖 admin；**commander 不可**（避免指揮官誤觸把全 COP 弄瞎；斷線屬基礎設施控制，非 per-incident 指揮決策）。**安全**：每次開/關**強制 audit**（`TAK_CONNECTION_TOGGLE operator={uid} enabled={bool}`，**不得 best-effort**——記不下不放過）；403 負向（commander/operator/observer）必通。依賴 P2-23（狀態顯示）+ P2-03。<br>**[2026-06-11 完成 — [#198](https://github.com/winson3QQ/ICS_COMMAND/pull/198) `a78de4b`]**：`POST /api/tak/connection`（SYSADMIN_ONLY，audit-first `TAK_CONNECTION_TOGGLE`）+ 新 `services/tak_runtime.py` 控制器（訂閱 task 升模組級單一 handle、`asyncio.Lock`、lifespan 與 toggle 共用）+ 持久化 `config.tak.connection_enabled`（開機優先序：持久值>env）+ `/api/tak/status` 改讀 `effective_enabled()`（燈跟開關走）+ role_enum gate。**code-review 修**：`cop.py _resync_tak_if_shared` gate 由 `config.TAK_ENABLED`→`effective_enabled()`（否則關掉後 move 已分享標記仍推、純 toggle 開啟反不推）。測試 `test_tak_connection_toggle`＋改寫 `test_tak_lifespan`→tak_runtime；全 suite 綠、`/code-review`＋`/security-review` 0 殘留。**純後端**（row DoD 無 UI 項；前端按鈕另算，會碰 cop.js=P2-33 領地）。無版號 bump。<br>**[2026-06-11 前端尾完成 — `feat/issue-164-tak-toggle-frontend`（issue #164）]**：reality check 推翻原「會碰 cop.js=P2-33 領地」顧慮——P2-33a/part-3/P2-33b 皆已 merge，按鈕落點（系統 tab + cop.js 燈邏輯）與 pending 的 P2-33c（map.js `_highlightEvent`/junction 聚合）零重疊；唯一在飛的 TAK 分支 #194 只動後端。落地：① **後端 additive** —— `tak_runtime.is_configured()`（URL+cert/key 齊備；與開關正交，config 屬部署層只消費）+ `/api/tak/status` 補唯讀 `running`/`configured`。② **系統 tab（管理員後台·SYSADMIN-only·PIN-gated）** 加「TAK 連線」開/關 toggle + 唯讀連線狀態行（`admToggleTak`/`_admLoadTakConn`/`_admRenderTakStatus`，POST 後即時重抓燈、用 `running` 顯示「開了但未連線」、網路錯誤 catch 還原）。③ **燈號認實化** —— 抽 `static/js/tak_light_state.js` 零依賴 `takConnState()`/`takLightState()`，依 enabled/configured/running/connected 分 6 態，**消除舊版「沒 task 在跑卻顯示斷線重連中」謊報**（未備妥→黃非紅；啟動失敗→「啟動失敗」非「重連中」）。**config 觀念定案**：使用者不設任何 config，「開＝包含 config」（部署時 `issue-tak-certs.sh`+env 備妥）；toggle 只消費、不暴露 cert——與 **P2-26 L2（TAK admin console cert/role 管理）** 為同地兄弟，唯讀診斷狀態即未來 P2-26 接縫。測試：後端 `is_configured`+status 新欄位；vitest `tak_light.test.js`（14 例含 takConnState/takLightState + 向後相容）。pytest 940 / vitest 281 / eslint 0；`/code-review`（2 修：網路錯誤回饋 + admin/header stale 狀態對齊）＋`/security-review` 0 finding。**活 app 對真 TAK 5.7 container 端到端驗**（connected/running/configured 全 true，四態行為正確）。**前端 MINOR `frontend-v1.4.0`**（CMD_VERSION v1.3.0→v1.4.0）。<br>**[2026-06-11 出向閘 + 燈號修正 + 重連對帳 — [#224](https://github.com/winson3QQ/ICS_COMMAND/pull/224) `2777e48`（[#222](https://github.com/winson3QQ/ICS_COMMAND/issues/222)）]**：dogfood 抓出開關**只 gate 入向訂閱、出向漏 gate**——關掉 TAK 仍能建/廣播 TAK 標記到 iTAK。① 後端 `push_downlink`/`share_entity_to_tak` audit 前 `effective_enabled()`→**409**（停用不送、不稽核）；② 前端長按「📍 TAK 標記」類別（原「感知/敵情標記」改名）+ 既有標記廣播鈕/右鍵選單 gate `_takEnabled`（cop.js `tak:status` 事件、僅成功輪詢更新避免閃）；③ **燈號 connected 即綠「已連線（可收發）」**，移除「無入向串流→黃」誤判（單一 client 無入向屬正常；`tak_light_state.js`+`auth.js` 鏡像）；④ **重連 outbound 對帳**（`reconcile_shared_outbound`+`cop_entity_repo.list_shared_tak_entities`）掛 `tak_service.subscribe(on_connect=)`，**每次 (重)連上**觸發（含 subscribe loop 內部自動重連，不只 `start()`）→ 斷線期間 shared 標記移動、重連後自動補回現場端；gate 同 `effective_enabled`（review 修 fail-open：原只 gate cert-presence＝停用仍推洩漏位置）。**dogfood 殘留＝iTAK client 限制**（iTAK 斷線恢復須完整重連才收得到 ICS 重推、非 ICS 側）→ 對齊 [#211](https://github.com/winson3QQ/ICS_COMMAND/issues/211)（ATAK 待驗）+ P2-14 doctrine。測試：後端全綠（disabled→409 / fail-open 守門 / on_connect 每次觸發 / 重連對帳真 DB）+ vitest 294 + pyflakes/eslint 0；`/code-review`（high，6 修）＋`/security-review`（0）。**未 bump 版號**（待決定 backend PATCH/MINOR + frontend PATCH + tag）。 |
| ✅ P2-25 | **地圖 TAK 篩選器（map-side TAK filter，2026-06-08 P2-10 dogfood 衍生）**：真機可見 COP 易塞——多 affiliation marker + 矩形/多邊形/圓繪圖疊一起（dogfood 截圖驗證），操作員需在地圖上篩選顯示哪些 TAK 內容。**純前端 view 篩選（不刪資料、不改 server、不送 TAK）**——各操作員篩自己的地圖視圖。**v1 篩選維度**：(1) **圖層開關**：TAK 單位（2525 markers）/ TAK 繪圖（polygon/route/circle）各自顯示/隱藏（延伸既有 `_layerVis` + EntityLayer `setVisible`）；(2) **敵我 affiliation**：友/敵/中立/不明勾選（對映 2525 色，前端依 `type` 推 affiliation；MapLibre filter expression）；(3) **stale 顯示**：顯示/隱藏已過 stale 的活追蹤。**enhancement**：team_color 分組、callsign 文字搜尋、how（活追蹤 vs 人工標註）分流。**RBAC**：`READ_ROLES`（純視圖篩選、無 server 變更、**無 audit**——不是改資料只是改自己看到什麼）。落地於既有地圖工具列圖層控制。**依賴** P2-05（2525 渲染、affiliation 推導已備）；可與 P2-23 並行。[issue #168](https://github.com/winson3QQ/ICS_COMMAND/issues/168)。**[完成 — #169 `879f4ee`]**：「TAK 單位」圖層開關 + affiliation（友/敵/中/不明）/ stale 子篩選（MapLibre `setFilter` 純視圖、不動資料）。真機驗證篩選即時生效。 |
| **——— 【應用 ⑤b COP 分層治理：感知標記 / 事件 / 決策對應（TTX 前；2026-06-09 設計對話 + reality check 衍生）】———** | — |
| P2-27 | **感知標記 / 事件 / 決策 分層 + 導航鏈 + 事件梳理**：把「感知標記（一等公民、可共享）」與「事件（工作流、留 ICS、N:1 聚合標記）」**分層解耦**——現狀建 event 配一個 `attributes.kind='event'` 的 cop_entity（JSON 名義綁定、**無 FK、會孤立、僅 1:1**），decisions 又獨立 → 報(chats)→事(events)→決(decisions)→行(下行) 四表割裂、無導航。工作：(1) 規範化 event↔marker(N:1) + event↔decision 關聯（FK / 關聯表，取代 attributes JSON glue）；(2) 一處導航完整鏈（任一節點 → 全脈絡）；(3) 統一位置 SoT（現 events.lat/lon 死 + cop_entity.lat/lon 活 + location_zone_id 三份）。**📐 動工必讀**：[`cop-event-layering.md`](design/cop-event-layering.md)（分層 / 資料模型 SoT）+ [`cop-marker-event-decoupling.md`](design/cop-marker-event-decoupling.md)（操作 / 呈現 / 緣起 SoT）。依賴：cop_entities(#29)/events 既有 schema。<br>**[2026-06-09 跨 session 協調 — 與 P2-30 part 2 並行]** P2-30 part 2（分享既有標記到 TAK）**刻意只待在 TAK/下行層**、與本項零重疊：(a) 動的檔僅 `services/tak_downlink.py`（加 `entity_to_cot` adapter）+ `routers/tak.py`（加 `POST /api/tak/share/{uid}`）+ 新測試——**不碰 `routers/cop.py`/`cop_service.py`/`events`/`decisions`/schema**（全本項領地）；(b) 對 cop_entities **唯讀**（get_cop_entity），零 schema 改動；(c) adapter 只依賴穩定欄位（uid/type/lat-lon/`attributes.vertices`），點 vs 幾何 key 在 `attributes.kind∈{route,polygon}`——本項重構 event↔marker 關係不影響之；`kind='event'` 標記被 share 時當點推，與本項解耦。**唯一共享詞彙 = `attributes.kind`**：P2-30 讀 route/polygon、本項處理 event；part 3 未來加 `sighting`/`contact` 時再與本項對齊命名（part 2 不碰 kind 新值）。→ **兩條可完全並行、merge 不撞**。<br>**[2026-06-09 後端地基 slice — [PR #183](https://github.com/winson3QQ/ICS_COMMAND/pull/183)]**：⏳ 部分落地（不勾 ✅）。已交付（**純後端 additive**）：`event_markers` junction 表（m021；N:1、N:M-ready、雙向 ON DELETE CASCADE）取代 `attributes.kind='event'` JSON glue 當**權威關聯** + backfill（孤兒守門）；導航鏈 `GET /api/events/{id}/chain`（事→標記→決策，sub-goal 2）；`cop.py` event 圖釘 server 端建關聯（best-effort）；**位置 SoT 清理半** = 砍 `events.lat/lon` 死欄（m022）。`pytest 883 passed`；`/code-review` 2 修（marker 回傳改用 `_row_to_entity_dict` canonical decoder 對齊 shape；best-effort 攔 `sqlite3.Error` 含 DB-locked）；`/security-review` 0；**`/chain` 補 P1-14 `resolve_scope` 守門** —— reality-check 推翻「演習每次重頭來」前提（exercises **archived 保留**、多場同庫並存、AAR 回放需要、`resolve_scope` docstring 明文「防 operator/observer 越權窺看其他場次」），`/chain` 原為唯一漏走該閘的 event PII 讀取路徑 → 已補：event 不在 caller scope（含跨場）→ 404，不洩 `related_person_name` 等跨場 PII。**仍 pending（拆出獨立 PR）**：(a) **decisions FK** —— 子表 `ai_recommendations.related_decision_id` 為 INTEGER 指向 TEXT(uuid) PK 的型別不符 FK 且活用，rebuild 須連帶修；(b) **location_zone_id 砍** —— 前端 `map.js` 在寫，與前端 cutover 一起；(c) **前端 cutover** —— 消費 junction/`/chain`、拔 `attributes.event_id` glue（junction 與 `/chain` 目前**無前端消費者**，故本 slice 無使用者可見變化）。**位置 SoT 校正**：原文「三份」實測 `location_zone_id` 是 client uuid 垃圾欄（無 location_zone 表）、**非位置來源**；真相 = `cop_entity.lat/lon` 唯一活 SoT，`events.lat/lon`（已砍）+ `location_zone_id`（待砍）為死/垃圾。<br>**[2026-06-10 ownership 收斂 + 模型補]**：本 parent 三 sub-goal 拆出歸屬——(1) event↔marker 規範化＝**P2-33b「降級」**（`kind='event'` cop_entity 退回純標記、停 glue、junction 為唯一關聯）+ **P2-33c N:1 即時聚合**；event↔decision FK＝**P2-31 ✅**。(2) 導航鏈＝**P2-33a**。(3) 位置 SoT＝**P2-32 ✅**（location_zone_id）+ `events.lat/lon`（#183）。**新增模型決定**：marker=原子（**觀察類型**）/ event=工作流外殼（**事件分類**）/ junction=唯一關聯（可 re-parent）；**兩 type 軸分離**（N:1 時 event 自成一格）。完整模型 + founding-why SoT → [memory `cop-marker-event-decoupling`] + [`cop-event-layering.md`](design/cop-event-layering.md)。本 row 收斂為 **parent/設計錨**，實作經 P2-31/32/33 落地。 |
| P2-28 | **入向升級：現場回報自動成 tracked event**：實證 MEDEVAC(P2-09)/GeoChat(P2-07) 進來只落 `cop_entities`/`chats`，**不進 events** → 重大現場回報只在地圖閃 critical icon、**無流程追蹤**（無決策/派任/結案）。工作：現場回報（MEDEVAC 9-line / 指定 GeoChat / 高 severity marker）→ 依規則**升級建立 tracked event**（連結來源感知標記，呼應 P2-27 的 N:1）。**設計**：升級規則白名單（避免每筆 PLI 都成 event）；server-authoritative（不信 client 宣告，對齊 [strategy §1.1](roadmap/tak-integration-strategy.md)）。**📐** [`cop-event-layering.md`](design/cop-event-layering.md) #3/(iv)。依賴 P2-07/P2-09 + P2-27。<br>**[2026-06-10 reality-check 對齊 founding-why（[memory `cop-marker-event-decoupling`]）— 標題「自動成 tracked event」須改]**：**自動生孤立事件正是本系統要打的迷霧**（指揮部窮於應付 N 個孤立事件、只有 AAR 才見全貌）。對齊模型：現場回報 → **先落 marker（感知層）** → **triage**，且 triage **必含「關聯進既有大事件」、非只 spawn 新事件**；自動程度只到「marker + 升級提示 / 輔助判級」，「成 event」為受控動作（白名單 + 可關聯既有）。承 P2-33b 降級後的 marker-first 世界；與 P2-33c 的輔助聚合同一條脊椎（入向自動化餵料、聚合呈現全貌）。 |
| P2-29 | **角色 / 分層感知地圖視圖**：現 map filter 僅 3 維（來源-kind / TAK affiliation / stale），**無角色感知、無分層**；`cop_entities.visible_to` 欄位**預留未用** → 指揮官與幕僚看同一張圖。工作：(1) **分層視圖**（原始感知標記 / 事件 / 計畫指令 分層 toggle，疊在既有 `_layerVis`）；(2) **角色視圖設定檔**（指揮官＝事件+決策+計畫指令；幕僚＝原始感知標記；觀察員＝唯讀）；(3) 激活 `visible_to`（per-entity 可見性，RBAC 守門）。**純前端 view + 後端 visible_to 過濾**（延伸 P2-25 機制）。**📐** [`cop-event-layering.md`](design/cop-event-layering.md) G1。依賴 P2-25。<br>**[2026-06-10 對齊；2026-06-11 校正]**：①「事件層」要**無自己的圖釘**（變成 marker 之上的聚合層）分層 toggle 才分得乾淨——這是 **B2 概念降級**（kind→sighting/contact），**已延後到 P2-33c/本項**；**P2-33b 只做了 B1（連結來源遷移、kind 留 `event`，事件仍有圖釘）→ 故本項依賴 P2-33c（B2），非 P2-33b**；② founding-why 的「**即時事件全貌視圖**」（P2-33c③）與本項分層/角色視圖同源，宜協同（角色過濾 × 全貌聚合）→ [memory `cop-marker-event-decoupling`]。<br>**[2026-06-11 前置 issue]**：地圖 UX 重整拆 **CREATE × VIEW 二分**——**前置 = [#219](https://github.com/winson3QQ/ICS_COMMAND/issues/219)（VIEW 側：☰ filter 面板分群可收合 + 角色預設骨架，零依賴先做，doctrine 分層語意仍待本項/P2-33c）**；姊妹 = [#220](https://github.com/winson3QQ/ICS_COMMAND/issues/220)（CREATE 側：建立動作整合進長按對話框，doctrine 掛 P2-28/P2-30/P2-33c）。本項做完整分層語意 + `visible_to` + 角色強制。 |
| ✅ P2-30 | **感知標記共享閘 + 手動感知標記（無線電上車口）+ 線/區出向幾何序列化**：完成「感知層雙向性」出向半（#2）。工作：(1) **per-entity 共享閘**（哪些感知標記推 TAK；指揮層 validate 後外推，非自動轟全網）；(2) **手動感知標記一等公民**——「無線電回報 → 可共享 COP 標記（敵情/接觸點，affiliation）」命名流程（非 node/facility 放置）；(3) **線/區出向幾何序列化**——補 `geometry_service.geometry_to_cot()`（vertices → `<link>`序列 / `<shape><polyline>` / `<ellipse>`，對稱 P2-08 入向 `extract_geometry`）+ `tak_downlink.build_geometry_cot()`，解鎖線/區下行（P2-13 A 只做點）。**📐** [`cop-event-layering.md`](design/cop-event-layering.md) #2/G5、[memory `tak-marti-authz-model`]。依賴 P2-08/P2-13。<br>**[2026-06-09 邊界]** (3) 幾何序列化器 + (1) 共享閘 = **P2-13 與本項共用下行基建**（非各做一套）；**本項先行**（路線 2），P2-13 線/區指令依賴之。<br>**[2026-06-09 part 1 落地 — [#181](https://github.com/winson3QQ/ICS_COMMAND/pull/181)（merge `a803af7`）]**：✅ (3) **線/區出向幾何序列化** 完成——`geometry_service.vertices_to_cot_shape`（+ `clean_vertices` 單一驗證點）+ `tak_downlink.build_geometry_cot`，對稱 P2-08 入向 round-trippable；unit 7 + 端到端對活 iTAK 推線+區實收。`/security-review` 0、`/code-review`（形心/shape 點集分歧）修。**仍 pending（不勾 ✅）**：(1) per-entity 共享閘 + (2) 手動感知標記一等公民 = 後續 slice。<br>**[2026-06-09 part 2 落地 — [#182](https://github.com/winson3QQ/ICS_COMMAND/pull/182)（merge `b801eaf`）]**：✅ (1) **共享閘** 完成（後端）——`tak_downlink.entity_to_cot`（cop_entity→CoT 點/幾何分流）+ `POST /api/tak/share/{uid}`（查 entity → adapter → **audit-first `COP_SHARE_TAK`** → send_cot；COMMAND_ROLES）。「閘」= 指揮層顯式受 audit 的分享動作（非自動轟全網）。**零碰 P2-27 領地**（只動 tak_downlink/tak.py、唯讀 cop_entities）。unit 3 + api 5；端到端對活 TAK 分享敵情點+區實收。`/security-review` 0、`/code-review`（畸形幾何→422 非未審計 500）修。**仍 pending**：(2) 手動感知標記一等公民（前端敵情 picker）+ visible_to 細緻過濾 = 後續 slice。<br>**[2026-06-10 part 3 完成 — [#192](https://github.com/winson3QQ/ICS_COMMAND/pull/192)（merge `50c67f7`，Closes #180）]**：✅ (2) **手動感知標記一等公民** + 互動/雙向同步。渲染：2525 milsymbol 框（取代 circle+abbr，milsymbol 未就緒退 fallback）+ callsign 下方標籤。互動：右鍵單鍵「廣播」menu（`EventDragManager` 加 `onContextMenu`）+ 左鍵拖曳移動（`_contactDragMgr` handleSize 44）+ detail modal。**廣播權限放寬 operator+**（role_enum 窄洞 `/api/tak/share/` → WRITE_ROLES，其餘 `/api/tak/*` POST 維持 COMMAND_ROLES、#146 防注入不破 + 回歸守門測試）。**廣播後即時同步**：share 標 `attributes.shared_tak`（`json_set` 原子單語句、不 bump version）→ move/note 編輯即時重推 CoT；CoT remarks 標 `source: ICS`。後端 906 / JS 253 / eslint 0 / doc_sync pass；`/code-review` 2 修、`/security-review` 0。**item 4 可靠刪除 → deferred P2-14**（streaming 刪不掉 archived；Marti REST 無單顆 CoT DELETE → 只在 Mission/DataSync）。**衍生另開**：ICS↔iTAK 詳情對齊、Marti 權威 resync = P2-12/P2-14。<br>**[2026-06-10 對齊]**：(2) 手動感知標記（part-3 sighting/contact）＝**第一批純標記**，與 **P2-33b 降級**退回的 marker kind **同一套詞彙**（收斂、非兩套）；且這些標記應**可被 N:1 聚合進事件**（P2-33c，與降級標記同源）→ [memory `cop-marker-event-decoupling`]。<br>**[2026-07-03 節點手動廣播到 TAK — [PR #469](https://github.com/winson3QQ/ICS_COMMAND/pull/469)（merge `d921fd4`，Closes #467）]**：把 share 閘從感知標記擴到**指揮官節點**——節點 modal 加「📡 廣播」鈕（重用 `POST /api/tak/share`，gate=指揮層 + TAK 啟用）。現場端依 node_type 顯示**不同** 2525 符號（`tak_downlink._NODE_COT_TYPE`：指揮部 `a-f-G-U-H` / 醫療 `a-f-G-U-U-S-M` / 安全 `a-f-G-U-U-M` / 前進 `a-f-G-U-C-I` / 收容 `a-f-G-U-U-S`）——實作 `mil_symbol.js` 預留的 cot_type 字典擴充；存儲 `a-f-G-I` 不動、**出向才換**（server-authoritative）。`/code-review`（無 bug）+ `/security-review`（0：XSS/CoT 注入/授權/資料外洩皆清）。**真機 ATAK dogfood PASS**（五類符號可辨）。**backend-v2.29.0 / frontend-v1.22.0**。**⏸ 設施（infra）推 TAK = 延後 backlog（短期不做）**：收容所/警局/消防等民事概念無淨 2525 對應，要走 EMS/HADR 符號或 degrade 到通用框，須先符號研究 + 真機 dogfood 定案；機制與節點同（`_NODE_COT_TYPE` 擴 `_INFRA_COT_TYPE` + infra marker 加廣播鈕），要做時照節點模式即可。**未開 issue，此為唯一注記點。** |
| **——— 【P2-27 後端地基 slice（[#183](https://github.com/winson3QQ/ICS_COMMAND/pull/183)）拆出的後續】———** | — |
| ✅ P2-31 | **decisions 關聯 FK 規範化（P2-27 拆出，[issue #184](https://github.com/winson3QQ/ICS_COMMAND/issues/184)）**：`decisions.primary_event_id` / `parent_decision_id` / `superseded_by` 現全 TEXT **無 FK**；補 FK（primary_event_id → events(id)、自我參照 → decisions(id)；`ON DELETE` 取捨——問責鏈建議 **SET NULL** 保留決策紀錄不隨 event 消失）。**⚠️ Landmine（#183 reality-check 挖出）**：子表 `ai_recommendations.related_decision_id` 為 **INTEGER 指向 TEXT(uuid) `decisions.id`** 的型別不符 FK 且**活用**（`ai.py`/`ai_repo.py`）→ decisions table rebuild（CHECK/FK 不可 ALTER，走 `_rebuild_*` 的 `foreign_keys=OFF` 12-step）須**連帶修 ai 型別不一致**。**📐** [`cop-event-layering.md`](design/cop-event-layering.md)。<br>**[2026-06-10 完成 — [#185](https://github.com/winson3QQ/ICS_COMMAND/pull/185) `1ec52dd`]**：m023（decisions 三欄補 FK，全 `ON DELETE SET NULL`）+ m024（`ai_recommendations.related_decision_id` INTEGER→TEXT + FK）；泛化 `_rebuild_with_fk`（自 `_rebuild_cop_entities` 抽出：12-step `foreign_keys=OFF` + 動態欄位複製 + 動態 index 重建 + dangling scrub）。**reality-check 補正落地**：SET NULL 為 `delete_exercise` 刪除順序（events→decisions→ai）下的**硬需求**非偏好；ai FK 的 ON DELETE 補指定 SET NULL；rebuild 後 `foreign_key_check` 驗乾淨；`related_decision_id` 前端無 UI、現存全 NULL → 無資料遷移；`schemas/ai.py`＋`ai_repo.py` 型別 `int→str` 同步。測試 `tests/integration/test_decision_fk.py`（9：schema FK／強制性／SET NULL ×2／delete_exercise 級聯不炸／scrub）。 |
| ✅ P2-32 | **砍 `events.location_zone_id` 垃圾欄（P2-27 拆出，位置 SoT 清理收尾，[issue #186](https://github.com/winson3QQ/ICS_COMMAND/issues/186)）**：#183 reality-check 實測 `location_zone_id` 存的是 client uuid（`'evt_'+Date.now()`）、**無 location_zone 表**、**非位置來源** —— 與「`cop_entity.lat/lon` = 事件位置唯一 SoT」doctrine 牴觸的誤命名死欄。砍除須動前端 `map.js`（停送）+ `event_repo` create/patch（停存欄）+ `ALTER TABLE events DROP COLUMN`。（`events.lat/lon` 已於 #183 砍，此為位置清理另一半。）<br>**[2026-06-10 後端半完成 — [#187](https://github.com/winson3QQ/ICS_COMMAND/pull/187) `a721bae`]**：m025 `DROP COLUMN`（比照 m022）+ base CREATE 移除；後端停存/停讀全清（event_repo create/patch、schemas EventIn/EventPatch、events PATCH、ttx inject、`dashboard_service.open_events_on_map` 死 field）。前端優雅退化（`EventIn` extra=ignore 不 422、events.js `|| ev.id` 兜底）。**reality-check 補正**：原 row 漏列的 4 觸點（dashboard 死 field + 兩 schema + ttx + 前端反查）已盤清。無版號 bump。<br>**[2026-06-10 前端尾完成 → 結案 — [#190](https://github.com/winson3QQ/ICS_COMMAND/pull/190) `7fb4193`（squash）]**：停送（`map.js _evPopupSubmit` + `events.js submitEvent` body/樂觀物件）+ 表單 plumbing 拔除（hidden input `#ev-zone-id`、`openEventForm` zoneId 參數、`_zoneEventsTab` 鈕 `data-zone`、`main.js` 取值）+ 停讀（`showEventDetail` fakeZone.id→`ev.id`、`showEventProcessModal` 移除恆 false 的反查 fallback）。**reality-check 校正前述「前端半 = P2-33」的 drift**：P2-33 是 event↔marker junction cutover（`attributes.event_id` glue），與 location_zone_id 正交、只是共用前端檔；本前端尾自成一刀，#186 於本 PR 完整收尾。dogfood 衍生並順修一個 **pre-existing 事件處置 modal 無限遞迴**（同 squash `7fb4193`；feature commit `13dd5a7`：cop.js 把 wrapper 誤注入 initEvents 致 listener 自我遞迴；與 location_zone_id 無因果）。`/verify` PASS（POST payload 無 location_zone_id + 圖釘點擊正常）、`/code-review` 0、`/security-review` 0。 |
| P2-33 | **event↔marker 前端 cutover（P2-27 拆出，JSON glue 退役）**：#183 已建 `event_markers` junction 為**權威關聯** + 導航端點 `GET /api/events/{id}/chain`，但**前端仍走 `attributes.event_id` JSON glue**、且 junction / `/chain` **無前端消費者**（故 #183 無使用者可見變化）。依賴 P2-27（#183）。**📐 必讀** [`cop-marker-event-decoupling.md`](design/cop-marker-event-decoupling.md)（操作 / 呈現 / 緣起 SoT）+ [`cop-event-layering.md`](design/cop-event-layering.md)（分層）。<br>**[2026-06-10 reality check — 拆 a/b + 順序硬約束]** 原文「拔掉 `_evPopupSubmit` glue → junction 成唯一來源」**低估 scope 且 remove-first 會反殺**，三個實證漏列真相：① **junction 目前被那條 glue 餵養**（`routers/cop.py` event 圖釘建立時讀 `created.attributes.event_id` 才 `link_marker`）→ 前端停寫 = `link_marker` 永不觸發 = **junction 反而變空**；② 前端 `zone.event_id`（10+ 處點擊/拖移/渲染/孤兒判定消費）**唯一來源 = `attributes.event_id`**（`entity_layer.js` `copEntityToEventZone` + `map.js`）→ cutover 後須改由 cop entity payload 帶 junction 解出的 event_id（後端 `_row_to_entity_dict` **現不帶**）；③ 反向查詢 `get_events_for_marker`（uid→event）repo 有但**無 HTTP route、零 caller**（latent）。**目標模型（2026-06-10 與使用者釐清，SoT→ [memory `cop-marker-event-decoupling`]）**：marker=原子（感知層，載**觀察類型**）/ event=可選工作流外殼（事故層，載**事件分類**，reference ≥1 標記）/ `event_markers` junction=唯一權威關聯（role、**可 re-parent**）。**兩 type 軸分離**（觀察類型 on marker、事件分類 on event）。**triage 三態**：升級為獨立事件 / 關聯進既有大事件 / 留裸標記。現前端硬 1:1。<br>**🎯 founding-why（為何是脊椎非 polish）**：演習第一波協同攻擊被當成 N 個孤立事件、指揮部窮於應付、**只有 AAR 才見全貌** → 催生本系統。**marker-first 解耦 + N:1 即時聚合 = 脊椎**：把「只有 AAR 才看得到的協同網」前移到交戰當下。<br>**故拆三刀**：<br>**P2-33a（低風險·先出價值）** = 導航鏈唯讀 UI：事件處置 modal 消費 `GET /{id}/chain`，渲染「關聯標記 / 決策」區塊（**標記純資訊清單**；逐標記導航留 c）。不碰 glue、主戰場 `events.js`。**與 live `feat/issue-180-part3-contact`（part-3 動 `map.js`/`main.js`/`mil_symbol.js`，零碰 events.js）無重疊**（reality check 實證）。**[2026-06-10 ✅ 完成 — [#195](https://github.com/winson3QQ/ICS_COMMAND/pull/195) squash `13ab7a3`，Closes #191]**：events.js 事件處置 modal 加導航鏈唯讀清單（消費 `/chain` = #183 junction/端點**前端首個消費者**）；標記**純資訊·不可點**（1:1 下定位多餘且卡既有 `_highlightEvent` 聚光燈，逐標記導航留 c）、決策重用 `showDecisionModal`；404 靜默、scope 由後端守。`/verify` PASS、eslint 0、vitest 249。**b/c 待做。**<br>**P2-33b（降級＝結構地基·後做）** = glue 退役 + 解耦 1:1：今天兼任事件圖釘的 `cop_entity(kind='event')` **退回純標記**（kind 改 sighting/contact、**對齊 part-3 詞彙**、停寫 `attributes.event_id`）；`events` row 改 junction reference primary 標記；前端 `copEntityToEventZone` 改吃 junction-fed event_id（後端序列化要帶、`_row_to_entity_dict` 現不帶；視需要接 reverse `get_events_for_marker`）。**硬約束：先加新讀/寫路徑 → 遷前端 → 才刪 glue（不可 remove-first，否則 junction 餵食斷 + 前端 event_id 全失效）**。遷移安全：m021 junction 已 backfill，降級後現有事件**預設各自獨立 1:1**。⚠️ 動 `map.js`/`entity_layer.js`/`cop.py`、**與 part-3 `map.js` 有實重疊** → 排 part-3 併入後做。**[2026-06-10 ✅ 完成（B1）— [#208](https://github.com/winson3QQ/ICS_COMMAND/pull/208) squash `21b6040`，Closes #196]**：① 後端序列化 get/list 帶 junction primary event_id（批次避 N+1）；② 前端 `copEntityToEventZone`/`findZoneByEventId`/drag 改吃**頂層** event_id；③ `map.js` 寫頂層 first-class、`cop.py` **link-before-broadcast** + pop first-class event_id（back-compat fallback attributes）。**實作只做 B1（連結來源遷移），kind 留 `event`**——B2 概念降級（kind→sighting/contact、事件無自身圖釘）併入 **P2-33c/P2-29**。review 硬化：create link 對 DB-lock(`OperationalError`) 重試一次保「event 圖釘必有 event_id」不變式（`IntegrityError`→orphan 不反射 forged）。`/code-review` 1 finding→修、`/security-review` 0、full pytest（1 skip）+ vitest 254。<br>**P2-33c（N:1 聚合＝脊椎）** = 在解耦地基上長即時聚合：① 多選聚合 / 標記「升級為事件」/「關聯進大事件」/ re-parent（寫 junction）；② **長按事件 → 查 junction → 亮「事件＋n 標記」、其餘灰、畫細線、放開復原**（擴 `_highlightEvent` press/release，**瞬時不全域**避免目不暇給）；③ **即時長出來的事件全貌視圖 + 輔助聚合**（系統提示「時空相近 N 顆可能同一波」，人確認）＝ founding-why 真正交付物。事件分類自成一格（≠ 單一 marker 觀察類型）。<br>**[2026-06-12 reality check + 開工]**：實查 **a/b1 已落地**（非舊 intro「前端仍走 glue」——`copEntityToEventZone` 已吃頂層 junction event_id、glue 寫入已退役）→ 剩 **B2 降級 + c N:1** ＝ 開 [#240](https://github.com/winson3QQ/ICS_COMMAND/issues/240)（脊椎，刀0 降級→刀1 聚合操作→刀2 呈現三鐵則→刀3 輔助聚合）。**regime 地基先行**：[#241](https://github.com/winson3QQ/ICS_COMMAND/issues/241) 落地 taxonomy `regime` 欄（civil ◆ / alert ▲ / military 2525，與 cot_type 解耦；seed + 前端 fallback + validator + 回填通用化 source/regime）＝ **刀0 前置**（降級後 sighting marker 有 regime 可吃）。`/code-review`（修 2：stale doc ref + regime 回填 override 測試）+ `/security-review` 0；後端全綠 / vitest 311 / doc_sync 綠。<br>**[2026-06-12 render 收斂 乙-2a — [#243](https://github.com/winson3QQ/ICS_COMMAND/issues/243)]**：事件外框依 `regime` 分流（`_renderZones` 解 `_EVENT_TYPES[evType].regime` → `zoneToNodeFeature` 帶 property → `zones-event(-outline)` 層 `icon-image` 改 `['match', regime, 'alert', triangle, diamond]`）。新 `bakeTriangleSdf`（**等邊·形心置中**，壓在點上）。**evacuation → ▲**、其餘 civil → ◆、**military 暫 fallback ◆（乙-2b 才接 2525 milsymbol）**。altitude：branch 依 regime 非 `kind='event'` → survives #240 刀0。`/code-review` + `/security-review` **0**（純前端、regime enum-sourced、無 sink）。vitest 312 / eslint 0；**真機 Mac human verify PASS**（撤離出等邊 ▲ 壓點上、其他 ◆）。<br>**[2026-06-12 乙-2b 軍用 2525 + 甲-1 marker 自描述/降級 — Closes [#243](https://github.com/winson3QQ/ICS_COMMAND/issues/243) · [#240](https://github.com/winson3QQ/ICS_COMMAND/issues/240) 甲-1]**：**乙-2b**＝drone/不明/QRF（regime=military）→ milsymbol 2525 框，**獨立 `zones-military-icon` 層**（同 source、**不套 icon-color** 否則被 severity 染色；halo/pulse/click 自動保留）+ 拿掉 abbr → **敵我=框色、severity=光暈**兩軸分離。**甲-1（#240 刀0 走「甲：真大一統」）**＝marker **自描述感知原子**：自帶 `event_type`（render 依 marker 自身推 regime/abbr，不再查 linked event）+ **kind `event`→`sighting`**（m027 遷移既有；guard/搜尋/cop.py 認 event+sighting back-compat）＝解鎖 N:1。`/code-review` 0 + `/security-review` 0；後端全套（1 skip）/ vitest 315 / eslint 0 / doc_sync 綠。**真機 iPad human verify PASS**（藍友/黃不明/紅敵框、無小字、critical 紅暈、5 個既有事件 m027 降級後零回歸 + 點得開）。**刀0 剩甲-2（N:1 即時聚合＝P2-33c）。** |
| ✅ P2-34 | **地圖建立動作整合進長按 contextual 對話框（CREATE 側，[issue #220](https://github.com/winson3QQ/ICS_COMMAND/issues/220)）**：地圖 UX 重整的 **CREATE × VIEW 二分**之 CREATE 半——建立流程現散在兩入口（長按→事件 NAPSG popup；☰ 面板「地圖設定」→ 放置節點/設施/敵情標記 arm-then-click），且放置動作擠在 filter 面板（IA 錯）。長按是**位置錨定**的，本就是建立的正確 altitude。落地：把放置動作收進長按對話框 = 多類別 launcher（**點建立**：事件 / 設施·節點 / 感知·敵情標記），**角色條件顯示**（事件=`canCreateEvents` / 設施·節點=`canUseRealModeControls` / 標記=`canAccessMapObjects`），filter 面板回歸純 view（接 #219）。**📐 doctrine SoT** [`cop-marker-event-decoupling.md`](design/cop-marker-event-decoupling.md)（marker-first：選單以標記為首、事件為升級選項）。依賴/協同 P2-28（入向升級）、P2-30 part 3（手動感知標記，已 ✅）、P2-33c（marker-first 事件降級語意）。<br>**[2026-06-11 reality check（#220 留言）]**：純前端中等重構、**零後端**（全走既有 `_copStream.createEntity`+P1-14 scope）。① 資料層 `_placeAt(lat,lng)`（map.js:3047，kind∈zone/contact/infra）**可重用**，長按只需餵 kind/type+座標 → arm-then-click 機制（`_placeState` banner/crosshair/點擊抑制 map.js:427）對點放置可退場（簡化）。② ⚠️ 長按 gate（map.js:444）現為 `canCreateEvents` only → 須放寬到 `canAccessMapObjects`（最寬）再依角色濾類別（**operator 現無法長按 = 行為改變**）。③ ⚠️ poly/route 是**多頂點 draw 流程**（工具列 `startPolyDraw`/`startRouteDraw`），不收進長按；本項 scope = 點建立。④ `EventPopup` 一般化成 `CreatePopup`（類別→子型；模式已存在）。⑤ ⚠️ 注意選單深度（事件勿變 3-tap）。**Out of scope**：通用 2525 SIDC picker（淨新增）、marker-first 事件降級語意（P2-33c）、VIEW 側（#219/P2-29）。<br>**姊妹 = [#219](https://github.com/winson3QQ/ICS_COMMAND/issues/219)（VIEW 側）**。<br>**[2026-06-11 完成 — `feat/issue-220-create-longpress`（Closes #220）]**：① 新 `static/js/map/create_popup.js` `CreatePopup`——類別→子型 launcher（marker-first 排序、`getCategories` 於 open() 即時解析反映角色/taxonomy）；② `map.js` 長按 → `_openCreatePopup`（gate 放寬 `canAccessMapObjects‖canCreateEvents`）+ `_buildCreateCategories()`（角色濾：標記=operator+／事件=canCreateEvents／設施·節點=指揮層）+ `onCreate` 路由（事件→`_evPopupSubmit`、其餘→`_placeAt(lat,lng,{kind,type})`）；③ filter 面板放置入口移除 → 純 view（接 #219、無提示）；④ **arm-then-click 死碼全退場**（`_placeState`/`_startPlace`/`_cancelPlace`/`_openInfraForm`/`_openNodePlacePicker`/`_openContactPlacePicker`/`_startContactPlace` + 單擊分支 + main.js imports/dispatch + HTML `node-place-banner` + CSS；`event_popup.js`+test 刪除）。reality-check 三修正全落實（不叫「TAK 類」、通用 2525 picker out-of-scope、marker-first）。`/code-review`（affiliation 色票抽 `_AFFILIATION_COLOR` 共用；長按 gate 完整性經角色階層確認）＋`/security-review` **0 finding**（全 `textContent` 無 XSS、後端 RBAC 未動仍守 zone/infra=COMMAND、payload 全 hardcoded）。新 `create_popup.test.js` 10 例；vitest 279 綠、eslint 0。**前端純 UX 重整，無 API/版號變更**（CMD_VERSION 不 bump——無新 UI 功能組，係建立入口位置遷移；視 #219 完成後一併評估前端版）。Human verify by user（dev env 長按各角色建立）。 |
| **——— 【感測層大一統：TAK↔ICS 功能對等原則 ——「TAK 有的重要功能，ICS 也要能對應」（使用者 2026-06-14 拍板；ATAK e2e dogfood 衍生）】———** | — |
| P2-35 | **即時 ops 地圖移動尾跡（對應 ATAK breadcrumbs）—— 感測層大一統「TAK 功能對等」第一個 instance（[issue #256](https://github.com/winson3QQ/ICS_COMMAND/issues/256)）**：**原則**（使用者 2026-06-14 拍板）＝ ICS 作指揮端權威 COP，**現場 TAK client 有的重要態勢能力，ICS 要有對等呈現**；本則統御整個感測層大一統 backlog（出向 #216/#212、declutter #217/#168/#172/#219、dogfood #211、taxonomy #66、L4 脊椎 #240），未來 TAK→ICS 對等缺口逐一援引。**第一 instance**＝ATAK 有 breadcrumbs（移動單位即時路徑尾跡），ICS 即時 ops 地圖無對應。**reality check（ATAK e2e A2 衍生）**：`static/js/map.js` 無 trail 渲染；尾跡只在 **AAR 回放頁** `aar/aar_map.js`（`aar-trails`、歷史·讀 DB·exercise-scoped）；後端 `_record_track`（P2-06a/[#120](https://github.com/winson3QQ/ICS_COMMAND/issues/120)）於無 active 場（`exercise_id=NULL`）跳過（[#123](https://github.com/winson3QQ/ICS_COMMAND/issues/123)，`cop_service.py:318`）。**設計方向**：前端累積 WS 位置流（per-uid 近 N 分鐘 → MapLibre LineString，affiliation 色）—— **獨立於 exercise-scope·對齊 ATAK 本機 breadcrumb·零後端改動**；☰ toggle 接 [#219](https://github.com/winson3QQ/ICS_COMMAND/issues/219)。**與 AAR 尾跡（P2-20 B2）明確分離**（即時 SA·ephemeral·非 DB·reload 重置）。**降階純前端、無 TAK 硬依賴**。**📐** [`cop-event-layering.md`](design/cop-event-layering.md) L3 感知層。 |
| ✅ P2-36 | **出向幾何 ATAK 格式修正 + zone/route 廣播 UI（[#212](https://github.com/winson3QQ/ICS_COMMAND/issues/212) 後端+前端 / [#214](https://github.com/winson3QQ/ICS_COMMAND/issues/214) 幾何半；#211 ATAK dogfood 衍生）**：#211 真機驗出 **ICS 出向幾何 ATAK 不渲染**（點 marker 可、幾何不可）—— `build_geometry_cot` 舊用 `<shape><polyline>` **無樣式**（iTAK 寬鬆才吃，ATAK 嚴格不畫）。**根因鐵證**＝ICS 入向 parser 本就解 ATAK 原生 `<link>` 序列。**修**：`geometry_service` 加 `vertices_to_cot_links`（ATAK 原生 `<link point=...>` 序列、closed 補閉合）+ `hex_to_argb_int`（出向色，對稱入向 `cop_service._argb_int_to_hex`）；`build_geometry_cot` 帶 strokeColor/strokeWeight/**strokeStyle（dotted>dashed>solid 三態，對稱入向不漏 dotted）**+ closed 帶 fillColor，色/筆觸由 `attributes.color`/`dash`/`dotted` 衍生。**ATAK＋iTAK 雙吃**（真機端到端驗）。前端：`_featureInfo` 加「📡 廣播」鈕 + polygon/route 右鍵 `contextmenu` 廣播選單（鏡像 contact、重用 `_shareContactTak`/`_openContactBroadcastMenu`、唯讀 TAK 來源不開）。pytest 全綠 / vitest 321 / eslint 0 / pyflakes 0；`/code-review`（dotted 漏填 + fill alpha drift 2 修）＋`/security-review`（0）。**真機 ATAK 端到端 /verify PASS**（畫 zone/route → 廣播 → ATAK 渲染黃/紅框+填色）。**剩**：#214 marker 欄位（`<__group>`/`<color>`/`<usericon>`）＝後續刀。<br>**[2026-06-14 完成 — [#261](https://github.com/winson3QQ/ICS_COMMAND/pull/261) squash `506edec7`，Closes #212]**。route 出向另以 `b-m-p-c` 控制點壓「SP」航點（partial 止血）；route 富物件忠實度深挖 = [#260](https://github.com/winson3QQ/ICS_COMMAND/issues/260)。#214 marker 欄位（`<__group>`/`<color>`/`<usericon>`）續刀；雙向對稱 doctrine = [#257](https://github.com/winson3QQ/ICS_COMMAND/issues/257)/[#258](https://github.com/winson3QQ/ICS_COMMAND/issues/258)（memory `cop-collaborative-edit-shared-truth`）。<br>**[2026-06-14 #257 α 全數完成：COP 雙向協作編輯落地（[#262](https://github.com/winson3QQ/ICS_COMMAND/pull/262) / [#263](https://github.com/winson3QQ/ICS_COMMAND/pull/263)）]**：自建 polygon/route/marker 可像 TAK client 原地編輯、已分享者即時上 TAK；唯讀外部來源不可（=#258）。**α-1** 長按整體移動＋「抓取」游標/提示回饋（#262 squash `759e87c`）、**α-2** 逐頂點 reshape ＋ **α-2b** 拖中點插入/點頂點✕刪（#263 squash `83778f0`；頂點存開放陣列、renderer 自動收尾→增刪純 splice）、**α-3** marker 敵我態點即換（`swapCotAffiliation` 換 CoT affiliation 字元、折入未存注記、PUT 即時重推 TAK；vitest +5）。後端零改動（type 非 PUT-forbidden、`_resync_tak_if_shared` 自動重推）。後續：route 深挖 #260、marker 完整 2525 picker 留 β。<br>**[2026-06-15 #258 β-1 merged：TTX 模式可編輯外部 TAK 來源幾何]**：前端編輯閘 mode-aware（`_isReadonlySource` 外部來源僅 active exercise type='ttx' 可編，對齊後端 `_require_editable_source`；走 main.js `setExerciseMode` 橋接守 module boundary）。後端零改動（TTX 早放行、已測 `test_put_tak_allowed_in_ttx`）。編輯不自動回推 TAK、走明示「📡 廣播」鈕。範圍：只幾何（polygon/route）；marker=β-1b、實戰=β-2。eslint clean / vitest 329。**注意**：live 驗證受 [#265](https://github.com/winson3QQ/ICS_COMMAND/issues/265)（切換演習後 WS scope 不更新、新場 entity 需硬重整才 render）影響 —— β-1 閘邏輯正確，#265 為既有 P1-14 遺留 bug、另案修。<br>**[2026-07-03 #260 route 富物件忠實度 A–C2 落地]**：route 深挖分片推進——**A+B**（[PR #461](https://github.com/winson3QQ/ICS_COMMAND/pull/461)）route 主座標取首頂點 + 出向從 `attributes` 原始 link/link_attr 忠實 round-trip（保 waypoint 名字+導航屬性），🔑補 `<__routeinfo><__navcues/>` 使 ATAK 認定 route 渲染（逐元素真機比對定根因）；**C1**（[PR #462](https://github.com/winson3QQ/ICS_COMMAND/pull/462)）route 詳情 modal 顯示導航屬性（method/routetype/direction，值走 `_escapeHtml`）；**C2**（[#464](https://github.com/winson3QQ/ICS_COMMAND/issues/464)）命名 waypoint（SP/CP/TGT）顯示成 map marker+label——`extractRouteWaypoints`（`attributes.link` 的 `b-m-p-w`+callsign → `[lng,lat]` 經 `coord_tools.parseWgs84` 邊界驗證）+ `routeWaypointsToFeatures`（併入既有 'routes' source，`circle` 圓點 + native `text-field` label；不設 `properties.id` 避撞 route feature-state）；control point 不畫、純自建 route 無 link 不顯示（scope）；route 移動時 waypoint 隨 `attributes.link` 同步位移（`/code-review` F1 回彈 bug 修）。`/code-review`（F1 回彈 + F3 `parseWgs84` reuse+邊界 + F2 guard）＋`/security-review`（0，label 走 WebGL text-field 非 HTML sink）。vitest 387 / eslint 0 / doc_sync 綠。**真機 ATAK 4-route dogfood PASS**（含中文 waypoint + 純自建無 waypoint 的 scope 驗證）。`frontend-v1.21.0`。<br>**[2026-07-03 #260 D1：route 頂點編輯同步 attributes.link（`frontend-v1.21.2`）]**：**真機 dogfood 定位到根因**——出向 route CoT 從 `attributes.link` 重建，但頂點編輯（reshape，#257 α-2）過去只寫 `vertices`、不碰 link → 編輯後廣播送**舊形狀**、ATAK 不變、回灌更把 ICS 打回原狀（round-trip 斷裂，非 C2 造成、C2 讓 waypoint 可見才顯現）。**D1 修**：編輯期把「帶 point 的 link」（geoLinks，`splitRouteLinks` 拆出、`geoLinksAlignVertices` 逐點驗證對齊）隨頂點拖/插/刪 **lockstep splice**（插入補 `newControlPointLink` b-m-p-c），commit 依 vertices 重建 point（`reassembleRouteLink` 保 callsign/type/uid+hae、接回 otherLinks）回寫 `attributes.link` → 廣播送**新**形狀、ATAK 更新、回灌不再打回。純前端、`kind==='route'` 且對齊成立才動（保守，自建 route 無 link 不碰）。`/code-review`（F#2 對齊改逐點位置驗證、非僅數量）＋`/security-review`（0，半可信 link 欄位只回 backend PUT / 非 HTML sink）。vitest 393 / eslint 0 / doc_sync 綠。**真機 ATAK dogfood PASS**（reshape→廣播→ATAK 變形+不回彈）。<br>**[2026-07-03 #260 D2：route waypoint 命名 UI（`frontend-v1.23.0`）]**：頂點編輯加「🏷 命名」徽章（route，左上；✕ 刪除右上）→ modal 命名/改名/清空取消命名（`geoLinks[i]` b-m-p-w↔b-m-p-c）；命名頂點**白點 + 名字 label 即時回饋**（編輯中即見、不靠 C2）；**自建 route** 首次命名 `synthesizeGeoLinks` 現場合成對齊 control links → 也能有命名 waypoint；commit 走 D1 `reassembleRouteLink` 回寫 `attributes.link` → C2 顯示 + 出向 `route_links_to_cot` 送 callsign。`/code-review`（自建判定 `!geoLinks` 誤含「有 link 卻對齊失敗」的 route → 收斂為「真無 link 才合成、否則拒絕命名」防覆蓋原始 waypoint/producer link）＋`/security-review`（0，callsign 走 `_escapeHtml` attr + `textContent` label）。命名徽章首版藍色（`--blue` 未定義退 #58a6ff 與頂點藍點同色融掉，dogfood 抓出）→ 改綠 `--green`。vitest 394 / eslint 0 / doc_sync 綠。**真機 ATAK dogfood PASS**。**至此 #260 route 編輯（A/B/C1/C2/D1/D2）落地完成**；後續 marker 2525 picker、實戰模式編輯（β-2）為另線。 |
| ✅ P2-37 | **紅藍陣營隔離（faction isolation，演習先決）**：admin 把連線 TAK client 分類紅/藍/中立（server-authoritative），commander 及以下只見藍/中立、sysadmin（白隊）全見。**📐 設計 SoT** [`red-blue-faction-isolation.md`](design/red-blue-faction-isolation.md)。**核心 doctrine（真機抓包）**：faction ≠ affiliation（同 ATAK 裝置丟 4 種符號 creator 全同台 → 必須按 producer 指派，不可用 CoT type 判紅藍）；歸屬鏈僅 by-uid（`creator.uid`/`link[p-p].uid`/self-SA uid，iTAK 繪圖無 uid→fail-closed）；只 `source='tak'` 受過濾（manual/command 自建恆對藍方可見，#146 所有權）。<br>**[2026-06-23 全棧完成 — PR [#347](https://github.com/winson3QQ/ICS_COMMAND/pull/347)（Closes #343），9 commits `540a3cf`..`a0095d4`]**：① 資料層 v31（`client_faction` 表 + `cop_entities.faction/faction_source`）+ v32（`chats.faction`）；② ingest producer 歸屬鏈解析；③ 三層 server-side 強制點——REST `list_entities`/`get_entity`/`squads`/`/api/tak/share`、WS hub `_Conn` faction gate、GeoChat `/api/chat`+broadcast；開關 `ICS_FACTION_ISOLATION`（預設 off，演習前分類完 roster 再開）；④ AAR 互斥閘（有 active 演習時 aar/tracks/timeline/kpis/report/export 對所有角色 409，白隊一致）；⑤ admin API `/api/admin/factions/{clients,classify,entity-override}`（SYSADMIN_ONLY）+ 重解析 + resync；⑥ 前端 admin「紅藍」tab。**review**：`/code-review`+`/security-review` 跑兩輪——首輪抓「讀取面比初版廣」（補 squads/GeoChat/timeline）、merge 前次輪抓 `/api/tak/share` 漏 faction 過濾（`a0095d4` 修）。順帶清 pre-existing rbac golden 漂移（#317/#325/#327）。~40 新測試，全套綠。**dogfood `feat343-3c4153f` 上線現役 prod，使用者驗 admin 紅藍分類 PASS**（隔離效果待開關開）。**後續**：[#344](https://github.com/winson3QQ/ICS_COMMAND/issues/344) TAK 現場層 faction→group 綁定（第二支槓桿，演習先決）；[#346](https://github.com/winson3QQ/ICS_COMMAND/issues/346) admin 面板 IA 重整。<br>**[2026-06-23 follow-up 完成（dogfood 雙實機 ATAK+iTAK）]**：① **#344 TAK 現場層核心驗證通過**——唯一 CN 裝置證（`gen-device-pkg` 發 red-01/blue-01）+ `UserManager certmod -g` 指派不重疊 TAK group → 兩台 **SA/marker/GeoChat 互不可見**（現場層隔離成立）；ICS 自身（`ics-cot`）certmod 入 `red`+`blue` → **白隊全收**；接回 #343 角色濾合驗（commander 只回藍、admin 全見）。**機制成立、演習可手動 roster 達標**；**產品化（發證後自動 certmod / 接 #343 分類、`update-groups` REST 寫對 cert-flat-file 回 500 → 需 certmod 橋）續留 #344**。關鍵實測收 memory [[tak-faction-group-identifier]]：識別軸=cert CN（callsign/team 自報不可信）、managed user vs 匿名連線、REST 讀通寫 500。新增 ICS 管理級 cert（[#357](https://github.com/winson3QQ/ICS_COMMAND/issues/357)）。② **#346 admin/設定 IA 重整完成**（PR [#353](https://github.com/winson3QQ/ICS_COMMAND/pull/353)/[#355](https://github.com/winson3QQ/ICS_COMMAND/pull/355)：帳號群 segmented 子分頁 + 演習收一「演習中樞」面板 + 演習入口限指揮層）。③ **#358 faction UX 完成**（PR [#359](https://github.com/winson3QQ/ICS_COMMAND/pull/359)/[#360](https://github.com/winson3QQ/ICS_COMMAND/pull/360)/[#361](https://github.com/winson3QQ/ICS_COMMAND/pull/361)：分類面板顯裝置呼號【非 marker 名】+ 隊伍面板改 **faction 主軸**【藍軍/紅軍/中立，membership=有 team_color 或友軍，marker 不混入】+ self-SA 呼號測試補齊）。各 PR 皆過 `/code-review`+`/security-review`。**版號**：`backend-v2.15.0`/`frontend-v1.11.0`（#343 release）；#346/#358 frontend 續進。<br>**[2026-06-23 #344 enrollment 產品化完成 + live 重分類橋修正]**：補掉「#315 發證落匿名 → faction 改不動」系統缺口。① **發證即註冊 managed user**：`POST /tak/device-cert` 算 cert SHA-256 fingerprint → `services/tak_user_enroll` 寫共享卷**檔佇列** → **registrar sidecar**（`deploy/tak-server/registrar/registrar.sh`，reuse takserver image、`network_mode: service:takserver` 共享 netns、root 跑 `UserManager.jar usermod -f -g`）註冊進初始群 **neutral（fail-closed）**；best-effort 不拖垮發證。② **live 重分類橋修正**：`tak_group_sync._username_for_client_key` 由 `/Marti/api/clientEndPoints`（殭屍 row → 解析到錯 username，實測 itak→別台 blue-01）改 **`/Marti/api/subscriptions/all`**（比對 clientUid，可靠在線視圖）。③ **PoC + 雙實機 dogfood 全驗**：證實 usermod 須 takserver **本機 IPC**（裸寫 auth 檔被忽略+下次 re-marshal 清掉、獨立 netns sidecar timeout、唯 docker-exec 或共享 netns 行）；3QQ-atak/3QQ-itak 發證自動進 neutral、admin 紅藍分類**即時移群**、**gotcha 2 正解**（已連線 session live 重綁、免重連）；殘留舊 marker 為 TAK client stale 顯示限制（非 #344 gap，重連即清）。④ 過 `/code-review`（修 CJK callsign 500/幽靈列【#324 回歸】+ registrar 防線：leading-dash、crash-safe rm-after-result）+ `/security-review`（無 HIGH/MED）。**已知限制**：TAK 群為全域、非 exercise-scoped；非 ASCII callsign 不自動 enroll；iTAK 無 uid 繪圖 fail-closed（走單一物件手動歸屬）。實測脈絡收 memory [[tak-faction-group-identifier]]。<br>**[2026-06-25 紅藍分類面板 UX 修正 #389（dogfood）]**：① 「實戰池」誤名→「待命池（未開場：非演習非實戰）」（NULL scope ≠ 實戰；實戰是 type=real 的 active 場、有 id）；② 文案「連上的」→「本場觀測到的（含已離線）」（list 本就 `include_stale=True`）；③ 每行 🟢/⚪ 在線指示。**follow-up（dogfood 抓出）**：online/last_seen 改用 `updated_at`（最後活動）非 `received_at`（首見、再廣播不更新→live 裝置誤判離線）。PR #390/#391，`frontend-v1.12.2` / `backend-v2.16.2`，公網雙實機（ATAK+iTAK）驗 PASS。**另議**：**per-scope 重複分類 doctrine**——同裝置每開一場要重分；待 owner 定操作流程後決定是否「全域預設 + 各場覆寫」。<br>**[2026-06-28 #344 紅藍分類改綁 cert CN（Slice 1，`backend-v2.26.0`/`frontend-v1.18.1`）]**：根因——分類綁**易變的 uid**（裝置 GUID），裝置重裝/重 enroll 換 uid 就丟分類、舊分類成殭屍堆積（dogfood 實證：重發裝置後 online 兩台顯「未分類」、舊 neutral 殭屍離線、`3QQ-itak`→`3QQ-iTAK` uid `AC4B…`→`05ED…`）。改綁**穩定的 cert CN（= TAK username）**。**架構**（CoT 流只有 uid、CN 只在 TAK 連線元資料）：① `client_identity`（m035，uid→CN 快取）——`list_clients`/分類時從 `subscriptions/all`（可靠在線視圖，非殭屍 clientEndPoints）批次寫入；② ingest faction 解析（`cop_service._resolve_faction`）經快取把 producer uid 翻 CN 再查（無對照→退 uid→fail-closed）；③ 面板 `list_clients` 改 async、來源 = `subscriptions/all`（online）∩ `tak_device_certs`（active 發證）→ 只列「**發證後且在線**」、per-CN 去重；④ classify 綁 CN、經 `client_identity` 解 CN→uids 重解析名下 entity、`sync_client_faction` 直接用 username。**核心測試**：裝置換 uid（同 CN）後分類續存（`test_classification_survives_uid_change`）+ online∩issued 過濾 + per-CN 去重 + fail-closed；~17 faction/repo 測試重寫綠、全套 exit 0。**殘留（Slice 2 選用）**：uid→CN 快取即時性靠面板載入/分類觸發 → 加背景 poller 可免「裝置重連後須開一次面板才著色」的窗口；離線預分類 UX；舊 uid-keyed `client_faction` 列成孤兒（不遷移、重分類即可）。<br>**[2026-06-28 #344 Slice 2 — uid→CN 快取背景 poller（`backend-v2.26.1`）]**：補掉「裝置換 uid 重連後須先開一次紅藍分類面板才著色」的窗口。main lifespan 加週期任務 `_periodic_client_identity_refresh`（45s，對齊 ~60s online 視窗）→ 從 `subscriptions/all` 刷新 `client_identity`（uid→CN）→ 裝置換 uid 重連後**最多一輪詢週期即自動解析 faction**，免人工開面板。單次主體抽 `_refresh_client_identity_once`（可測）：TAK 未配置跳過（不建 client/session）、拉取錯 best-effort 回 0 不中斷迴圈；shutdown 隨其他週期任務 cancel 回收。+4 測試。**剩**：離線預分類 UX（演習前 roster prep，現面板只列在線）。<br>**[2026-07-03 演習/實戰＝記錄窗口 + faction 可見性模型重設計（umbrella [#471](https://github.com/winson3QQ/ICS_COMMAND/issues/471)）]**：#260 route 編輯 dogfood 撞出「演習啟動後 operator 看不到常駐 TAK/route」——現行把「AAR 記哪場」與「誰看得到什麼」綁同一 `exercise_id` scope。決策：**演習＝記錄窗口、可見性改 faction 軸、TTX=實戰**。📐 設計 SoT [`exercise-recording-visibility-model.md`](design/exercise-recording-visibility-model.md)；分片 #472–#477。<br>**[2026-07-03 #472 可見性軸轉 faction 落地（`backend-v2.30.0`/`frontend-v1.23.1`）]**：**cop entity 讀取（list/get/squads + WS）拿掉 exercise scope → faction 狀態驅動**（紅永遠藏、未編隊平時可見/演習中 fail-closed 藏、自建恆藍可見）；`exercise_id` 降記錄歸屬鍵；WS `wants` 加 `scope_by_exercise` 旗標分流（cop 只過 faction、chat 保 exercise scope）。**reality check 關鍵修正**：`resolve_scope` 身兼「跨場資料隔離（events/decisions/chat/manual + #288 IDOR）」與「地圖可見性」——**只改後者、前者保留不碰**。**dogfood 抓漏 + 補**：前端 `showStanding` client filter 演習中會遮蔽後端修復 → 移除；**/api/dashboard 的 tak_squads 過去無 faction 過濾（紅隊 centroid/兵力洩漏給 READ_ROLES）→ 補 visible_factions**（既有 #343 破口）。`/security-review` 4 邊界守住 0 洩漏；pytest 全綠（#288 events/decisions 隔離回歸在內）/ vitest 393 / eslint 0。**dogfood 續發現（歸 #473）**：`cop_entities.faction` 沒跟「當前演習的 per-exercise 分類」同步（每場重來對面板生效、對可見性用舊分類）→ 開場精靈一併補。**剩 umbrella 分片**：#473 開場精靈 / #474 拆編輯閘 / #475 中途分類 / #476 stale / #477 #344 驗證。<br>**[2026-07-03 #473-A faction 解析改 key 在 active 演習（`backend-v2.30.1`）]**：#472 dogfood 抓出「每場重來對可見性沒生效」根因——`_resolve_faction` 綁 `entity.exercise_id`，但 #472 後 cop 為跨場共享池（entity 多 NULL scope）→ per-exercise 分類套不到。**修**：faction 解析 key 改 `current_exercise_id()`（平時=None=全域）；新 `restamp_all_factions()`（activate/archive 依新場分類重解析 live entity，只動 `faction_source='auto'`、CN 無對照→None fail-closed）；`_reresolve_producer` gate 在 active 場、撈全 live 池。`/security-review` 5 邊界守住 0 洩漏（紅永遠藏、fail-closed、#344 anti-spoof 鏈不變、manual override 保護）。pytest 全綠（+3 測試：解析 key 在 active/restamp 重解析）。從 [#473](https://github.com/winson3QQ/ICS_COMMAND/issues/473) 拆出＝[#480](https://github.com/winson3QQ/ICS_COMMAND/issues/480)。<br>**[2026-07-03 #474（β-2）拆 TTX-only 編輯閘（`backend-v2.31.0`/`frontend-v1.23.2`）]**：承 2026-06-14「COP＝共享真實、撤實戰鎖死」（memory `cop-collaborative-edit-shared-truth`）——**`_require_editable_source` 整個移除**，外部來源（tak/pi-node/waveink）**幾何 + marker** 在任何模式（TTX/實戰/平時）皆可編/刪（前端 `_isReadonlySource` 恆 false）。威脅改由 **audit 問責（PUT/DELETE 恆留痕）+ RBAC（WRITE_ROLES）+ `_PUT_FORBIDDEN_FIELDS` + P2-10 白名單 + 編輯不自動回推 TAK** 五道緩解，非模式鎖。**threat_model §8.2 TAK-B row 改寫**（「實戰鎖死」→「audit 問責」記殘餘風險）。`/security-review` **5 補償控制守住、0 未緩解漏洞**（#145 來源所有權未動、RBAC/forbidden-fields/zone-infra gate 全在）。pytest 全綠 / eslint 0 / vitest / doc_sync 綠。<br>**[2026-07-03 #473 開場精靈——快照片砍除 + #473-B1 開場權收 sysadmin（`backend-v2.31.1`/`frontend-v1.23.3`）]**：**reality check 砍掉「開場快照 baseline」**——AAR（`timeline_service`）是時間窗衍生（tracks+events，無 snapshot 輸入），裝置週期發 SA→窗內第一個 track 即初始位置，另抓快照與現有 track-derived AAR 重複 → 不做（試了再說）。**#473-B1**：`activate` 收 **SYSADMIN_ONLY**（開場含紅藍分隊＝白隊/導調之責、commander 恆藍不得經手偷紅隊名單）；golden RBAC 矩陣同步 + deny 測試（commander activate→403）+ 前端「啟動」鈕收 sysadmin；create/archive/status 維持 COMMAND。**剩 #473 精靈**：選擇性清圖（按 stale/殘留清、保永久，非按場）+ 開場精靈 UI（分隊→清圖→確認開始引導流程）。<br>**[2026-07-03 #473-B2 開場選擇性清圖（`backend-v2.32.0`）]**：新端點 `POST /api/admin/clear-residual`（**SYSADMIN_ONLY + confirm**，路徑落 `/api/admin/` 自動歸類）——批量 **soft-delete 外部週期性殘留**（`source NOT IN (manual,command)` 且 `archived=0`），**保永久物件**（指揮部自建 route/polygon/事件標記 ＋ `archived=1` CoT `<archive/>` 釘住標記）。**方案 A（使用者拍板）**：外部鏡像全清不論新舊，live 裝置週期 SA 幾秒內重報自然重建＝真正「每場乾淨重來」。**faction 中立**（清「來源類別 × archived」不碰紅藍，非偷清漏洞）、**可逆**（tracks/歷史 row 不 hard delete）、回傳 `{cleared, kept}` 供精靈預覽、廣播 `resync`。開場精靈第二步的後端（B3 接 UI）。golden 矩陣 + deny 測試（commander→403）+ 4 整合測試。**剩**：B3 開場精靈 UI。<br>**[2026-07-03 #473-B3 開場精靈 UI（`frontend-v1.24.0`/`backend-v2.32.1`）]**：把三段既有能力縫成開場引導流程——「啟動」鈕改開**單一 modal 三段清單**（非分頁 stepper）：**① 紅藍分隊**（導流到既有分類面板 `admExerciseSub('faction')`，不重造）→ **② 選擇性清圖**（可跳過，按鈕呼叫 B2 的 `clear-residual`、inline 回饋「清 N／留 M」）→ **③ 確認開始記錄**（`activate`，只在成功時關精靈+刷新，失敗留 modal 顯訊息）。sysadmin-only（gate 在 `openExOpenWizard`，後端 activate/clear-residual 皆 SYSADMIN_ONLY 為真實邊界）；全走 CSP-safe `data-action` 委派（exWizard/exWizGoFaction/exWizClear/exWizStart）。**順帶修 `kept` 高估**（backend PATCH：`_PERMANENT_WHERE` 補 `stale>now`，對齊 list 顯示，預覽「保留數」不再比實見多）。vitest +4（精靈三段/gate/清圖 POST/取消）+ 整合測試 +1（kept 過濾過期永久）；eslint 0 / doc_sync 綠。**#473 精靈完成**（B1 開場權 + B2 清圖後端 + B3 UI 三片到齊）。<br>**[2026-07-03 #477a 開場對齊分類到現場（`backend-v2.33.0`）]**：dogfood 連鎖抓出並修 —— **① 修根因 bug**：精靈在待命池（平時 NULL scope）分隊，按開始記錄後進紅藍分類「全變未選」，因 activate 把 scope 切到該場、該場籃子是空的。修法 `seed_exercise_from_baseline`：activate 時把待命池分類**繼承**進這場（各場覆寫語意保留），須在 `restamp_all_factions` 前（restamp 才解析得到）。**② 補 #344 現場層對齊**：`restamp_all_factions` 只改 ICS 儀表板 faction、**不推 TAK 群** → 「每場重分隊」現場層沒生效。新增 `sync_exercise_tak_groups`：activate 把該場分類推到 TAK 現場群（server 端隔離對齊）、archive `to_neutral` 重置（現場回統一）。best-effort 不擋開場。**dogfood 實證**：`/Marti/api/subscriptions/all` 查得 iTAK=blue、atak=red 兩不重疊群 → 現場層隔離**本就有效**（先前「仍互看」為 TAK client 舊標記殘影，重開 app 即消 —— server 端刪除信號對 iTAK 硬限制，見 [[tak-streaming-archive-stale-vs-mission]]）；故「開場前分隊」為正解（無殘影），中途改隊需裝置本機重開。+7 測試（seed 繼承/覆寫、推群/重置/best-effort、activate 繼承回歸）。**umbrella #471 剩**：#477b（失敗 UI 警示 + 離線 reconciler）、#475、#476。<br>**[2026-07-04 #477b 現場隔離可靠性（`backend-v2.34.0`/`frontend-v1.25.0`）]**：補「隔離有沒有真的生效看不見 + 離線分類不會自動補」兩痛點。**① 面板顯 ⚠ 未隔離**：`list_clients` 改用 `list_online_subscriptions`（含每台實際 TAK 群）加 `actual_groups`/`isolated`（分類且實際群=[faction]→🛡；分類但群不符/`__ANON__`→⚠；未分類→None），前端逐台渲染。**② 分類即時回饋**：`admClassifyFaction` 讀 `tak_group.synced`，沒推成就大聲提示（離線＝預期緩語氣、其餘＝真問題），不再假裝成功。**③ 背景 reconciler**：`reconcile_online_groups`（在線+已分類+實際群≠分類→補推 `sync_client_faction`，離線→上線約 30s 自動歸位）；**合併 #344 Slice 2 的 uid→CN 刷新與群 reconcile 成一次 `subscriptions/all` poll**（原 45s 調快至 **30s**，一個 poll 服務兩事、負擔不加倍）。best-effort 不中斷。+9 測試（reconcile 只補不符/離線跳過/未配置、list_clients isolated 三態、combined poll identity 半保留）。剩 #471：#475、#476。<br>**[2026-07-04 #475 中途新連線提示 + 即時重分隊三軸（`backend-v2.35.0`/`frontend-v1.26.0`）]**：補「演習進行中」動態半。**① 三軸傳播補齊**：classify 原只傳地圖 entity + resync，**漏 GeoChat 軸** → 新增 `_reresolve_chats`/`restamp_chats_for_client`/`chat_repo.set_faction_for_ids`：改分類即重蓋該 CN 名下**既有通聯** faction（顯/藏即時；sender_uid 經 `_geochat_client_key` 取裝置段比對）；前端 `resync`→派 `chat:resync`→`refreshChatNow`。三軸一致。**② 重分隊入 AAR timeline**：`upsert_faction` audit 補傳 `exercise_id`（原=None、timeline 撈不到）+ 白名單加 `client_faction_classify`→`classification`（AAR「分類變更」）。**③ 中途新連線提示（方案 A）**：背景 poller 同一 poll 算「未分隊在線」快取 + `GET /factions/unclassified-count`（sysadmin）→ 演習 chip `⚠N` badge + 分類面板 banner（未分隊 fail-closed 藏不變）。+6 測試。<br>**[2026-07-04 #471 收尾——隊伍名冊退役 exercise_id「在場/納編」（`frontend-v1.26.1`）]**：dogfood 抓出 —— roster 面板頂部對 OP 顯「⚠ 無單位在場／加入全部連線」，卻同時列出已分類、地圖可見的藍軍單位（矛盾）。根因＝roster 的「在場/納編/加入全部連線」是 #267 舊 **exercise_id-scoping** 模型，#472 後可見性改 **faction 軸**、entity 多為跨場共享 NULL scope、AAR 記錄靠 track 自動蓋 `current_exercise_id`，故 exercise_id「編入」已無實質作用。**修法（方案 A）**：退役 `scopeStatus`/`_renderScopeTop`/`_addAllBtn`/納編-退編鈕/`onEnroll`/`onAddConnected`，名冊改純以 faction 分組呈現「連線＋分類＝參與」，頂部換誠實在線摘要（`N 在線 · M 編成單位`）。後端 enroll 端點暫留（UI 已不用，屬 API，後續可清）。vitest 392（移除 8 舊測試 + 加 2）/ eslint 0。**另記**：dogfood 第二問（ICS 自畫 zone `source=manual` 恆對藍可見、不隨裝置改陣營）＝#146 doctrine 邊界，待 owner 定調（甲：團隊 zone 該在裝置畫；乙：改 `_faction_clause` 讓 manual 也能分陣營藏）。**umbrella #471 剩最後 #476（stale 政策）**。<br>**[2026-07-04 修未分類 GeoChat 顯示閃現（`backend-v2.35.1`，dogfood）]**：OP 觀察「未分類裝置的通聯剛開始看得到、過一陣子消失」。根因＝chat 的兩條讀取路徑對 `faction=NULL` 過濾**不一致**——WS `_faction_ok` 平時放行 NULL（`current_exercise_id() is None`），但 GET `list_chats` **一律排除 NULL**（漏了 cop entity #472 早有的 `allow_null_faction`）→ WS 先推顯、30s 輪詢重取後 GET 濾掉 = 閃現。修：`list_chats`/`build_chat_feed` 補 `allow_null_faction`，router 傳 `current_exercise_id() is None` → **平時未分類 chat 對 OP 恆顯（對齊地圖 marker + WS）、演習中恆藏（fail-closed），只放 NULL 不影響紅**。+2 測試（state-aware 演習藏/平時放、allow_null 不漏紅）。**釐清**：現場層隔離（裝置↔裝置，未分類只看自己）≠ 儀表板層可見性（OP 看的，#472 平時放行未編隊）；兩者獨立。<br>**[2026-07-04 #476 stale 政策結案 → #471 umbrella 完成]**：reality check 發現 #476 三改動大半已由既有機制滿足 —— ① **平時 stale 灰化**：#160/#161 已落地（前端 `_isAging` 過 stale→dim、後端 list `stale>now` 過濾移除）；② **開場快照排除 stale**：**快照整個作廢**（#473-B1 已砍，AAR＝track 衍生無 snapshot）→ baseline 排除規則 moot；③ **不污染下場**：靠 #473-B2 `clear-residual`（明示清）+ **AAR 軌跡按 `t.exercise_id`（m038 per-point、ingest 凍結）開窗** → 上場殘渣不進下場 AAR。**收尾＝驗證 + 鎖測試 + 文件**（非新功能）：+1 explicit 生命週期測試（`test_track_timeline_survives_entity_restamp_not_pollute_next`：entity restamp 回 NULL 後 A 場 AAR 不被抹、不污染 B 場；既有 `test_cross_exercise_isolated` 亦已覆蓋）+ 設計 SoT §3.3 結案註記。**umbrella [#471](https://github.com/winson3QQ/ICS_COMMAND/issues/471)（演習/實戰＝記錄窗口 + faction 可見性模型重設計）全 7 片（#472–#477）到齊、關閉。** |<br>**[2026-07-04 faction 敵我符號 + 開場鈕 mutex UI（`frontend-v1.27.0`，現場 dogfood）]**：① **紅軍還是藍框** → self-SA 單位（有 team_color 的裝置本身）地圖符號改吃 **faction**（`factionToAffiliation`：紅→hostile/紅框、藍→friendly、中立→neutral；`cotToSidc` 加 affiliationOverride 改 SIDC 位2；**faction 才是權威隊別、非 CoT type**）——紅隊裝置即使自報 a-f 也畫敵；**裝置丟出的敵情標記不吃 faction、維持原 CoT type**（藍隊標的 a-h 敵蹤仍是敵）。主要利白隊全視角。② **跨 session 好像能開第二場** → 實為前端 UX 漏洞（後端 `set_active` 原子 mutex 早已擋、實測開不成第二場）：面板對其他場照顯「啟動」→ 走完精靈才 409。修：有 active 場時其他場「啟動」禁用 + 顯「需先封存進行中場次」，讓鎖在 UI 可見。純前端；+6 vitest（factionToAffiliation/cotToSidc override、啟動禁用/可啟動）。<br>**[2026-07-04 AAR 回放版面 + seed 灌爆修（`backend-v2.35.2`/`frontend-v1.27.1`，dogfood）]**：① **地圖被時間軸壓小**（iPad）——`#aar-root` 改 `height:100dvh`（iOS Safari `height:100%` 鏈條在動態工具列下不穩 → `#aar-body` 拿不到確定高、地圖 `flex-basis:55%` 失效被列表撐小）+ `#aar-map` `min-height:180px` 防塌 + `overflow:hidden` 只讓步驟列捲。② **時間軸「10:38 十幾筆分類變更」**——#477a 開場繼承 seed（待命池→這場）每台 `upsert_faction` 各寫一筆 `client_faction_classify` → 灌爆 AAR 同一時刻；改 `upsert_faction` 加 `action_type`，seed 用 `client_faction_seed`（非 timeline 白名單），真人中途重分隊仍走 classify 留 timeline。+1 測試。 |
| **——— 【應用 ⑥ 演習驗證 + 評估（TTX / AAR）】ICS 強項區；P2-22 為實戰延伸 gate ———** | — |
| P2-19 | **情境注入 + O/C 控制台**（新 `services/scenario_service.py`）：讓 O/C（Observer/Controller）向學員 ATAK 注入合成情境 + 向 Dashboard 注入事件/任務。三組件：(A) **ATAK 側注入**：`tak_rest_client.py` 推合成 CoT（`how="h-g-i-g-o"` → `simulated=True`，P2-11b）到 TAK Server，學員 ATAK 地圖出現合成位置/威脅；(B) **腳本執行器**：JSON 腳本（`[{t_offset, action, params}]`）→ background task 按時觸發；`action` 嚴格白名單（`inject_cot` / `inject_event` / `inject_mission`），Pydantic strict 驗證，**禁任何動態執行路徑**；(C) **O/C 控制頁** `/admin/exercise-control`（獨立頁面，與學員 dashboard 分離，sysadmin role-gate）+ `scenario_designer.html` → API 接線（export JSON → `POST /api/exercises/{id}/scenario/upload`）。**安全**：注入 API sysadmin-only（commander/operator 403）；per-exercise mutex（`scenario_running` 欄位，並發第二個 run → 409）；合成實體地圖顯示虛線框 + `[SIM]` callsign 前綴（學員看得出是情境道具）；archive 時 `simulated=True` 實體整批清除（防污染下一場或實戰）。<br>**📐 動工必讀**：演習/實戰 server-authoritative、不信 client `opex`（TAK-C 拒絕）、`simulated` 只由 O/C server 端設 → [strategy §1.1](roadmap/tak-integration-strategy.md)（自 memory 收編）。 |
| ✅ P2-20 | **AAR 回放（Wave 6 前移）**：(A) **後端統一時間軸 API**：`GET /api/exercises/{id}/timeline` → 按 t 排序的合併陣列（`{type, t, actor, payload}` from cop_entity_tracks + events + chats + missions audit）；(B) **前端 AAR 頁面** `/aar/{exercise_id}`（獨立頁面，非 ops dashboard toggle；layout：地圖 60% + 通聯/事件面板 40% + 底部時間軸 slider）；**Step mode（優先）**：逐事件跳進（桌面推演首選）；**Play mode（後）**：1x/2x/4x 連續播；地圖顯示各 entity 選定時間點位置 + **軌跡尾跡**（過去 N 分鐘路徑線）；**Touch-friendly**（iPad 觸控，點擊目標 ≥ 44px，時間軸可 swipe）。**安全**：`COMMAND_ROLES` + `resolve_scope()` 跨演習存取守門；軌跡 PII retention policy（90 天 TTL 或 exercise 刪除 cascade，`docs/compliance/threat_model.md` 文件化，P2-17）；AAR 匯出（若做）= COMMAND_ROLES + `AAR_EXPORT` audit log<br>**[2026-06-11 (A) 完成 — [#200](https://github.com/winson3QQ/ICS_COMMAND/pull/200) `d40b875`（issue [#199](https://github.com/winson3QQ/ICS_COMMAND/issues/199)）]**：`GET /api/exercises/{id}/timeline`＋新 `services/timeline_service.py`——四源（tracks JOIN cop_entities／events／chats／**audit 白名單** decision_*／TAK_DOWNLINK／COP_SHARE_TAK／event_status_updated，event_created 不取防雙計）合併按 t 排序、統一 `{type,t,actor,payload}`＋meta（count/t_start/t_end/**truncated**，limit+1 探測不誤報）；`?from=&to=`＋limit clamp，RBAC=中央 gate COMMAND_ROLES（同 /tracks）。**設計定案＝事件流、不落盤快照**（虛擬時鐘等速回放、seek=前端記憶體 keyframe；業界調查 event-sourcing/遊戲回放/PAR TAK Data Replay 三域同證，全記 #199）。review 修 2：`event_repo.occurred_at` 入庫 `iso_utc` 正規化（offset 混 Z 排錯序）＋truncated 探測；裁決：soft-deleted 軌跡**刻意保留**（AAR 完整歷史，同 /tracks 先例）。測試 10（合併排序/shape/不雙計/時間窗/truncated/跨場隔離/403/正規化）。**(B) 前端 AAR 頁面延後**（動 map.js＝P2-33/part-3 領地）→ 本 row 保持 ⏳。<br>**[2026-06-11 (B) B1 完成 — [#203](https://github.com/winson3QQ/ICS_COMMAND/pull/203) `de71509`（issue [#201](https://github.com/winson3QQ/ICS_COMMAND/issues/201)）]**：reality check 推翻「(B) 須動 map.js」——**獨立頁** `static/aar.html`＋`static/js/aar/`（直接 import maplibre_core/auth 模組，**map.js/events.js/main.js 全不碰**＝與 P2-33b/c、part-3 零衝突）。B1=骨架＋**Step mode**（側欄事件流 40% 點選/↑↓ → T 跳轉 → 自帶 map 實例 60% 折疊 tracks ≤T 畫單位）；事件無自身座標 → 側欄呈現（上圖經 junction 留 v2）。三雷已繞（sessionStorage per-tab → 同分頁進出／CSP enforce → 全外部模組＋登錄 ENFORCE_PATHS／pmtiles load 不 fire → 能加先加＋styledata 重試）。vitest ×8、JS suite 261 綠、preview 實走（enforce 下登入→Step→折疊正確）。**B2（Play mode＋虛擬時鐘＋尾跡）/ B3（觸控收尾）續 #201；Human verify 待使用者實機**；版號等 B1-B3 功能組全齊再進 frontend MINOR。<br>**[2026-06-11 (B) B2 完成 — [#206](https://github.com/winson3QQ/ICS_COMMAND/pull/206) `fb2b4d2`]**：虛擬時鐘（rAF＋wall-delta×速率 1x/2x/4x、delta cap 1s 防隱藏分頁回前景瞬移、t_end 自停、空白鍵）＋底部播放列（▶/⏸＋速段＋range slider＋T 鐘；scrub/跳步/書籤自動暫停）＋軌跡尾跡（tracksByUid→10 分窗 LineString→aar-trails 層）＋增量折疊游標（前進 O(Δ)、倒退 reset，#199 不做 keyframe 成立）＋bookmark 用虛擬 T（**#204「Play 中打標記」解鎖**）。review 修 3（_ensureLayers idempotent guard／meta 兜底／NaN T 早退）；security 0。vitest 266 綠；preview 實彈 scrub/尾跡正確（rAF 播放=preview hidden 環境凍結，實機即動）。**剩 B3：iPad 觸控收尾＋Human verify**。<br>**[2026-06-22 AAR dry-run 修正批次 — PR [#336](https://github.com/winson3QQ/ICS_COMMAND/pull/336) merge `0fa5821`（issues [#334](https://github.com/winson3QQ/ICS_COMMAND/issues/334)／[#335](https://github.com/winson3QQ/ICS_COMMAND/issues/335)）]**：實機 dry-run 回饋一批修正——**#334** 指揮台設定面板加 **AAR 回放入口**（COMMAND_ROLES、同分頁導航）；**#1 播放卡死**：虛擬時鐘 `msToT` 砍秒 → 每幀次秒進度被丟、永遠跨不過當前秒，加全精度 `_playMs` 累進器（顯示時才砍秒）；**#3 (0,0) null island 壞點**（ATAK 無 GPS fix／手點前）前端 `validCoord` + 後端 `_record_track` 雙端濾，尾跡不再拉線到 (0,0)；**#335 (#4)** 單位改 **live 同款 MIL-STD-2525 符號**（milsymbol，via `cotToSidc`+`bakeMilSymbol`，敵我色烤進 SIDC 圖框＝與 live 一致）+ 無 sidc 退敵我配色點 + 尾跡線依敵我態配色（友藍/敵紅/中綠/不明黃）。`/code-review`（8 角度，混精度時間戳/bake seq-guard/配色漂移三疑慮逐一查證後 refuted；修過時 docstring）+ `/security-review`（後端僅 `SELECT` 多取 `e.type`、參數化無新 input → 0）。vitest 341／timeline pytest 10／eslint・pyflakes 全綠；dogfood `aar-milsym-05b8c5e` 使用者驗 #1 PASS。**v1 限制**：軌跡不存歷史敵我態 → 用 entity 現值；漸層淡出尾跡與 per-feature 敵我配色互斥留 polish。**仍 pending（row 不關）**：B3 iPad 觸控＋#2 區域上圖／#5 事件上圖（junction 座標）＋整組 Human verify。`backend-v2.13.3`／`frontend-v1.9.5`。<br>**[2026-06-22 #2 區域 + #5 事件/接觸上 AAR 圖 — issues [#338](https://github.com/winson3QQ/ICS_COMMAND/issues/338)／[#339](https://github.com/winson3QQ/ICS_COMMAND/issues/339)]**：**#2 區域時間精確重現**：`cop.py _audit_cop` 對 polygon/route 記**整包 attributes 快照**（vertices+label_anchor，關鍵路徑記錄器、不可回填）→ timeline `zone` 源（`detail LIKE attributes` 預篩）→ 前端 `foldZonesAt`（畫/改/刪 @ T，delete≤T 移除）+ fill/line 渲染。dogfood 實證畫→移label→改形狀→刪正確重現。**#5 事件+接觸上圖**：事件經 `event_markers` junction 帶 marker + audit 位置歷史 → **騎在標記折疊位置**（跟隨移動標的）；**符號重用 live**——military regime→milsymbol 2525（drone 紅/QRF 藍方/不明黃，via taxonomy `cot_type`→`cotToSidc`）、civil→severity ◆ + NAPSG 象形/abbr，`/api/event_taxonomy` 同 SoT；**#1** 人類描述標籤（非 event_code）；長按「**TAK 標記**」敵我接觸（`kind=contact`）→ audit 歷史出 track-type → 走 **#335 milsymbol 單位層**（友藍方/敵紅菱/中綠方/不明黃梅花）隨 T 移動。`/code-review`（cross-exercise 洩漏／事件↔contact 雙畫／alert 死碼 皆 refuted；修誤導註解）+ `/security-review`（SQLi 全參數化／XSS 僅 maplibre 表達式+textContent／authz exercise-scoped／json.loads 系統寫入 → **0**）。vitest 353 + timeline pytest 18 全綠；dogfood 多輪使用者驗（符號對齊 live PASS）。**限制**：孤兒標記（標圖時無 active 場 → `exid=None`）不進任何 AAR；移動/label 需 active 場記錄。**仍 pending**：B3 iPad 觸控＋整組 human verify；symbology 兩份決策（`eventsToGeoJSON` vs `_renderZones`）記後續抽共用 resolver。<br>**[2026-06-22 P2-20 收尾 ✅]**：**B3 iPad/iPhone 觸控 + 整組 human verify 由使用者實機完成** → B1-B3 功能組齊（#201）；AAR 回放（時間軸 / Step / Play / 區域 / 事件 / 敵我接觸 / 尾跡 / milsymbol 符號）核心完整可用。`backend-v2.14.0`／`frontend-v1.10.0`。**選做留後續**：AAR 匯出（`AAR_EXPORT` audit）、symbology 共用 resolver、零星 polish。 |
| ⏳ P2-21 | **演習指標 + 課程標記（[issue #204](https://github.com/winson3QQ/ICS_COMMAND/issues/204)）**：(1) 回放時可 bookmark → 帶時間戳的 AAR entry（`created_at=T+N`，連結回放時間點）；(2) 演習統計面板（**COMMAND_ROLES 限定，不暴露為 public API**）：MEDEVAC 請求 → 確認反應時間、Mission 下達 → EXCHECK 完成率、GeoChat 通聯量 by 組；(3) 演習報告（COMMAND_ROLES + `AAR_EXPORT` audit log）<br>**[2026-06-11 子集完成 — [#205](https://github.com/winson3QQ/ICS_COMMAND/pull/205) `3914d6e`（#204 reality check 4 發現 + 決策 C）]**：① `GET /api/exercises/{id}/kpis`（新 `kpi_service`）——事件處置時長/通聯量 by 組/決策裁示時長/軌跡量/AAR 條目數（=P2-22 gate 對帳）；**誠實邊界** medevac/excheck → null+reason。② **bookmark 已可用**（#204 推翻「全卡 P2-20(B)」）：m026 `aar_entries.ref_t`（**`created_at=T+N` 原文誤導已更正**——牆鐘載不了回放 T）+ category `'bookmark'` + AAR 頁「標記此刻」鈕 + 書籤 chips 跳轉。③ `AAR_EXPORT` audit（report/ml_export audit-first）。review 修 2（group 計數覆蓋→累加；垃圾 ref_t→422）。**仍 pending（issue 不關）**：MEDEVAC 反應時間（**決策 C＝「承接」動作**，#204 comment 記錄，待落地）/ EXCHECK 完成率（卡 P2-18）/ Play mode 中打標記（卡 P2-20(B) B2）/ 統計**面板 UI**（dashboard 側＝P2-12 領地，現為 API-only）。 |
| P2-22 | **TTX Gateway（實戰延伸層解鎖條件）**：**P2-15 ~ P2-18 被此 gate 保護**，TTX 通過後才解鎖（**P2-14 已於 2026-06-08 提前解鎖**——與 #161/#173 同屬 TAK 同步 feature、是當前 dogfood 需求，不受此 gate 限制，見 P2-14 行說明）。完成標準：≥ 2 次完整演習流程（建立 → 腳本注入 → 學員應變 → 指揮回應 → 歸檔 → AAR）；每次 AAR ≥ 5 條課程標記；`cop_entity_tracks` 完整無缺失；`simulated` 實體無污染到下一場（驗清除機制）；無系統崩潰。**ZELLO**：語音通聯暫走 ZELLO（不整合），頻道分配記錄在 exercise SOP 文件；P3 WaveInk 上線後取代 |
| **——— 【應用 ⑦ TTX Gateway(P2-22) 後解鎖的延伸 + 多機構】federation 需第二台 server + peer cert 治理(#8)———** | — |
| P2-15 | **Federation 設定**：server-to-server 交換 CoT（`:9000`/`:8444`）。含 `fed-truststore.jks` + step-ca peer cert profile。**Security**：peer cert 加入須 sysadmin 審批 + audit log；`fed-truststore.jks` 變更納入 change management。先單機 PoC → 跨機關（Wave 7+）<br>**[2026-06-08 admin UI 實證]**：TAK Server 有 federation 管理 GUI（`Configuration → Federation` + `Federate Certificate Authorities`）。現況 **federation server DISABLED**；scaffold 齊（V1 `:9000` TLS1.2/1.3、V2 `:9001`、`Allow Mission/DataFeed Federation: true`、**`Allow Federated Delete: false`**、Mission Disruption Tolerance 12h、`fed-truststore.jks` 路徑已在）、**無 peer**。→ **GUI 開關 trivial，真正瓶頸＝第二台 TAK server + peer cert 治理（缺口 #8）**；對齊「gated（P2-22）+ 需第二台 + 低即時價值」評估，現行只先備 PKI 地基（軸2）。`Allow Federated Delete: false` 亦印證跨域刪除預設關（呼應 #161/P2-14 可靠刪除主線）。 |
| P2-16 | **影像串流整合**（Marti REST `/video/`）：URI reference only，**ICS Command 不 proxy 串流**。可選 / 低優先 |
| P2-18 | **EXCHECK 任務查核整合**（Marti REST `/excheck/`）：ICS-204 任務指派追蹤。依賴 P2-11；視 P2-13 downlink 完成度決定優先順序 |
| ⏳ P2-26 | **ICS 作為交付產品的 TAK admin console（cert / role 管理，[issue #177](https://github.com/winson3QQ/ICS_COMMAND/issues/177)，另一 session 進行中）**：ICS 交付給場域時須能管 TAK 端 **cert / role / group**（對應 Marti `/federatecertificates`、`/groups`、admin「Manage Users / Client Certificates」），讓部署者**不必裸操 TAK admin GUI**。接 **federation 治理（缺口 #8：peer cert 審批 + audit log）**。**RBAC** sysadmin-only；**每次 cert/role 變更強制 audit**（不得 best-effort）。屬**交付化（product-ization）** 而非 COP 功能，與 P2-15 federation 互補。**[2026-06-09 漸進重構 S5 留痕：對應活躍 code，非本 doc session 實作]**<br>**[2026-06-09 L1 落地 — [#178](https://github.com/winson3QQ/ICS_COMMAND/pull/178)（merge `8d2120c`）]**：L1 = cert/role provisioning 腳本層（`issue-tak-certs.sh` 抽 `sign_client_cert()` 簽 3 張 client cert／新 `register-tak-fingerprint.sh` dry-run+`--apply`／config `TAK_MARTI_READ|WRITE_CERT/KEY`）。**端到端實測（活 docker TAK 5.7-RELEASE-43）推翻 #176 cert-role 數前提**（收 memory `tak-marti-authz-model`）：讀取靠 truststore 信任、`register fingerprint` 對 Marti 讀寫 gating **無作用**；寫入靠 **mission-role（`MISSION_WRITE`/owner）**；`creatorUid` 偽造擋得住；docker **不 hot-reload** 須 restart。`register` 腳本**重定位**為 server-admin 級（L2 admin console CLI 前身），非讀寫前置。`/security-review` 0 finding、`/code-review` 2 項已修。**主體（L2 admin GUI + cert/role/group 管理）仍 ⏳**。<br>**[2026-06-21 L2 第一刀 — admin TAK tab + 裝置證自助發放（[#315](https://github.com/winson3QQ/ICS_COMMAND/issues/315)）— 真機 iTAK dogfood PASS]**：admin 設定頁加 **TAK tab**（sysadmin-only）—— TAK 連線開關搬入（P2-24 #164）+ **TAK 裝置證自助發放**：`POST /api/admin/tak/device-cert` → Python 組 ATAK/iTAK data package（client p12 + truststore + pref，密碼內嵌 `atakatak` 免打），取代 deploy-time CLI。config `TAK_DEVICE_CONNECT_HOST`（對外 TAK 位址）+ `TAK_DEVICE_CA_DIR`。**四個 reality check（皆真機/容器/log 實證，逐個推翻假設）**：(1) iTAK 吃 **legacy p12**（openssl `-legacy`，modern PBES2/AES 匯入沒反應，同 #307）；(2) **flat zip** 結構（cert/pref/MANIFEST 子目錄 iTAK 不認，借使用者 OpenTAKServer 樣本格式）；(3) **truststore = TAK server CA**（裝置驗 server）；(4) **🔑 client 證必須 ICS-TAK-SVC-CA（offline `tak-ca.key`）簽** —— TAK truststore 只信它、**step-ca 簽的證 TAK 回 `peer not verified`**（log 實證），故**後端用 offline TAK CA 私鑰簽**（與 ICS 登入證 step-ca **刻意隔離** #305；推翻初稿「後端不持鑰」，threat_model §8.3 改正）。+ 第五關 8089 對外 forward（部署）。最終 TAK log `Added Subscription` + iTAK 位置上 ICS COP。**doctrine 推翻 #255「UI 不簽證」對裝置證的部分** → sysadmin 線上簽 + 強制 audit（`tak_device_cert_issue`）。`/code-review`（FileNotFoundError→502 順序修 + callsign 防禦驗證）+ `/security-review`（clean，callsign allowlist 擋 openssl `-subj` DN 注入）。`backend-v2.10.0` / `frontend-v1.9.0`。<br>**[2026-06-22 裝置證列管 [#317](https://github.com/winson3QQ/ICS_COMMAND/issues/317)]**：reality check 確認 **TAK 不記 offline 證**（TAK `certificate` 表實測=0、CoreConfig 無 CRL、truststore 信整個 CA 無 per-cert 拒絕）→ ICS **自建 `tak_device_certs` 盤點表**（m030；記 callsign+**serial**+operator+status，serial 為 #318 CRL 前置）+ TAK tab「已發裝置證」列表 + 撤銷-flag（**明標不 enforce、裝置照連**）。`backend-v2.11.0` / `frontend-v1.9.1`。<br>**[2026-06-22 #324/#325 — PR [#326](https://github.com/winson3QQ/ICS_COMMAND/pull/326) merge `b15fe0c`]**：**#324** 中文 callsign 發證 500 修補——`Content-Disposition` 用 `isalnum()` 濾檔名，但 Python `isalnum()` 對中文回 True → 中文進 HTTP header → uvicorn `UnicodeEncodeError` → 500（且 `record_issued`/`audit` 在 `Response` 前跑 = 留 active 幽靈列）；改 ASCII-only `filename=` + RFC5987 `filename*=UTF-8''` 保留中文檔名。**#325** `DELETE /api/admin/tak/device-certs/{id}`（sysadmin、強制 audit、僅刪 `status='revoked'`、active 拒→404）+ 前端已撤銷列「刪除」鈕（清累積 revoked / 幽靈列；**刪紀錄≠撤證**）。`/code-review` 0、`/security-review` 0；dogfood 實證主教發證 200 + DELETE×6 清幽靈全 200；release `release-b15fe0c` 上線。`backend-v2.12.0` / `frontend-v1.9.2`。<br>**[2026-06-22 桌機/ATAK 憑證 UX 三修]**：**#327**（PR #328 `c7ca485`）桌機登入憑證——後端 `GET /api/admin/ca/root`（sysadmin，下載 step-ca root CA PEM 供桌機信任 server）+ 前端 .p12 標 Windows/iMac/Android、「下載 root CA」鈕、信任提示（root CA 匯「登入」勿系統根、cert-bound session 須綁帳號、Chrome 換證重開）；**真機 iMac 連入 PASS**。`backend-v2.13.0`/`frontend-v1.9.3`。**#329**（PR #331 `2407973`）ATAK 裝置證 pref 補 `deviceProfileEnableOnConnect`（對齊使用者實機成功包）；**真機 ATAK 安卓連上 TAK PASS**——**了結上面「ATAK 真機驗」⏳**。`backend-v2.13.1`。**#330**（PR #332 `2afe368`）桌機 Chrome 分享 data package `NotAllowedError` → 自動退回下載（`canShare` 回 true 但 `share({files})` 桌機丟錯）；**真機 iMac PASS**。`frontend-v1.9.4`。憑證上手坑收 memory `desktop-cert-onboarding-gotchas`。<br>**[2026-06-26 #404 — TAK 存取控制層1：`__ANON__` 死群隔離 enforcement（PR [#405](https://github.com/winson3QQ/ICS_COMMAND/pull/405)）]**：源碼定讞 TAK `X509Authenticator` 對 CA 信任證**架構上永不拒絕**（無群落 `__ANON__`）→「CoreConfig 白名單擋連」死路、存取控制只能靠 group。落地：① `register-tak-fingerprint.sh` 拒 `__ANON__` 預設（須顯式 named group，`--allow-anon` 逃生口）；② `registrar.sh` reconcile 回 groupList（awk 兼容單/多行）+ 新 `strip-anon` op；③ `admin.py` reconcile 回 per-user `in_anon`/`anon_users`（偵測 producer 卡 `__ANON__`）+ `online_anon`（在線匿名連線＝被刪帳號/未授權仍掛著，補名冊盲區）+ 新 `POST /tak/users/{cs}/strip-anon`（SYSADMIN+audit）；④ 前端面板：面板級⚠ + per-row 徽章 + 一鍵「移出匿名群」+「🔴 在線匿名連線」段；⑤ `ics-tak-admin`（REST-only）`__ANON__` 良性豁免（`anon_exempt`，不誤報）。`threat_model.md` §8.3 機制定讞 block + 兩層解法（隔離/撤銷）。`/code-review`（high：awk 漏單行回歸已修 + asyncio/取值韌性 + strip 空 fp 409）+`/security-review` **0**。雙實機 dogfood E2E PASS（偵測/一鍵修/在線匿名顯示）。`backend-v2.20.0`／`frontend-v1.16.0`。**真「踢連」＝層2 撤銷 [#318](https://github.com/winson3QQ/ICS_COMMAND/issues/318)**（補登 TAK `certificate` 表 + `x509checkRevocation`，#403 已實證可行）。<br>**[2026-06-26 #318 Slice 1 — 層2 真撤銷後端（PR [#406](https://github.com/winson3QQ/ICS_COMMAND/pull/406)，`abd16f2`，不 Closes #318）]**：reality check 定案**方案 C**（CRL 只擋 :8443 不擋 :8089 串流 → DB 撤銷唯一覆蓋主威脅）。撤銷現行裝置證 → ICS 直寫 TAK `certificate`(hash+revocation_date) → `x509checkRevocation` 在 :8089 串流/:8443 REST 連線層**踢掉該證**（非僅 deregister 降匿名），**按 fingerprint 精準**（同 callsign 多證只撤這張）、infra(ics-cot/admin)護、無 fp 跳過、best-effort 不 raise。新 `services/tak_revocation.revoke_in_tak`（lazy `pg8000`、冪等 exists→UPDATE/else INSERT、`finally` 關連線）+ config `TAK_DB_*`（空=不寫僅帳面）。`/code-review`（唯一風險＝fingerprint 格式對齊 ICS↔TAK `certificate.hash` → 指定 dogfood 釘死）+`/security-review` **0**（SQLi 全 `:name` 參數化、authz=sysadmin gate、密碼不落 log）。**dogfood 實證**：①**格式**——reality-check 證 colon-uppercase openssl fingerprint（`sha256=13:A4:…`）插 `certificate.hash` + `x509checkRevocation=true` → :8089 連線被擋，正是 `cert_issuance` 產的格式 → 格式風險 closed；②**真 pg8000 寫**——`revoke_in_tak`（INSERT＋冪等 UPDATE 雙分支）對**活機 tak-database** 落正確列（hash/subject_dn/revocation_date 正確、NOT NULL 全滿足、第二次呼叫仍只 1 列、清理歸零）。unit 6＋integration 3 全綠。**未含（下 slice）**：provisioning（CoreConfig `x509checkRevocation` + ICS `.env` `TAK_DB_*` + restart 上 live enforce）、前端撤銷文案（明標不 enforce→真 enforce）、README/threat_model §8.3、整機 iTAK E2E。無版號 bump（slice、未 live）。<br>**[2026-06-26 #318 Slice 2 — live enforce 接通（backend-v2.21.0／frontend-v1.16.1）]**：reality check first 揪出方向關鍵後落地。**provisioning**：CoreConfig `<auth x509checkRevocation="true">`（git-tracked + bind-mount 持久）+ ICS `deploy/prod/.env` `TAK_DB_*` + compose env → dashboard 撤銷時 `revoke_in_tak` 直寫 TAK `certificate` 表。**前端**：撤銷文案誠實化（降 `__ANON__` 隔離、非硬斷線、在線證需重啟）+ 回饋 `tak_revoke` 結果 + 修「真撤銷需 CRL」舊文案。**docs**：threat_model §8.3 三點校正 + deploy README provisioning。**活機 reality check（皆實證）**：(a) **fail-open**——`certificate` 表空時開旗標，ics-cot/iTAK restart 後照常重連（無 row=放行、不誤鎖合法證）；(b) **撤銷≠硬斷線**——被撤證降 `__ANON__`（`RevokedException` 後仍 `Added Subscription`），靠層1 隔離達 recv 0（兩層必合流）；(c) **快取**——「先撤再連」即時生效（`revtest-318pre`/`revtest-e2e` 當場 `RevokedException`），「在線已認證」證 TAK 快取其有效性（實測 >5min、撐到 restart）→ 即時踢在線證須**重啟 TAK 清快取**（業主拍板：當網路不穩定處理，client 自動重連）。**端到端 dogfood**：prod ics-command 容器（is_configured=True）跑 `revoke_in_tak('revtest-e2e')` → TAK log `RevokedException: CN=revtest-e2e` 連線被擋；測完清乾淨（`certificate`=0）。<br>**真撤銷 enforce（[#318](https://github.com/winson3QQ/ICS_COMMAND/issues/318)）後端碼 ✅ Slice 1（PR #406）+ provisioning/機制 live ✅ Slice 2（PR #407）。**❗ 真機 iTAK dogfood（2026-06-26）推翻「核心完整可用」**：撤銷對**全新證**即時生效，但對**已連過/被快取的證**（實機 iTAK 用的正是這種）**撤了照連**，需**重啟 TAK 清快取**才生效（實證 `blue-01`：重啟前 `Added Subscription`、重啟後 `RevokedException`；同一張證翻轉）。更揭三個 backfill/盤點缺口。**→ #318 未完成，開 Slice 3（[#408](https://github.com/winson3QQ/ICS_COMMAND/issues/408)）**——**part ①② 已落地**（`backend-v2.22.0`/`frontend-v1.16.2`：`POST /tak/revocations/backfill`「↑ 撤銷補登 TAK」鈕一次推 ICS 已撤+有 fp 證進 TAK 表 + 已撤無 fp 證標「⚠ TAK 撤不掉」；PR [#409](https://github.com/winson3QQ/ICS_COMMAND/pull/409) merge `448f9fb`，`/code-review`+`/security-review` **0**，**使用者實機點鈕 dogfood PASS**：HTTP→endpoint→TAK 表寫入 2/2 + audit 留證 + 12 張 NULL-fp 標示）；**part ③④ 續**：① **go-live backfill**（把 ICS 已撤、有 fingerprint 的證一次推進 TAK 表——現況 #318 前撤的全沒寫進 TAK）② **NULL-fingerprint 舊證**（12 張無 hash 可撤）UI 標「TAK 撤不掉」+ 重發途徑 ③ **盤點完整性**（如 `blue-01` 根本不在 `tak_device_certs`、UI 撤不到）④ **撤銷即重啟**（或排程重啟）入 SOP，讓快取對在線證即時。<br>**[2026-06-26 #318 Slice 3 part ③④ 落地（`backend-v2.23.0`/`frontend-v1.16.3`）]**：**reality check 定案**——(③) TAK API **不吐連線證 hash**（`clientEndPoints`/`contacts/all/full`/`certadmin` 皆無，certadmin 只認 TAK 自發證）→ ICS 無法自動發現不明連線證，**改操作員驅動**；(④) **無安全的容器內路徑觸發 TAK 重啟**（ics-command 無 docker.sock、registrar 獨立容器只共享 network ns）→ **走手動 SOP**（不引入 docker.sock 攻擊面）。**part③**：`POST /tak/revocations/by-fingerprint`——操作員自取 fingerprint（`openssl x509 … -fingerprint -sha256`）貼入撤盤點外/非 dashboard 證；格式驗證 + **禁撤 infra**（`tak_revocation.infra_fingerprints` 按 hash 比對；stdlib `_cert_sha256_fingerprint` 實證 == openssl == TAK `certificate.hash`）+ 強制 audit；前端 TAK 面板加輸入框。**part④**：面板常駐 SOP 提示「撤在線證 → 部署機 `docker restart takserver` 清快取（~95s）」。integration 5 測試全綠。PR [#410](https://github.com/winson3QQ/ICS_COMMAND/pull/410) merge `2cc4a95`，`/code-review`+`/security-review` **0**，**使用者實機驗 3 案例 PASS**（禁撤 infra ics-cot→403、`blue-01` by-fp 撤→寫入 TAK、`hello`→格式擋）；查 `certadmin/cert/active`=`[]` 確認本部署 **TAK 自發證 0 張**（自發證反而易撤=certadmin 列得出 hash，但這套沒有；撤起來難的全是 ICS offline 證）。**未解硬限制（誠實標）**：操作員拿不到證的不明連線證 per-cert 撤不掉（TAK 不吐 hash）；12 張 NULL-fp 永久撤不掉（已 UI 標）。**→ #318 撤銷能力核心完整（發/撤/補登/by-fingerprint/誠實標示/重啟 SOP）**，可評估 Close。**仍 ⏳**（更大範圍）：dev `takdevpass123` 汰（§8.4）；#255 連線設定 DB/UI 化、role/group、federation 治理。<br>**[2026-06-27 enrollment 落地 — :8446 Certificate Enrollment（[#411](https://github.com/winson3QQ/ICS_COMMAND/issues/411) / umbrella [#420](https://github.com/winson3QQ/ICS_COMMAND/issues/420) Phase B）— 真機 TAK Aware dogfood PASS]**：場端 TAK Aware 1.8.1 改走 **enrollment**（繞過 data-package `-25300` app 缺陷：登記 server 卻不存 client identity）。落地：CoreConfig `<certificateSigning CA="TAKServer">`（簽證庫 `signing-ca.jks` = ICS-TAK-SVC-CA，簽出證被 :8089 truststore 信任）+ UserManager **bcrypt** 帳號 → 簽出證進 TAK `certificate` 帳本、**reconcile 成權威、消除 offline 簽撤銷盲區**。**安全配套（A→B 順序強制）**：VPN 前置（[#280](https://github.com/winson3QQ/ICS_COMMAND/issues/280) WireGuard）先收 TAK 公網入口（:8089 forward 刪、:8446 經查從未 forward、外部實測不可達）**才**上 CA（CA 私鑰上線進 TAK = blast radius，threat_model §8.3/#323）。實機 `3QQ-takaware` 連上 :8089 實證（`Set client ... to 3QQ-takaware`）。**三坑**：手寫明文密碼=401/`Invalid salt`（須 `usermod -D` 再建）、密碼 ≥15 含特殊符、CSR 須含 nameEntries DN（O/OU）否則 `CSR validation failed`。可重現步驟 `deploy/tak-server/ENROLLMENT.md`、memory `tak-enrollment-working`。**仍差「完整管理」**：[#403](https://github.com/winson3QQ/ICS_COMMAND/issues/403) 門禁 Phase C（非名冊 cert 仍落 `__ANON__`、deregister 只降匿名非真擋）+ dashboard 發證流程接 signClient + `aware1` 測試帳號正式化（明文、待 RBAC）。<br>**[2026-06-27 #429 — dashboard 主導 enrollment 發證落地（PR #430，真機 PASS）]**：ICS 面板「發裝置證」改走代理 enrollment（`services/tak_enrollment.py`：admin cert `new-user` 建 managed user → CSR → :8446 signClient → 證進帳本），取代 #315 offline。iTAK 經面板發證走 WG 連 :8089 成 **managed user(neutral)** 實證。dogfood 修 4 坑（new-user 缺 groupIN/OUT NPE、signClient/v2 回 JSON 非 PEM、enroll_status ok、connectString 改 WG IP）。`TAK_ENROLL_URL` 空則回退 offline（零破壞）。完補 #420 殘留①。<br>**[2026-06-27 #431 — enrollment 證補綁 fingerprint（PR [#432](https://github.com/winson3QQ/ICS_COMMAND/pull/432)，`fce03b8`）]**：修 #429/#430 漏洞——enrollment 模式只 signClient 發證、**漏把 fingerprint 寫回 TAK 名冊**（`admin.issue_tak_device_cert` 的 `use_enroll` 分支硬寫 `enroll_status="ok"` 且跳過 `enroll_device`）→ 帳號停在密碼認證、reconcile 對不上帳本 fingerprint（面板顯「未同步」）、證不可撤（#318 撤銷需 cert hash）。**改**：移除 `use_enroll` if/else，兩種發證模式**統一呼叫 `enroll_device(cn, fingerprint, group)`（registrar `usermod -f` 綁 fingerprint）**，不再硬寫成功值。順修 #430 遺留 4 個破測（`build_device_package` mock 漏收 `use_enrollment` kwarg → `TypeError`，CI 暫停才沒擋到）+ 補迴歸測試 `test_issue_enrollment_mode_binds_fingerprint`。**live 驗證**：手動補綁 `3QQ-itak` 後 reconcile fingerprint==帳本→已同步、裝置不斷線；新映像 `bdb7dec` 上 prod、發新證自動綁定 PASS。`/code-review` LGTM。**過程另揪 ops bug**：registrar `usermod` IPC 因 cutover 重啟 takserver 致共享 netns（`network_mode: service:takserver`）失聯 → 自 cutover 起**所有 usermod 寫操作（register/deregister/strip-anon）靜默失敗、只 reconcile 讀檔活著**；已 `docker restart` registrar 修復，**治本（compose takserver↔registrar 重啟連動 / healthcheck）待另開單**。**殘留**：去密碼（純證帳號）硬化；`TAK_ENROLL_TIMEOUT_S=8` 對 cold-JVM `usermod` 偏短（enroll_status 報 timeout 但綁定成功，面板同步狀態以 reconcile 為 SoT 不受影響）。<br>**[2026-06-28 #434 — 容器化 WireGuard + ICS 統管 VPN（PR [#435](https://github.com/winson3QQ/ICS_COMMAND/pull/435) merged，`backend-v2.24.1`）— 實機 E2E PASS]**：補「ICS 完整管理 TAK client」的**另一半（連線）**。**reality-check 定案走容器化（方案 B）**：Windows-native WG 跨 OS 邊界、容器內 ICS 驅動不了（`wg.exe` 要 host admin、無 netns 捷徑）；改 Linux 容器後 ICS 比照 tak-registrar 經共享卷佇列加/刪 peer。三關實測：kernel WG（WSL2 6.6 原生）、公網 UDP 穿 Docker NAT 握手 OK、漫遊 ~60–90s 恢復。**落地**：① `deploy/perimeter/wireguard/container/`（alpine+wireguard-tools；entrypoint wg0+wg0→TAK/dashboard DNAT 路由；`wg-peer-registrar.sh` 控制面 injection-safe；`wg-persist.sh` 重啟不丟 peer）；② ICS 端 `services/wg_provision`（純 Python X25519 keygen〔與 `wg` 相容實證〕+ 配 IP〔`wg_peer_repo`/m034〕+ add/remove peer + `.conf` 組裝 + QR〔segno，德國 BSD-3，非中國〕）；③ **發證整合**：`admin.issue_tak_device_cert` 在發 TAK 證時連帶產 keypair/配 IP/註冊 peer/夾 `.conf`+QR **外層 bundle**（一檔含 TAK 包 + WG，測試者掃 QR + 匯 TAK 包兩步上線）；撤證連動撤 peer；best-effort 不擋發證（WG 未配置/失敗仍交付純 TAK 包）；④ compose `ics-wg` 服務（profile wg；`WG_HOST_PORT` 並排測試可調）。**~60 測試綠**。**並排 E2E（prod，不碰 live Windows WG）**：發 `3QQ-atak3` → 自動配 peer(10.13.13.2) → 掃 QR 連容器 WG(51822) handshake → DNAT → **裝置同時上 TAK**（`subscriptions/all` 在線）；4G 上 `3QQ-itak`（live Windows WG）同時不斷 = 並排零干擾。**dogfood 修 bug**：entrypoint `umask 077`（server key 用）洩漏到 watcher → 結果檔 600、ICS（uid 10001）讀不到 → 誤判失敗；修＝watcher `umask 022` + entrypoint 子殼 scope。**剩**：階段3 cutover（停 Windows WG、容器轉正、全裝置重發新 bundle）；前端結果面板顯 X-WG-Status / WG 帳本 UI；threat_model 補容器 WG 段。<br>**[review fix `backend-v2.24.1`]**：`/code+security-review` 抓唯一 blocker——`allocate_and_record` 並發配同號撞 unique index 的 `IntegrityError` 未處理 → 在 TAK 證已發後冒 500 穿透 best-effort 契約；修＝repo IntegrityError 重算重試 + `admin.issue_tak_device_cert` WG 整段 try/except backstop（WG 任何例外都 fallback 純 TAK 包）。其餘（注入面/keygen/私鑰/撤證連動/SQLi）LGTM。+2 測試。**prod 已 redeploy `2.24.1`**（branch 已刪）。<br>**[2026-06-28 #434 follow-up — 前端 WG 可視化 + 持久化 hotfix]**：**#437**（PR [#437](https://github.com/winson3QQ/ICS_COMMAND/pull/437)，`frontend-v1.17.0`/`backend-v2.24.2`）發證結果面板顯 `X-WG-Status` + WG peer 帳本 UI（admin TAK tab）。**#438**（PR [#438](https://github.com/winson3QQ/ICS_COMMAND/pull/438)，`backend-v2.24.3`/`frontend-v1.17.1`）**WG peer 在線指示**——registrar 新 `status` op 回各 peer latest-handshake（`<pubkey>\t<unix>`，0=從未），ICS `peer_handshakes()` 解析 → 帳本 🟢/⚪。**#439**（PR [#439](https://github.com/winson3QQ/ICS_COMMAND/pull/439)，無版號 bump＝ics-wg 腳本非 ics-command 映像）**部署 dogfood hotfix**：① **持久化路徑**——`wg0.conf` 原寫 `/etc/wireguard`（容器 ephemeral fs，docker restart 留著但 `--force-recreate`〔每次重部署〕即 wipe）→ 改 `/wg-data` 持久卷（與 `server.key` 同卷）；prod force-recreate 後 2 peer 留存實證、現場裝置不再因重部署斷線。② **0600 硬化**——持久 `wg0.conf` 含 server PrivateKey（`wg showconf` dump），`wg-persist.sh` 加 `umask 077` 鎖 0600 與 server.key 一致。**`/security-review` #434 全套 clean**（命令注入：4 欄位 op/pubkey/ip 正則閘門後才走 argv、label 只進 log echo + `_submit_op` 過濾換行；路徑穿越：`<id>`=server `uuid4().hex`；RBAC：三新路由 server 端 `require_role(SYSADMIN)`；SQLi 全參數化；越權：watcher 獨立 re-validate subnet；keygen：X25519 CSPRNG 每裝置新鑰；私鑰不入 log）。<br>**[2026-06-28 #434 階段3 cutover 完成 — 容器 WG 轉正、退役 Windows-native WG]**：盤點確認**全部 active ICS 名冊裝置（`tak_device_certs`/`wg_peers` 各 2：`3Q-itak3`/`3QQ-atak3`）早已在容器 WG（公網 `1.34.230.218:51822`）** → 「全裝置重發」moot。Windows-native WG（:51820，PID 63164，僅餘業主自用舊裝置、業主確認可棄）→ **`Stop-Service` + `StartupType Disabled`**（提權 UAC；**可逆**——保留 tunnel 設定不 uninstall）→ :51820 釋放（`Get-NetUDPEndpoint` 確認只剩容器 :51822）、容器兩 peer handshake 即時不受擾。**容器埠維持 51822**（零重發，業主拍板；51820 空出可日後清）。**殘留（業主手動，非阻塞）**：router 移除 :51820→Windows 的 port-forward。**threat_model §8.9「容器化 WireGuard 周界」已補（v1.1：特權容器/佇列控制面/DNAT 信任邊界 + 撤證連動撤 peer 雙層撤 + 消除 WG 盲區 + zero-trust 對照）。** **→ #434 核心 + 階段3 cutover + 威脅模型 完整。**<br>**[2026-06-28 儀表板 VPN-gate（#280 周界一致化，`backend-v2.25.0`/`frontend-v1.18.0`）]**：補「ICS 也走 VPN」——#434 把 TAK :8089 收進 VPN 後，儀表板 :443 仍公網直曝（mTLS 守）＝不對稱。**reality check 揪出關鍵**：瀏覽器走 VPN 連 WG 私網 IP 會 TLS 主機名驗證失敗（nginx server cert SAN 實測只含公網 IP `1.34.230.218`+localhost，無 `10.13.13.1`）。**解法繞開 SAN**：device `.conf` 的 `AllowedIPs` 加公網 IP/32（config `WG_EXTRA_ALLOWED_IPS`）→ 瀏覽器**照用原網址**、封包改走 tunnel → 既有 DNAT(wg0 :443→nginx，實測規則 match 任意 dest :443) → SAN 已含公網 IP；WG transport 封包由 fwmark 排除不成迴圈（0.0.0.0/0 full-tunnel 同機制）。**落地**：① `build_device_conf` 併入 `WG_EXTRA_ALLOWED_IPS`；② `POST /api/admin/wg/issue`（純儀表板使用者發 WG-only bundle，不簽 TAK 證，sysadmin+強制 audit）+ `_bundle_wg_only`；③ `POST /api/admin/wg/peers/revoke`（撤 WG-only peer；TAK 裝置仍走撤證連動）；④ 前端 TAK 面板加「發 WG-only 設定」+ WG 帳本列「撤除」鈕（TAK-linked 標「隨證撤」唯讀）。RBAC＝`/api/admin/*` default SYSADMIN（golden 矩陣 +2 列）。**~70 測試綠**（含 AllowedIPs ×3 / wg-issue ×4 / wg-revoke ×3）；ruff + esbuild 綠。**殘留（業主手動、可逆、分階段）**：部署層設 `WG_EXTRA_ALLOWED_IPS=1.34.230.218/32` + 移除公網 :443 forward → 儀表板僅 VPN 可達（公測可暫留 mTLS-公網，§8.8 記明可接受）。**真機驗待辦**：掃新 QR → 確認儀表板經 tunnel 用原網址可達 + tunnel 仍握手（fwmark 假設）。威脅 threat_model §8.9.1（v1.2）。 |
| **——— 【收尾 ⑧ 規格】P2-07~21 完成後補 TAK 章 ———** | — |
| P2-17 | **規格書補 TAK 整合章節**（P2-07 ~ P2-21 完成後補）：含 Marti REST cert 認證 SOP（非 OAuth2，#138 更正）、Mission downlink 流程、planned/actual 視覺、TTX 運作 SOP（ZELLO 頻道分配、ATAK 學員設定）、track PII retention policy、TAK 信任邊界（補入 `docs/compliance/threat_model.md`）。<br>**[2026-06-09 確認 pending]** `command-dashboard/docs/指揮部儀表板設計規格.md` v3.0 **尚無 TAK 章**。過渡期 TAK 架構 SoT＝[`tak-integration-strategy.md`](roadmap/tak-integration-strategy.md)（含 §1 invariant / §4b 生命週期 / §4 API 矩陣）；本項動工時從 strategy + threat_model §8 收斂進規格書正式章。 |

### TAK 介面整合缺口與架構決策（2026-06-05）

以下缺口在對應項目動工前需設計決策。

| # | 缺口 | 現況 | 影響 | 決策方向 |
|---|---|---|---|---|
| ✅ 1 | **CoPEntity `source` enum 凍結** | `schemas/cop.py:21` 加 `"command"`（P1-03 解凍） | **[#144](https://github.com/winson3QQ/ICS_COMMAND/pull/144)** | 完成：table rebuild（`foreign_keys=OFF`，CHECK 不可 ALTER；`legacy_alter_table` 實測無效已棄用）。`CoPSource` 含 command |
| 2 | **CoPEntity 缺 `planned` / `simulated` 欄位** | 無 planned/actual 區分；無合成標記欄位 | P2-11b | P2-11b 統一加（`planned: bool = False` + `simulated: bool = False`）|
| 3 | **GeoChat taxonomy 衝突** | `events` 表 FK → event_types（NAPSG）；GeoChat 通聯不符 NAPSG | P2-07 | **決策：獨立 `chats` 表**（ICS-214 通聯語意不同 NAPSG 事件）|
| 4 | **TAK 在線狀態須 bypass exercise scoping** | `resolve_scope()` 套 cop_entities；presence 是基礎設施狀態 | P2-12 | `cop_hub.broadcast(exercise_id=None)`；前端不套 exercise filter |
| 5 | **前端 UI 落點無既成 panel** | P1-11 已移除 left sidebar；無現成 panel 位置 | P2-12 | **P2-12 統一一次 layout 設計決策**，三面板同批落地 |
| 6 | **cop_hub None 廣播前端守門** | `cop_stream.js` 是否誤丟 `exercise_id=null` 廣播未確認 | P2-12 | 確認 `cop_stream.js` filter 邏輯 |
| 7 | **CoT 裝置准入隱性信任假設** | ICS 信任 TAK Server 所有 CoT；裝置准入是 TAK 管理員責任 | 全部 CoT 流入 | `docs/compliance/threat_model.md` 文件化（P2-17）；P2-10 內容層驗證作最後一道防線 |
| 8 | **Federation peer authorization governance** | `fed-truststore.jks` 無正式審批流程 | P2-15 | sysadmin 審批 + audit log；納入 change management |
| 9 | **CoPEntity 缺 `simulated` 欄位** | 合成注入實體（`how="h-g-i-g-o"`）無標記，archive 後無法辨識清除 | P2-19 | **P2-11b 統一補**（與 `planned` 同批）|
| 10 | **O/C 控制頁落點** | 無 sysadmin 專屬頁面；放主 dashboard 學員可能看到 | P2-19 | 獨立頁面 `/admin/exercise-control`（sysadmin role-gate）|
| 11 | **scenario_designer.html 脫離 API** | 52KB 靜態工具，無任何 API 連接 | P2-19 | 補 export → JSON → `POST /api/exercises/{id}/scenario/upload` 接線 |
| 12 | **AAR 頁面 layout 未定** | 無現成頁面；嵌主 dashboard 與即時 ops 模式衝突 | P2-20 | 獨立頁面 `/aar/{exercise_id}` |
| 13 | **PII retention policy 未定義（兩個保留域，目前都無界）** | `cop_entity_tracks` 累積 = 人員移動時間序列（高度敏感）；無清除 SOP。**[2026-06-08 admin UI 實證]** 還有**第二個域**：TAK Server PG。TAK 有保留 GUI（`Administrative → Data Retention`，per-type TTL：Cot/GeoChat/**Mission(含 tracks/files)**/Files + 排程器），**但現況全 TTL 空白、排程 `Never`** → TAK PG 也無界（呼應 #161 server 留全部）| P2-06a/P2-20 | **兩域分治**：① TAK 側 = 部署時開 Data Retention GUI（設 TTL + 排程）② ICS 側 = `cop_entity_tracks` 自管 TTL（TAK 保留管不到 ICS SQLite）。皆 90 天 TTL 或 exercise 刪除 cascade；文件化 threat_model.md（P2-17）。<br>**✅ ICS 半 done — [#207](https://github.com/winson3QQ/ICS_COMMAND/issues/207)（[#259](https://github.com/winson3QQ/ICS_COMMAND/pull/259) `84e4f23`，P2-20 收尾）**：`retention_service` 90 天 TTL 每日清理 + SYSADMIN 開關 `GET/POST /api/admin/retention`（audit-first、`exercise_id=NULL` 不隨單場 cascade）+ `idx_cop_tracks_t` 索引 + threat_model §8.4 政策 + 6 測；`/code-review`（1 fix：索引）＋`/security-review`（0）。開關 API-only。TAK PG 半待部署 ops 開 Data Retention GUI。|
| 14 | **Scenario runner 並發控制** | 無 per-exercise mutex；兩腳本同時跑 → 學員地圖混亂 | P2-19 | `exercises.scenario_running` 欄位；第二個 run 請求 409 |
| 15 | **REST ingest 端點缺機器間認證**（→ TAK-A）| `POST /api/tak/events` 要求 WRITE_ROLES session token；外部 TAK server / federation 無 session 取得機制（無 API key / HMAC inbound / service account 路徑）→ 端點對「REST federation push」用途事實上不可呼叫 | P2-03 後 / 任何需要 REST federation push 的場景前 | **三選一**：(A) HMAC inbound（對齊 Pi-node `verify_hmac` 模式）；(B) mTLS inbound client cert；(C) 只保留 :8089 pull、標此端點 internal-only。動工前先確認 TAK federation 架構需求（影響 P2-11 M2M auth 設計） |

#### 確認架構決策（後續 PR 的 SoT）

1. **認證**：Marti REST API（`:8443`）走 **client cert（mTLS）**，複用 :8089 同一套 step-ca cert（`TAK_CLIENT_CERT/KEY/CAFILE`）。**[P2-11 #138 規格更正]** 原述「OAuth2 client credentials」為錯誤前提 —— 官方 TAK Server 5.7 Marti 走 cert，OAuth2 是 OpenTAKServer 才有（見 memory `tak-server-marti-cert-not-oauth`）。
2. **下行指令**：走 **Mission API**（group-scoped + 持久），**不走** pytak TXWorker（無 group scope）。
3. **GeoChat 落地**：進獨立 `chats` 表（ICS-214 通聯語意），**不進** `cop_entities`，**不進** NAPSG `events` 表。
4. **幾何解析共用**：`services/geometry_service.py` 統一解析 CoT `<shape>` 與 DataSync GeoJSON，P2-08 建、P2-14 共用。
5. **多媒體原體邊界**：照片/串流只保留 URI reference，ICS Command **不 proxy 原體**。
6. **DataSync URI 不 follow**：`datasync_service` URI 為純 string，**永不 HTTP fetch**（防 SSRF，OWASP A10）。
7. **合成實體清除**：演習 archive 時所有 `simulated=True` 的 cop_entities 整批清除，**不污染下一場或實戰模式**。

### Definition of Done

**降階紀律（跨層，2026-06-09 SC6）** — 每個碰 TAK 的 item 完成時必驗
- [ ] 宣告所需 **Tier**（0 全通 / 1 降階 / 2 全掛）+ TAK 掛掉時的 **fallback**（對照 [strategy §4 矩陣](roadmap/tak-integration-strategy.md) #3 欄）
- [ ] **斷線負向測試**：`TAK_ENABLED=false` 或模擬 :8089/:8443 斷線時，該功能**降階（退回 manual / Pi / cached stale）而非報錯 / 白屏**
- [ ] 兩軸狀態（連線 transport vs 信心 DCI）不混；來源 chips 收斂（SC4）併 P2-12

**地基層**
- [ ] `cop_entity_tracks` 每次 CoT upsert 後有寫入（含 exercise_id），5s min-interval 抽樣（P2-06a）
- [ ] `GET /api/exercises/{id}/tracks` 回傳完整時間序列，COMMAND_ROLES 限定（P2-06b）
- [x] `cop_entities` 加 `team_color`/`role`/`battery` 一等 column + team_color index（源自 CoT `__group`/`status`，設計 A）（P2-06c）
- [x] `GET /api/cop/squads` 回 per team_color 聚合（total/online/avg_battery/centroid），READ_ROLES（P2-06d）
- [x] `dashboard_service.build_dashboard()` 含 `tak_squads`（dashboard 對 TAK 不盲視）（P2-06d）
- [x] GeoChat（`b-t-f`）**不進** `cop_entities`（負向），進 `chats` 表；XSS 負向（P2-07）
- [x] CoT `<shape>` 幾何萃取正確，`geometry_service.py` 單元測試通過（P2-08）
- [x] MEDEVAC 9-line 欄位完整萃取（→ `attributes["medevac"]` 摘要 + severity critical，設計 B 進 attributes 不開新表），schema 對齊 P3-06（P2-09）
- [x] Marti REST `:8443` 走 **client cert（mTLS）** 複用 :8089 cert（**規格更正：非 OAuth2**，#138）；`build_marti_ssl_context` fail-closed 無 CA 不裸奔（P2-11）
- [x] `TakRestClient`：rate-limit + 指數退避重試（cap）+ poll scheduler + 並發鎖；`_join_url` 杜絕 SSRF host override（P2-11）
- [x] CoPEntity 加 `planned` / `simulated`（`_m017` ADD COLUMN，bool↔INTEGER），現有資料不破壞（P2-11b）
- [x] CoPEntity `source` enum 加 `"command"`（`_m018` table rebuild `foreign_keys=OFF`，動態重建 index）（[#144](https://github.com/winson3QQ/ICS_COMMAND/pull/144)，issue #141）

**核心雙向層（Human Verification 必通）**
- [ ] 真實 ATAK 推 CoT → 地圖 < 5s 更新（**human verify**）（P2-10）
- [ ] CoT 內容層驗證：座標越界拒絕、callsign 白名單、type prefix 白名單（P2-10）
- [ ] XXE 防護（`defusedxml` forbid_dtd/entities/external）（P2-10）
- [ ] 三個 UI 面板 human verify：在線人員 + 通聯記錄（`textContent`）+ MEDEVAC card（P2-12）
- [ ] Mission downlink：ATAK 收到指令，`planned=True` 空心框顯示（**human verify**）（P2-13）
- [ ] Mission push RBAC：COMMAND_ROLES 以外 403 負向；audit log `MISSION_PUSH_TAK` 存在（P2-13）
- [ ] **E2E bidirectional**：真實 ATAK app ↔ ICS Dashboard 雙向全鏈驗證（P2-10 + P2-13）

**TTX 演習驗證層**
- [ ] 情境腳本執行：上傳 → background 按 t_offset 觸發；action 白名單拒絕非法 action（P2-19）
- [ ] 注入 API sysadmin-only，403 負向；runner 並發 409（P2-19）
- [ ] 合成實體：`simulated=True`，虛線框 + `[SIM]` prefix，archive 清除（**human verify**）（P2-19）
- [ ] AAR timeline API 回傳 tracks + events + chats + missions 合併時間軸（P2-20）
- [ ] AAR Step mode：逐事件跳進，地圖顯示對應位置 + 軌跡尾跡（**human verify**）（P2-20）
- [ ] AAR Touch-friendly：iPad 可操作時間軸（P2-20）
- [ ] TTX Gateway：≥ 2 次完整演習，simulated 無污染，track 無缺失（P2-22）

**實戰延伸層**
- [ ] DataSync URI follow 負向：`datasync_service` 不發任何 HTTP request to DataSync URI（P2-14）
- [ ] DataSync Mission geometry 進 cop_entities route/polygon（P2-14）
- [ ] Federation 雙向流測試（P2-15）
- [ ] EXCHECK 任務狀態顯示於 dashboard（P2-18）
- [ ] **Tag**：`command-v2.3.0`（TAK 整合 MINOR；rebase 至 2.x）

### Compliance touchpoints

- **NIST SP 800-53 SC-8 / SC-13**：TAK Server TLS 8089/8443 強制
- **NIST SP 800-53 IA-9**（服務識別與認證）：Marti REST M2M 走 **client cert（mTLS）**（P2-11；規格更正非 OAuth2）
- **NIST AC-3**（存取控制）：TAK presence bypass exercise scope（P2-12）；軌跡 COMMAND_ROLES 限定（P2-06b/P2-20）
- **NIST AU-2 / AU-12**（Audit Events / Audit Record Generation）：Mission push audit log（P2-13）；AAR export audit log（P2-21）；scenario inject audit（P2-19）
- **ASVS V13 (API) + V5 (Validation)**：CoT XML XXE 防護；Marti REST mTLS cert 認證；CoT 內容層白名單（P2-10）
- **OWASP API2（Broken Authentication）**：Marti REST 走 client cert（mTLS）；無 CA 不裸奔 fail-closed（P2-11；規格更正非 OAuth2）
- **OWASP A03（Injection / XSS）**：GeoChat `html.escape()` + `textContent`（P2-07）；CoT callsign / type 白名單（P2-10）；scenario 腳本 action 白名單 + Pydantic strict（P2-19）
- **OWASP A04（Insecure Design）**：O/C 注入 API sysadmin-only gate（P2-19）；scenario runner mutex（P2-19）
- **OWASP A10（SSRF）**：DataSync URI 永不 follow（P2-14）；影像串流 URI reference only（P2-16）
- **ASVS V7（Error Handling / Logging）**：Mission push 必須留痕（P2-13）；AAR export audit（P2-21）
- **MIL-STD-2525C**：planned（空心）/ actual（實心）/ simulated（虛線框）符號視覺區分（P2-11b/P2-13/P2-19）
- **ICS-214 Unit Log**：GeoChat → `chats` 表；MEDEVAC 9-line incident card（P2-07/09）
- **ICS-204 Task Assignment**：EXCHECK 任務查核（P2-18）
- **PII / 台灣個資法**：`cop_entity_tracks` 人員移動軌跡資料 retention policy（90 天 TTL）；`docs/compliance/threat_model.md` 文件化（P2-17）
- **TAK 信任邊界假設**：裝置准入責任在 TAK 管理員；threat_model.md 文件化（P2-17）

### 風險與決策點

| 風險 | 緩解 |
|---|---|
| 官方 TAK Server Java 資源需求高（RAM ≥ 8GB），Pi 500 邊緣 | P2 部署目標暫定 x86 mini-PC（N100 class），Pi 500 留作 client / 備援 |
| Federation cert 與內網 step-ca 整合 | 沿用 ICS_DMAS C1-B 既有 step-ca 內網 PKI；fed-truststore.jks 管理 SOP 已在 P2-01 #101 |
| License：TAK Server 部分 plugin / DataSync 模組是 commercial | P2 只用 core CoT + Marti REST（Apache 2.0 core）+ federation，不依賴 commercial plugin |
| ~~OAuth2 client credentials 在 TAK Server 5.7 設定文件稀少~~ **[P2-11 #138 已解]** | reality check 證實官方 TAK Server 5.7 Marti 走 **client cert（mTLS）非 OAuth2**（OAuth2 是 OpenTAKServer）→ 已改用 cert（複用 P2-03 step-ca），risk 消解 |
| CoPEntity schema v2 migration 可能破壞 P1-03 凍結契約 | P2-11b 統一一次完成（不分批）；P1-03 contract tests 作為回歸守門 |
| 前端 UI 落點無既成 panel（P1-11 已拆 left sidebar）| **P2-12 統一一次 layout 設計決策**，三面板同批落地，避免各項各自貼 UI |
| ~~JWT refresh token 若裸存明文，成為新 secret 洩漏點~~ **[P2-11 #138 不適用]** | P2-11 改走 client cert（mTLS）非 OAuth2 → 無 JWT / refresh token，此 secret 洩漏點不存在 |
| CoT 資料完整性：任何 ATAK 裝置可推入 COP | 內容層三道驗證（座標 / callsign / type 白名單，P2-10）；裝置准入靠 TAK cert enrollment |
| Federation governance：加 peer cert 無正式審批流程 | sysadmin 審批 + audit log；fed-truststore.jks 變更納入 change management（P2-15）|
| 情境腳本 server-side 執行：惡意 payload 若沒擋住 | action 白名單 + Pydantic strict；**禁任何動態執行路徑**（P2-19）|
| 合成實體污染實戰模式：archive 後 simulated 實體殘留 | `simulated=True` flag + archive 整批清除 + TTX Gateway 驗證（P2-19/P2-22）|
| 軌跡 PII：cop_entity_tracks 累積長期人員位置 | 90 天 TTL + COMMAND_ROLES 限存取 + retention policy 文件化（P2-06a/P2-20）|
| AAR 暴露歷史指揮決策：可能成為 OPSEC 洩漏點 | COMMAND_ROLES 限定 + resolve_scope 跨演習守門 + 匯出 audit log（P2-20）|

---

## Phase 3 — WaveInk 整合（COP 第二個外部來源）

**目標**：WaveInk 的 STT 結果經 NLP parser 結構化後流入 COP；無線電通聯自動轉成事件 / ICS-214 條目。

### Scope

| Item | 說明 |
|---|---|
| **——— 【基礎 ① 前置 + 合約 + ingress 管線】單一 feature：WaveInk 語音→事件 ———** | — |
| **P3-00** | **前置阻塞**：WaveInk 端完成「資料平面分離」架構（見下方〈WaveInk 資料平面與邊界協定〉），且 WaveInk License 確定（建議 Apache 2.0，理由見《License 對齊》）。否則 P3 不啟動 |
| P3-01 | 與 WaveInk repo 議定 **API 合約 v1**（WS / REST、payload schema、auth）並 commit 至兩邊 `docs/api/waveink-contract-v1.md` |
| P3-02 | `routers/ingress.py`（P1 重構成果）增 `POST /api/ingress/waveink` 與 `WS /api/ingress/waveink/stream` 兩條路徑 |
| P3-03 | `services/waveink_service.py`：接 WaveInk push（`{transcript, timestamp, callsign?, channel, audio_ref, consent_id?}`）→ 寫入 audit + 觸發 NLP parser。`audio_ref` 視為 **opaque URI**，**不下載、不解析、不快取本體** |
| **——— 【應用 ② 正規化 + 落地】NLP → normalize（共用 cop_service）→ MEDEVAC → 前端面板 ———** | — |
| P3-04 | NLP parser：以規則 + LLM 雙軌（沿襲 ICS_DMAS AI roadmap）抽取 callsign / 座標 / SALUTE 元素 / MEDEVAC 9-line 欄位 |
| P3-05 | `cop_service.normalize_waveink(parsed)` — 結構化結果 → COP event（與 TAK CoT event 走**同一個 normalize 層**） |
| P3-06 | MEDEVAC 9-line：**ICS_Command 無 Medical PWA**，9-line 落地為 COP 上的 incident card + ICS-214 自動條目（取代 WaveInk README 中「→ Medical PWA」的目標） |
| P3-07 | 前端：commander_dashboard 加「無線電通聯時間軸」面板，可點擊回聽原始音檔。**回聽走瀏覽器直連 WaveInk** 的私有儲存（用 WaveInk 自己的 auth / signed URL），ICS_Command **不做 audio proxy、不暫存音檔**——保持「raw audio 永不跨界」邊界 |
| **——— 【收尾 ③ 測試 + 規格】———** | — |
| P3-08 | 測試：mock WaveInk push、NLP parser 結構化準確率回歸、prompt injection 防護（沿襲 ICS_DMAS C5-E）、**ingress 端拒收 raw audio payload** 的負向測試 |
| P3-09 | 規格書補 WaveInk 整合章節 + 修訂 ICS-214 自動填表流程 |

### Definition of Done

- [ ] WaveInk push → COP 顯示 < 3 秒（不含 STT 推論時間）
- [ ] NLP parser 結構化欄位準確率 > 85%（沿用 ICS_DMAS Phase 2 E2B baseline）
- [ ] Prompt injection 測試 green
- [ ] MEDEVAC 9-line demo：模擬語音 → 自動填好 incident card
- [ ] **負向測試**：ingress 收到含 base64 / binary audio 欄位的 payload 應 reject 並 audit
- [ ] **Tag**：`command-v2.4.0`（WaveInk 整合 MINOR；原規劃 1.2.0，rebase 至 2.x）

### Compliance touchpoints

- **OWASP LLM Top 10 LLM01 (Prompt Injection)**：必測
- **ASVS V8 (Data Protection)** + **台灣個資法 / 通保法**：原始音檔保管、同意書與 retention policy **完全在 WaveInk 端**；ICS_Command 因「邊界協定」自然不在個資範圍內
- **NIST AI RMF MEASURE 2.7**：輸出結構準確率追蹤
- **License 對齊**：見下方〈License 對齊〉

### WaveInk 資料平面與邊界協定（架構決策）

P3 整合的前提是 WaveInk 採以下五原則設計訓練 / 運行資料平面。原則歸 WaveInk repo 負責落實，ICS_Command 在介面層強制執行：

1. **資料平面隔離** — 程式碼可開源，**原始音檔走外部私有儲存**（自架 MinIO / Cloudflare R2 / Backblaze B2 / 私有 git repo + LFS），git 中只放指針
2. **同意書 registry** — 每段錄音對應一筆 metadata：`{recording_id, recorded_by, recorded_at, location, purpose, consent_scope, retention_until}`；無同意書不准進訓練集（自己本人錄音例外，但仍須登記 `recorded_by=self`）
3. **Pointer-only in git** — `pairs.jsonl` 結構為 `{audio_uri, sha256, duration_s, language, consent_id, transcript}`，音檔本體不入 git
4. **模型作為邊界** — ICS_Command 只消費**模型輸出（結構化文字事件）**，永不觸碰 raw audio；WaveInk push 到 ICS_Command 的 payload 內**不得**含 base64 編碼音檔（ingress 端會 reject）
5. **模型也是個資衍生品** — 若模型用真實錄音 fine-tune，模型權重納入個資政策；對外發佈前須確認訓練語料同意書涵蓋「模型發佈」用途

**ICS_Command 在這個架構中的責任邊界**：

| 範圍 | 屬於 ICS_Command | 屬於 WaveInk |
|---|---|---|
| 原始音檔儲存 | ❌ | ✅ |
| 同意書管理 | ❌ | ✅ |
| Retention policy | ❌（不存）| ✅ |
| 個資稽核 | ❌（介面不接觸）| ✅ |
| 結構化事件儲存與 audit | ✅ | ❌ |
| `audio_ref` URI 引用記錄 | ✅（僅 URI string）| ❌ |

### License 對齊

- **建議**：WaveInk 與 ICS_Command **皆採 Apache 2.0**。理由：
  1. **Patent grant**：SDR / 軍用周邊有專利地雷，MIT 沒保護
  2. **採購對標 TAK Server**：DoD 自己的 TAK Server 就是 Apache 2.0，整條 stack license 對齊（TAK Apache 2.0 + milsymbol MIT + MapLibre BSD-3 + Breeze ASR Apache 2.0 + WaveInk Apache 2.0 + ICS_Command Apache 2.0）
  3. **政府 / 民防採購友善**，無 AGPL 的法務障礙
  4. **商業模式不受傷**：consulting、hosted service、訓練資料 / 模型微調包不被 license 鎖
- **GPL 隔離**：WaveInk 內部 `rtl-sdr`（GPL-2.0）以 `subprocess` 呼叫即可避開 linking，repo 加 `NOTICE.md` 標明
- **替代方案**：若需保留商業控制，BSL 1.1 → N 年後轉 Apache 2.0（HashiCorp 模式）；但會增加採購談判摩擦

### 跨 repo 議題

- WaveInk Phase 進度與 ICS_Command P3 啟動時點需對齊（WaveInk 自述 P1/P2 仍在設計階段）
- API 合約 v1 (P3-01) 與資料平面架構 (P3-00) 是雙邊變更，需在兩 repo 同步 PR review

---

## 跨來源 / 新增 feature（情境檢討衍生 SC）

> **與各 Phase 平行的 feature 區塊**（使用者 2026-06-09 決定 A）：SC1/2/3/5 是**不屬單一 phase 的新來源 / 能力 feature**——SC1/2/3 非 TAK（故不放 Phase 2），升為**第一級 feature 區塊**、與各 phase 的 feature 群平行。SC4/6/7 已折進既有群（見內文末）。

> 來源：19 情境深挖（[`tak-use-cases-config.md`](reference/tak-use-cases-config.md)）+ TAK 整合策略（[`tak-integration-strategy.md`](roadmap/tak-integration-strategy.md) §6）累積出的跨情境候選。**SC1–SC7 為 ROADMAP 級 backlog ID**（同 RT-*/TAK-* 用法）。
> **✅ 2026-06-09 S4 圈定（使用者「都進」）**：SC1–SC5 全進、SC6 已入 DoD、SC7 進。**⚠️ 真正啟動＝開 GitHub issue**（PROCESS.md step 2，ROADMAP item ↔ issue）——**doc-only 不算數、沒人會去看**（使用者 2026-06-09 點明）；下方每項標「待開 issue」者即尚未進工作佇列。

| 代號 | 候選 | 服務哪些情境 | 與既有 ROADMAP 關係 | 依賴 | 狀態（S4 圈定）|
|---|---|---|---|---|---|
| **SC1** | **天氣 / 環境 feed**（雨量 / 風 / 風向 / 河川水位）| 颱風水災 · 野火（60% 受困死亡集中 3% 火災天氣日）· HazMat（plume 完全靠風）· 航空 | **全新**——非 TAK；**RC 修正：唯讀疊層（P1-17 facilities 模式），非 cop_service**（areal/raster 參考資料，非 entity）| 不依賴 TAK（**降階獨立**）| ✅ **進**（頭號）· **待開 issue** |
| **SC2** | **sensor source 模式**（CBRN / SDR / UGS / CCTV → COP）| HazMat（CBRN 偵測器）· 設施巡邏（CCTV/UGS/雷達）· SDR/RF | **半新**——泛化 P3 WaveInk 的 sensor ingest；對應 TAK `/Marti/api/datafeeds` + `/inputs` | `cop_service` normalize（與 WaveInk 共用接縫）| ✅ **進** · 與 P3 共用 normalize 接縫 · **待開 issue** |
| **SC3** | **主動告警 / 決策觸發**（geofence 後端 + 門檻告警）| 設施巡邏 · 指揮決策（OODA Decide「何時」）· HazMat 門檻 | **全新後端**——ICS 自建（非 TAK）；現多被動視覺（severity pulse / stale 灰）| SC2（感測器進來門檻才有料）| ✅ **進**（排 SC2 後）· **待開 issue** |
| **SC4** | **連線 / 降階可視化收斂**（來源 chips、退役 P2-23 獨立 `cd-tak` 燈、連線 vs 信心兩軸分離）| 全情境（降階）| **改**——併 P2-12 面板 + 重構 P2-23；修「`cl-server`+`cd-tak` 兩 dot 無 legend」破口 | P2-12 | ✅ **進** · **併 P2-12**（不另開，P2-12 動工時一起）|
| **SC5** | **威脅圖層**（`threat_state` 動態區 / 武裝掩護已清走廊 / 敵我 + 老化威脅情報）| RTF · 多機構 | **新**——擴 P1-16 zone（加 threat_state 屬性）+ P2-08 | P1-16 / P2-08 | ✅ **進（later 排程）** · 依 P1-16/P2-08 · **待開 issue** |
| **SC6** | **降階 DoD 紀律**（每 TAK item 宣告 Tier + fallback + `TAK_ENABLED=false`/斷線負向測試）| 全 TAK 能力 | **改**——加進 Phase 2 DoD（S3 已落地，見下方 DoD「降階紀律」段）| — | ✅ **已入 DoD** |
| **SC7** | **安全 backlog 重排優先級**（RTF/多機構給「人命級」論據）——項目皆**已在 [`threat_model.md`](compliance/threat_model.md) §8**：**§8.2 TAK-D**（`visible_to` 跨機構分級，現重歸 P2-12）/ **§8.4**（LUKS+金鑰，→ P1-12）/ **§8.5**（cert 撤銷 + federation 治理 #8，open gap）。註：**COP poisoning 本身大致已解**（TAK-B 來源守門✅ + P2-10 內容白名單✅）；§8.3 裝置准入為架構假設 | RTF · 多機構 · 共享 COP | **改**——**不是 threat_model 埋字**（沒人看）；改為**既有 §8.x 各項升優先級 + 開 issue** | threat_model §8 既有項 | ✅ **進＝升優先 + 開 issue**（#161/P2-14 已 dogfood 提前解鎖；**[2026-06-12 已開]** §8.4 LUKS → [#231](https://github.com/winson3QQ/ICS_COMMAND/issues/231)・§8.5 cert 撤銷 → [#232](https://github.com/winson3QQ/ICS_COMMAND/issues/232)；剩 §8.2 TAK-D 隨 P2-12 不另開）|

> **原則**（使用者拍板）：**統一檢視非全做**——但本輪「都進」（使用者 2026-06-09），多數仍**待開 issue 才進工作佇列**。
> **下一步（S4 收尾）**：SC1/SC2/SC3/SC5 + SC7 的升級項，**開 GitHub issue**（每個帶 SC 代號 + 一句 scope，依 PROCESS.md step 2）→ 才真正可被排程動工。SC4 隨 P2-12，SC6 已落地。

#### 升正式 item — 按 feature 群（編號 + 目的 + **reality check 後 scope/DoD**，2026-06-09）

> **編號決策**：沿用 **SC 代號為正式 item ID**（同 RT-*/TAK-* 非 Pn 前例）——SC1/SC2 是**非 TAK 的新 COP 來源**，塞 Phase 2(TAK) 會誤導、叫 Phase 4 又暗示排在 WaveInk 後（天氣 feed 可能更早要），故獨立編號、排程獨立。
> **以下 scope 經 reality check（盤實際 code）**——抓到 SC1 假設錯誤（見各項），動工前仍應對動工當下 code 再驗。

**——— 【新增來源 feature：非 TAK，與 TAK / WaveInk 並列為 COP 來源】———**

**SC1 — 天氣 / 環境 feed**
- **目的**：給 COP **氣象/雨量/風/河川水位**參考層，讓颱風水災·野火·HazMat 從「事後反應」轉「**預判**」。
- **⚠️ RC 修正**：**不走 cop_service/cop_entities**（原假設錯）——天氣是 areal/raster 參考資料、非 entity、不綁演習、無 auth → **走 P1-17 facilities 唯讀疊層模式**。
- **Scope**：`services/weather_store.py`（copy `facilities_store.py` mtime-cache 唯讀模式）+ `GET /api/weather`（public、cacheable、非 exercise-scoped）+ `static/weather.seed.json` 或 cron 刷新腳本 + 前端 raster/grid 疊層 + 工具列開關。**零 cop_service/migration/auth 改動。**
- **供應鏈**：dataset 來源逐一驗**非中國**（台灣 CWA 中央氣象署 / 水利署開放資料，比照 P1-17 NCDR）。
- **降階**：**完全獨立於 TAK**（TAK 掛照常）。**DoD**：唯讀疊層、不碰 cop_entities/scoping、來源非中國、斷源顯「來源不可達」。

**SC2 — sensor source 模式**
- **目的**：**泛化感測器 ingest**（CBRN/SDR/UGS/CCTV）→ `cop_service` normalize → COP entity。
- **⚠️ RC 修正**：「與 WaveInk 共用接縫」是 **aspirational**——seam 目前**只有 TAK 真用**（`ingest_cot_event`），`normalize_waveink`/`normalize_pi_node` 是 stub；manual 繞過、pi-node 不走。SC2 可能是 **TAK 以外第一個真走共用 seam 的 source**。
- **Scope**：CoPSource enum 加值（`schemas/cop.py:22` + `core/database.py` CHECK rebuild，比照 m018）+ `normalize_sensor`（沿用 `normalize_waveink` 契約模式）+ 新 ingress route + source-ownership gate + **untrusted 內容驗證**（座標/型別白名單，比照 P2-10）。
- **供應鏈**：感測器硬體/函式庫非中國（CBRN/SDR 生態，CLAUDE.md）。**對接**：與 P3 WaveInk normalize 模式對齊（未來真共用）、TAK `/datafeeds` 概念對應。**DoD**：新 source 經 normalize→cop_entities、內容驗證、ownership gate、斷源降階。
- **依賴**：SC3 的門檻告警靠它有料。

**——— 【新增能力 feature】———**

**SC3 — 主動告警 / 決策觸發**
- **目的**：把**被動視覺**（severity 色/stale 灰）升為**主動門檻告警**（geofence/門檻），回答 OODA Decide「**何時**」。
- **RC 確認**：**100% greenfield**——零 active alerting backend，現全前端被動重繪（20s）；框架在（severity/stale 欄位 + WS 廣播）但**無條件評估引擎、cop_stream 只聽不發**。
- **Scope（高工，從零）**：條件評估引擎（geofence 進出 / 門檻）+ 觸發後端 + WS **主動 push** + 前端告警 UI + **誤報節流**（狼來了，呼應設施巡邏失效模式）。**依賴 SC2**（感測器門檻）。**DoD**：條件越界觸發、可配置門檻、誤報節流、RBAC、與 P2-23/severity pulse 整合不重複。

**SC5 — 威脅圖層**（**later**）
- **目的**：zone 加 **`threat_state`（熱/暖/冷）+ 武裝掩護已清走廊 + 敵我老化情報**，服務 RTF/多機構。
- **RC 確認**：`attributes` 是 **free-form JSON blob**（`schemas/cop.py:115` / DB `attributes TEXT DEFAULT '{}'`）→ 加 `threat_state` **無 migration**（後端 +1 key、前端 data-driven 上色 +3 行，~15 分）。
- **Scope**：(a) zone `attributes.threat_state` + 前端上色（**trivial**，可順手）；(b) 已清走廊＝route/zone 帶 state（中等）；(c) 敵我老化情報＝沿用 `stale` + 可信度標註（同 Recon）。**排程 later**（情境窄；但 (a) 便宜）。**DoD**：zone 可帶 threat_state + 即時同步 + 渲染。

**——— 【併入既有 feature 群（不另列 item）】———**
- **SC4** 連線 / 降階 chips → Phase 2 應用群 **⑤ 前端面板 + 連線 UX**（隨 P2-12；退役 cd-tak 獨立燈）
- **SC6** 降階 DoD → 已入 **Phase 2 DoD**「降階紀律」段
- **SC7** 安全升優先 → **安全 feature**（[`threat_model.md`](compliance/threat_model.md) §8.2 分級 / §8.4 LUKS→Phase 1 安全群 ⑦ P1-12 / §8.5 cert 撤銷·治理 #8）


## 跨 Phase 持續事項

- **資安政策**：`docs/compliance/security_policies.md`（InfoSec / AC / AU / IR / CP / Privacy）每 phase 收尾 review
- **威脅模型**：`docs/compliance/threat_model.md` 每加一個外部資料源（P2 TAK / P3 WaveInk）就重跑 STRIDE
- **規格書**：每 phase 收尾發 vX.Y
- **Issue/PR snapshot**：`.github/workflows/issue-snapshot.yml` 已啟用，保護 GitHub 停權場景
- **DR drill**：每季演練 step-ca rotate + DB restore
- **版號**：`command-vX.Y.Z` SemVer，每 PATCH/MINOR/MAJOR 觸發按 CLAUDE.md 規則辦理

---

## 紅隊審查發現待辦（2026-06-05）

> 以紅軍視角審視全系統後的發現，**僅記錄事實 + 歸屬，未動程式碼**（待排程）。
> 分級已**按部署範圍校正**——先前疑似 CRITICAL 的項目經查落在「未部署的休眠 Node 層」，在 ICS_COMMAND 非現役攻擊面。每項標明 事實 / 推論，便於後續 session 直接接手不必重查。
>
> **代號與狀態約定（別的 session START HERE）**：
> - **`RT-x#` 代號**：`RT` = Red Team（2026-06-05 全系統紅隊審查）；字母 `H`/`M`/`L` = 該次審查原始嚴重度（High/Medium/Low）；數字 = 流水號。代號是**穩定 anchor**，本節與 commit / issue / branch 名互引時用它（例：`fix/issue-NN-rt-h1-snapshots-auth`）。
> - **這些是「發現 backlog」，尚未升為 GitHub issue**（無 issue 號 = 還沒排程）。狀態 marker 沿用本文件全域約定（**無 marker = pending**／⏳ = branch 已開／✅ = merged）；本節各項目前皆 pending。
> - **怎麼動工（升 issue → 收尾）**：排到某項時 →（1）開 GitHub issue，標題帶 RT 代號 + 一句事實；（2）開 branch `fix/issue-NN-rt-x#-*`；（3）走 [`docs/PROCESS.md`](PROCESS.md) 10 步 lifecycle（含 `/security-review`）；（4）merged 後**回到本節該列標 ✅ + `(#PR, hash)`**，與其他 ROADMAP item 同規格。**動工前先讀同列「動工時機」與〈三陷阱〉**。
> - **什麼時候動工**：見各表「動工時機」欄（已寫成具體觸發條件，非空泛 phase 名）。RT-H1/L4 無依賴、隨時可做；其餘綁特定 item 或 deploy 里程碑。
>
> **動工前的範圍鐵則（決定一切分級）**：`server/` Node.js relay **在 ICS_COMMAND 未被部署**——
> 事實證據：① `systemd/` 只有 `ics-command.service`（FastAPI uvicorn）+ `ics-backup`，無任何 unit 跑 node；
> ② `server/package.json` 不存在；③ `start_*.sh` / CI / `deploy/` 皆未引用 `server/index.js`；
> ④ `server/config.js` 只認 `--unit shelter|medical`（PWA 單元，CLAUDE.md 明文不在本 repo 範圍）。
> 故 `server/` = P1-04 保留的 federation infra（休眠），其內漏洞在 ICS_DMAS（PWA+Pi 真跑）才現役。

### 紅隊批次 2026-06-20（公網曝面深掘 — #286–290，✅ 全修 merge）

> 6 路並行靜態稽核（auth/session、mTLS header、RBAC/演習 scope、injection、secrets/供應鏈、網路周邊+Node relay），HIGH 級主稽核員親自對 code 二次查證。完整發現 + reality-check + 修補軌跡見 [`docs/security-audit/redteam-2026-06-20.md`](security-audit/redteam-2026-06-20.md)。本輪用 GitHub issue（非 RT 代號）追蹤；backend 2.7.0→2.7.1。

| Issue | 發現 | 修補 |
|---|---|---|
| ✅ #286 | H1 `upload_map_image` 任意檔寫入（→ 儲存型 XSS / 全站接管） | server uuid 命名 + `resolve()`/`is_relative_to` 守門 + 移除 svg + `.gitignore`（`routers/map.py`） |
| ✅ #287 | H2 TTX router 無授權 gate + 跨場（broken access control） | `/api/ttx/` 比照 `/api/exercises/` 鎖 COMMAND_ROLES（`auth/role_enum.py`） |
| ✅ #288 | H3 events/decisions 寫入 IDOR + 跨演習越權（讀有 scope 寫沒有） | 寫入端點對稱套 `resolve_scope` + 抽 `_helpers.scope_clause` 把目標 row 限 scope 內（`event_repo`/`decision_repo`/`events.py`/`decisions.py`） |
| ✅ #289 | H4 公網 docker nginx 缺 security headers + cipher | `nginx.mtls.conf`/`nginx.conf` 補 HSTS/X-Frame-Options/nosniff/Referrer-Policy + ECDHE/AEAD cipher（runtime 待 Windows `curl -kI` 驗） |
| ✅ #290 | H6 compose 公開預設密碼 + M1 mTLS-on-但-secret-空 fail-open | compose `${VAR:?}` fail-closed + `.env.example` + `main._assert_safe_mtls_config()` 啟動斷言 |

> 正面：SQLi/XXE/SSRF/CoT 跨源覆寫全防住、機密無入 git、**供應鏈無中國套件（紅線通過）**、mTLS 設計（配置正確時）健全。殘留 MEDIUM/LOW（M2 鎖定 DoS / M3 timing 枚舉 / M5 audit flood / L1 tile traversal / L3 token 明文 / CSP report-only 等）見稽核 log，未納本批。
> 待開單（非安全、預存）：scenario_designer.html 呼叫已移除的 `/api/ttx/sessions/*` → TTX 設計器壞掉。

### 紅隊批次 2 — 前端駭客視角（#292–296，2026-06-20/21）

> 以「攻擊者讀得到的前端」反推攻法（攻法①–⑥）。完整對照 + perimeter 防禦表見 [`threat_model` §8.7.1](compliance/threat_model.md) + 稽核 log。每單 reality check → 修 → 使用者實機/測試驗 → 單獨 PR merge。

| Issue | 發現 / 工作 | 狀態 |
|---|---|---|
| ✅ #292 | roster `_esc` 不跳脫引號 → 屬性脈絡 XSS（攻法③） | merged（PR #297）；iPad 實機驗無回歸 |
| ✅ #294 | prod image 排除不用的孤兒 HTML 頁（減攻擊面，取代 CSP-enforce 死頁；攻法③/④） | merged（PR #298） |
| ✅ #296 | RBAC 路由分類 golden 矩陣 + 端到端強制測試（防 #287 類靜默 broken access control；攻法①） | merged（PR #299） |
| ⏸ #293 | session token → httpOnly cookie | **降級**：cert-bound session 已稀釋價值（XSS 偷到也難跨裝置重放）；非全 auth 重構不划算，緩做 |
| ⛔ #295 | 鎖定-DoS：per-source 節流 / 高權不硬鎖 | **卡 #280**：per-source 需先「真實 client IP 還原」（Tailscale/Headscale 前置 + 非 Docker-NAT）才有意義 |

> 結論：可立即做的前端面資安單全收（#292/#294/#296）。#293/#295 依設計判斷緩做/有前置依賴。下一步周邊防護 = #280 軸 2（**Headscale 前置**，供應鏈已查過紅線：見 perimeter README）。
>
> **衍生待辦（已開單留痕）**：
> - [#301](https://github.com/winson3QQ/ICS_COMMAND/issues/301) Windows prod **runtime 黑箱驗證**（H4 header/cipher、M1 prod secret、IDOR/traversal 活靶）——補白箱盲區，前置 #300。
> - [#302](https://github.com/winson3QQ/ICS_COMMAND/issues/302) **商用化前端 IP 保護**（build step 砍註解+minify+no-sourcemap / 選擇性 obfuscate / 值錢邏輯下沉）——**IP 防抄，非資安**，與資安線正交。
> - 部署：[`deploy/prod/`](../deploy/prod/) 單機 TAK+ICS 合併棧（profile toggle，相同佈署不同 delivery）。

### ✅ 公網 prod 端到端實證 + dogfood（2026-06-21）

> [`deploy/prod/`](../deploy/prod/) 單機棧在公網 Windows/Docker 首次乾淨實證通過（[#305](https://github.com/winson3QQ/ICS_COMMAND/issues/305)）。完整 onboarding 與裝置憑證踩坑已固化進 [`deploy/prod/README.md`](../deploy/prod/README.md)〈首次 onboarding〉〈裝置憑證〉〈內網/LAN〉〈憑證效期〉四節 + memory `ios-mtls-client-cert-packaging`。**動 prod 部署/憑證前先讀那份 runbook。**

| 項目 | 狀態 |
|---|---|
| ✅ [#305](https://github.com/winson3QQ/ICS_COMMAND/issues/305) | ICS+TAK 同機 build + up + smoke；mTLS :443；TAK server cert 用獨立 `ICS-TAK-SVC-CA`（與 step-ca 隔離，儀表板 cert 碰不到 TAK）；ICS↔TAK CoT 端到端；多裝置（iPhone 公網 / Windows LAN）登入全綠 |
| ✅ [#307](https://github.com/winson3QQ/ICS_COMMAND/issues/307) | **裝置憑證管理 UX**（四段落地，全程公網 prod dogfood PASS）：(1) **iOS 發證根因**：`issue_p12` 加 `--legacy`（iOS 不吃 openssl3/step 預設 PBES2/AES p12，誤報密碼錯）`backend-v2.7.2`；(2) **revoked UI 清除**（PR #309 `7ecd6cf`）：`purge_revoked_certs` + `DELETE /certs/revoked`、前端摺疊+清除；(3) **p12 密碼顯示**：每張隨機（廢弱默認 `icsclient`，對齊 threat_model H6）+ `X-P12-Password` header + 面板常駐顯示（複製鈕）；(4) **iOS 取證路徑**：不自動下載（先顯示密碼再手動觸發，避開 iOS 安裝攔截蓋畫面）+ Web Share API「分享/存檔」→ 儲存到檔案 / AirDrop 轉交別台。`backend-v2.7.3` / `frontend-v1.7.1` |
| ✅ [#312](https://github.com/winson3QQ/ICS_COMMAND/issues/312) | **iOS 零打憑證密碼接入**（公網 iPad dogfood PASS）：#307 把 p12 密碼改隨機後，iOS 安裝裸 .p12 須手打 16 字混合大小寫密碼（鍵盤卡、不可貼上）→ 死路。解：面板發證「格式」選「iOS 描述檔」→ 後端產 `.mobileconfig`（root CA + p12 + **內嵌密碼**）→ 開啟即裝、免打。`build_mobileconfig`/`fetch_root_ca_pem` + `/certs/issue?fmt=mobileconfig`（reality check：iOS 安裝要的「密碼」是裝置解鎖碼非憑證密碼，已澄清進 UI/README）。`backend-v2.8.0` / `frontend-v1.8.0` |
| ✅ [#306](https://github.com/winson3QQ/ICS_COMMAND/issues/306) 待驗 | **首次 admin 免 env-toggle mTLS bootstrap**（方案 b，code）：fresh deploy（唯一帳號 + `account_certs` 零 row **單向閂**）+ 出示 nginx 已 CA 驗的證 → 放行首位 admin 在 `ICS_MTLS_REQUIRED=true` 下登入+綁第一張證，綁定後自動關窗、免翻 env/免 restart。放行 login（`cert_active_for_account` 略過）+ check_session（`is_cert_active` 略過），cert_cn 一致性仍強制。安全：撤光證不重開窗（單向閂，防撤銷被繞過）、多帳號永久關窗、仍需 first-run PIN + CA cert 雙前提。`backend-v2.9.0`。**驗證**：須 fresh deploy 演練（runbook §首次 onboarding 已更新），使用者待從頭驗。 |
| ✅ [#279](https://github.com/winson3QQ/ICS_COMMAND/issues/279) 部分 | **憑證效期 23h→90d**（live prod 已套用：server cert `notAfter` +90d、`STEP_CLIENT_CERT_DURATION=2160h`）。**reality check 修正 issue 根因**：prod provisioner claims 已（手動）放寬 2160h、非 24h 上限卡關 → 真正限制是 compose duration env 預設 23h + ca-bootstrap 未 passthrough `ICS_SERVER_CERT_DURATION`。committed：compose 加 server-dur passthrough + .env.example 兩 duration 變數 + README §憑證效期 三步 runbook。**殘留**：provisioner claims 仍手動（未進 step-ca init）→ fresh deploy 退 24h，須照 runbook 步驟 1；既有 23h 裝置證需面板重發。 |

### 現役（command-dashboard，已部署）— 需排程修補

| 編號 | 事實 | 性質 / 推論 | 動工時機（觸發）+ 怎麼動 |
|---|---|---|---|
| **RT-H1** ✅ **已修（[#153](https://github.com/winson3QQ/ICS_COMMAND/issues/153)，2026-06-07）** | `auth/middleware.py:46` 豁免 `GET /api/snapshots/{node_type}`，handler（`routers/snapshots.py:30`）無任何 auth；全 repo grep **查無前端/Pi 呼叫者**（dashboard 走 service 層 `get_snapshots`，非此路由） | 推論：孤兒豁免 → 匿名可讀資源快照（床位/傷亡聚合）。屬 P1-14 exercise scoping 的漏網（6 repo 套了 `resolve_scope`，此路由整條繞過 session） | **已修**：middleware 移除該 GET 豁免行 → 走預設 `READ_ROLES`；保住 `POST /api/snapshots` 的 HMAC 豁免（Pi push 命脈）；`test_snapshot_get_is_open_by_design` → `test_snapshot_get_requires_auth`（401）。與 RT-L4 同一獨立 hardening PR |
| **RT-L4** ✅ **已修（[#153](https://github.com/winson3QQ/ICS_COMMAND/issues/153)，2026-06-07）** | `routers/admin.py:282` `audit_log(limit:int=100)` 無服務端上限，`?limit=` 由呼叫端全控 | 推論：`?limit=999999` → 記憶體/慢查詢壓力（DoS 弱面，需 sysadmin session，影響有限） | **已修**：`limit = max(0, min(limit, 1000))` clamp（**下限 0 並修原規劃漏洞 —— SQLite `LIMIT -1` = 無上限，負值會反成「全撈」破口**）+ 負向測試（超大 clamp、負值回空）|
| **RT-M1** | `auth/rate_limit.py:29` + `auth/service.py:45` 皆優先信 `X-Forwarded-For`。nginx 已覆寫 XFF（P1-09 `command.conf:62`），但 `ics-command.service:18` 綁 `--host 0.0.0.0:8000` | 事實：走 nginx(443) 安全；推論：**直連 :8000** 可偽造 XFF 繞 10 次/分 login 限速 + IP binding | **觸發：正式上線前的部署 hardening checklist（非 app code）**。**怎麼動**：把 `ics-command.service` 的 `--host` 改 `127.0.0.1`（只讓 nginx 對外），或 OS firewall 擋外部 :8000；同步記 `threat_model.md §3.5 DoS`。⚠️不要改 app 內 XFF 解析邏輯（會壞掉 nginx 後的合法取值） |
| **RT-M3** | `core/input_safety.py:23` `_UNSAFE_CHAR_RE` 擋 `<>` `{}` 反引號 `&#` `&entity;` `javascript:`/`data:`/`vbscript:`，**未擋** `"` `'` | 推論：`<>` 已擋 → 開新 tag 受阻；殘餘=attribute 跳脫（`" onmouseover=`）需既有 innerHTML sink 未跳脫引號才成立。屬縱深不足非破口 | **觸發：P2-07 或 P2-12 動工時**（那兩項 DoD 已要求前端 `textContent` 禁 `innerHTML`，同批做）。**怎麼動**：改 sink 端輸出編碼（innerHTML→textContent），不是改本 validator。⚠️**禁擴張此 blocklist**（見下方陷阱 1） |

### 休眠 / 非 bug — 不在 ICS_COMMAND 動

| 編號 | 事實 | 為何不動 | 動工時機（觸發）+ 去向 |
|---|---|---|---|
| **RT-C1** | `server/ws_handler.js:220` WS `auth_result` 把 `HMAC_SECRET`（= Pi→Command ingress 的 `trusted_keys` secret，見 `config.js:95` 註）明文下發給每個已認證 WS client | 事實：此檔在 ICS_COMMAND 未部署（見上方鐵則）。在 ICS_DMAS 是現役且嚴重（低權帳號可拿 ingress 偽造金鑰） | **觸發：Wave 7+ PWA 回流 kickoff，且列為其 DoD 前置**（啟用 `server/` 前必先解此 secret 共用設計）。**去向**：另開上游 ICS_DMAS issue（該 repo 才現役）+ 本 repo Wave 7+ 規劃引用本代號 |
| **RT-H2** | `middleware.py:44` 放行 `POST /api/sync/push`（無 session） | 事實：仍受 `verify_hmac` Depends 把關，**非無認證**；只有 RT-C1 洩 secret 才可偽造。RT-C1 休眠 → 此項在 ICS_COMMAND 不可獨立利用 | 隨 RT-C1（Wave 7+）一併評估 |
| **RT-L3** | `server/config.js:64` 無憑證時退回明文 WS（`STRICT_TLS` 預設 false） | 同 RT-C1：休眠 Node 層；且 `STRICT_TLS=true` 已可 fail-fast | Wave 7+ |
| **RT-M2** ✅ **更正＝死功能（2026-06-25，#348-F5 動工時查證）** | `routers/admin.py` `_check_admin_pin` 實際只驗 sysadmin role，無第二因素 | **更正**：非「當前設計」而是**遺留空殼**——`verify_admin_pin` 全 codebase **零呼叫者**、後端**無處讀 `X-Admin-PIN` header**，RBAC sysadmin-role 才是實際 gate（早期第二因素被 RBAC 取代後 UI/儲存/first-run 產生器留下空轉） | **[#384](https://github.com/winson3QQ/ICS_COMMAND/issues/384) 移除**（前端分頁 + `X-Admin-PIN` header + 後端 endpoint/`verify_admin_pin`/config/死碼）。原 P1-12 第二因素路線作廢（mTLS 裝置證已是第二因素） |
| **RT-M4** | `auth/service.py:54` session IP binding 只比 /24 前綴 | 事實：刻意取捨，容忍現場 iPad 同網段漫遊。收緊 /32 會誤踢登出（見陷阱 2） | 不動，記 residual risk |
| **RT-L1/L2** | HMAC skew 預設 5 分（`config.py:97`）；TAK insecure TLS flag（`config.py:109`） | nonce 已蓋重放；TAK TLS 已 fail-closed（P2-02 security review 落實） | 不動 / 已緩解 |

### TAK 介面安全（TAK_ENABLED=false 時休眠，啟用前必查）

> 以下六項在 `TAK_ENABLED=false`（現役預設）時不可觸達，**不影響當前系統**。TAK-A 屬架構決策（啟用前必解）；其餘依對應 P2 子項動工時機處理。代號 `TAK-x` 為本次審查 TAK 介面專項，與 RT-* 並列追蹤。

| 代號 | 事實 | 性質 / 推論 | 動工時機（觸發）+ 怎麼動 |
|---|---|---|---|
| **TAK-A** 🔵 **決策已定（2026-06-07）** | `POST /api/tak/events` 走 WRITE_ROLES session token；外部 TAK server / federation 無機器間 session 取得機制（無 API key / HMAC inbound / service account 路徑），端點對「REST federation push」用途不可呼叫 | 設計間隙（非 bug）；見缺口 #15 | **決策：選 (A) HMAC inbound，延後實作**。端點現況 session-gated（WRITE_ROLES）本就擋外部機器、僅測試/internal 可呼叫，**無現役破口**；production 上行走 :8089 pull、federation server↔server 落 :8089，皆不經此端點。真有「外部系統 REST 推 CoT 進 Command」需求時才接 HMAC inbound（對齊 Pi-node `verify_hmac`）。詳 [threat_model §8.2](compliance/threat_model.md) |
| **TAK-B** ✅ **已修（[#147](https://github.com/winson3QQ/ICS_COMMAND/pull/147) `c5088df`，issue #145，2026-06-07）** | `cop_entities.uid` 無命名空間隔離；TAK CoT uid 與 ICS 本地 entity uid 相同 + 時間戳更新 → `ingest_cot_event` CAS update 覆蓋本地 entity 的 type/lat/lon/callsign/remarks（`_TAK_UPDATE_FIELDS`） | 完整性風險（Tampering）；意外碰撞或惡意偽造 uid 均可觸發 | **[2026-06-07 改採來源所有權守門，取代原「tak: 前綴」構想]** 業界/CoT 標準查證（[The Developer's Guide to CoT](https://tutorials.techrad.co.za/wp-content/uploads/2021/06/The-Developers-Guide-to-Cursor-on-Target-1.pdf)）：uid 由來源系統命名、應已全域唯一，且轉傳須**保留 uid 不改寫**（供 P2-13 下行對 ATAK 下令對位）→ **不前綴改名**。改在 `ingest_cot_event` 加守門：TAK 事件只准動 `source='tak'` 的 entity，撞本地他源（`manual:*`/pi-node/waveink）一律拒絕覆寫、回 None 記 log。`cop_service.py` + 負向測試 `test_tak_event_does_not_overwrite_non_tak_entity`。詳 [threat_model §8.2](compliance/threat_model.md)。**✅ 反向缺口亦已修**（operator PUT/DELETE 覆寫 tak entity → 來源所有權+情境守門；並順手收緊 POST /api/tak/* 至 COMMAND_ROLES 關注入後門）→ [issue #146](https://github.com/winson3QQ/ICS_COMMAND/issues/146) |
| **TAK-C** ❌ **評估後不採（2026-06-07）** | `CoTEventIn.opex` 被收但 `normalize_cot()` 不讀；`exercise_id` 由 `current_exercise_id()` 決定 | 紅隊原述：演習中 TAK 推 `opex="o"`（實戰）→ 綁 exercise_id → reset 隨場清（資料完整性） | **不採：違反 server-authoritative doctrine**。模式（演習/實戰）由**指揮部 UI 的 active exercise 決定（server 權威）**——對齊 [resolve_scope](command-dashboard/src/services/exercise_service.py) + cop create 強制 `source='manual'` 的「**不信任 client 宣告**」doctrine。讓 ATAK `opex`（client 宣告）覆寫 server 模式 = 給每裝置「自宣告繞過演習場」開關，反成新完整性破口。`simulated` 同理應由 **P2-19 O/C 注入（server 端）** 設，非信 `opex`。紅隊原始擔憂（reset 清實戰）的真正解 = **演習/實戰部署隔離**（指揮部不同時跑兩模式 / 獨立 instance），非信 client opex。**使用者 2026-06-07 確認無「同 instance 混演習+實戰」需求** → 維持 `current_exercise_id()` 現狀，不動 code |
| **TAK-D** 🔶 **重歸屬 P2-12（2026-06-07）** | `CoPEntity.visible_to` schema 預設 `["all"]`（`schemas/cop.py:85`）；`normalize_cot()` 不覆寫此欄位 → 所有 TAK entity 對全部 COP 訂閱者可見（含 observer）；`cop_hub` broadcast 亦無分級過濾 | 未來多分類環境的資訊洩漏（Information Disclosure）；現況 observer = read-only 非敏感部署下無害 | **歸屬 P2-12**（UI 面板分層顯示時評估）。**怎麼動**：建立 CoT `access` 欄位 → `visible_to` 映射規則（如 `access="FOUO"` → `["commander","sysadmin"]`）；無多分類需求可延後。詳 [threat_model §8.2](compliance/threat_model.md) |
| **TAK-E** ✅ **已修（[#151](https://github.com/winson3QQ/ICS_COMMAND/issues/151)，2026-06-07）** | `:8089` 串流訂閱（`subscribe()`）無流量管制；每 uid 有 1s 精度保護（`_normalize_iso8601` 秒 floor + `_is_newer` 比較），但 uid 數量無上限；50 ATAK @ 1Hz = 50 DB write + 50 WS broadcast/s 持續壓 | DoS 弱面（多 uid 高頻 burst）；單 uid 已有 1s 保護，uid 爆量未擋 | **獨立 hardening PR 完成**：`_TokenBucket`（全域 events/s 速率）套 `_consume_cot`（parse + 濾 TakControl 後、ingest 前），超量丟棄該筆 + 節流 `log.warning`、不中斷串流；config `TAK_INGEST_MAX_EVENTS_PER_SEC`（預設 60，`<=0` 關閉）；limiter 跨重連共用同桶（subscribe 建一次）。**per-exercise uid cap descope**（受信任 mTLS 源價值低、每筆 create 多一 COUNT query；速率桶已涵蓋持續高率 DoS，未來 entity 爆量再開 follow-up）。`/code-review` 通過（token bucket 數學無誤、向後相容 limiter=None） |
| **TAK-F** 🔶 **部分處理（2026-06-07 校正）** | ATAK 醫療擴充（CasEvac / 9-line）在 `<detail>` 放資料；`_extract_detail()` 全部進 `attributes` → 明文 JSON 存 `cop_entities`；`cop_hub` broadcast 傳給所有訂閱者 | PII 洩漏（Information Disclosure） | **原建議「9-line 分流 `medical_records`」評估後不採** — P2-09（[#137](https://github.com/winson3QQ/ICS_COMMAND/pull/137)，設計 B）釐清 **9-line 是聚合後送態勢（傷亡數 by precedence / 位置 / 通訊），非個別病患 PII（無姓名病史）** → 進 `attributes` 隨 COP 廣播、指揮部透明可見，符合單一 COP 不開新表紅線。<br>**殘餘風險（仍待）**：(1) 其他 TAK `attributes` 的明文邊界 → **P1-12c SQLCipher**（at-rest）；(2) TAK ingest path 無 server-side XSS escape → **[#136](https://github.com/winson3QQ/ICS_COMMAND/issues/136)**（P2-12 panel 連動）。9-line 本身的個資疑慮已由「態勢非個資」釐清消解 |

### 使用者操作安全風險（合法操作者誤操作）

> 以下五項不需攻擊者，普通 sysadmin 在壓力下誤操作即可觸發。分級以「系統損害」為主軸。代號 `OP-x` 為本次審查操作安全專項。

| 代號 | 事實 | 性質 / 推論 | 動工時機（觸發）+ 怎麼動 |
|---|---|---|---|
| **OP-1** ✅ **已修（[#153](https://github.com/winson3QQ/ICS_COMMAND/issues/153)，2026-06-07）** | `account_repo.suspend_all_accounts()` SQL：`WHERE status='active'` 無排除發起者本人；執行後零 active 帳號，系統進入「需主機 shell 直操 DB 才能解救」狀態 | 自鎖風險（Denial of Access，操作失誤）；`POST /api/admin/accounts/suspend-all` 一次觸發不可逆 | **已修**：SQL 加 `AND username != ?`（排除發起者）；端點強制 body `confirm:"SUSPEND_ALL"`（422）；測試「sysadmin 不被自鎖 + 確認字串強制」。與 RT-H1/L4 同一 hardening PR |
| **OP-2** | `POST /api/admin/reset-db` / `reset-exercise` 後端接受空 body，無確認欄位；前端若只靠 JS `confirm()` dialog，一次誤點觸發不可逆清除；`reset-db` 無 L3 防呆 backup（P1-12b 尚未完成） | 不可逆破壞（演習中觸發 = 全毀）；需要 sysadmin session，但誤操作門檻低 | **P1-12b（L3 防呆 hook）同批**。**怎麼動**：後端強制 body 帶 `confirm: "RESET"` 欄位（422 強制，不依賴前端 dialog）；P1-12b L3 hook 完成後自動備份再清 |
| **OP-3** | `IDLE_TIMEOUT=900s`（15 分）；`WARNING_THRESHOLD_SECONDS=120` 已存於 `core/config.py:42` 但前端尚未實作倒數 banner；session 到期後若前端靜默回 401 → 操作員誤以為資料已送出 | UX 可靠性（資料遺失風險）；壓力演習中影響最大 | P1-14 follow-up。**怎麼動**：前端於 `WARNING_THRESHOLD_SECONDS=120` 時彈 session 警示 banner（config 已備，補 UI 即可）；全域 fetch error handler 攔 401 明確提示「已登出，資料**未**送出」 |
| **OP-4** | `GET /api/admin/backups/{name}/restore-cmd` 回傳完整 CLI 還原指令（含路徑），為「系統關機後離線執行」設計；系統**運行中**執行 `cp` 覆蓋熱 DB → SQLite WAL 不一致 / 損壞；endpoint 無任何「需先停服務」警示 | 資料損壞風險（操作情境誤解）；P1-12b restore GUI 完成後此端點應廢棄 | **P1-12b（restore GUI）同批**。**怎麼動**：response 加 `warning: "stop service before running this command"` 欄位；P1-12b 完成後廢棄此端點（GUI restore 取代）|
| **OP-5** ✅ **已緩解（reality check [#153](https://github.com/winson3QQ/ICS_COMMAND/issues/153)，2026-06-07，不動 code）** | `ensure_default_admin(default_pin="1234")` 舊函式仍在 `account_repo.py:226`；原述「無強制換 PIN 流程」 | 預設弱憑證留存（Spoofing） | **reality check：風險已被既有機制消解，不需再動。** ① production 開機走 `ensure_initial_admin_token`（**隨機 PIN**，非 1234，`main.py:113`）；② `first_run_gate_middleware`（`main.py:159`）對 `is_default_pin=1` 帳號**擋全部 `/api/*`→423**，僅放行登入/改PIN/health → **強制改 PIN、不得繞過 API**（即原「怎麼動」要求，**已實作**）。③ `ensure_default_admin` 為**測試 fixture 命脈**（conftest 建 `admin/1234` 供全套測試登入），**production 零 caller** → 刪除會打掉全套測試、零 production 效益 → **保留**。原始擔憂（預設弱憑證留存）不成立。<br>**[2026-06-25 #348-F5 進一步硬化已上 prod]**：在此基礎上補 P1 `core/pin_policy`（min6 + blocklist + 開放長密語）/ **P2a** per-account 首登強制改閘（`is_first_run_required` 收斂 bootstrap-only、解「建第 2 帳號鎖死全系統」地雷 + cop WS 補閘 + reset_pin 限 first-run）/ **P2b** create/reset 改 `generate_temp_pin` 系統產隨機臨時 PIN（admin 不自設、一次性回傳）/ **P3** 前端密碼欄放寬接受密語 + show-password。`backend-v2.16.0` / `frontend-v1.12.1`，PR #383/#385/#386，公網實機 E2E PASS。詳見 `docs/compliance/security_policies.md §2.8.1` |

### ⚠️「補完又產生別的」三陷阱（修補前必讀）

1. **RT-M3 別擴張 regex**：把 `"` `'` 加進 blocklist 會**誤殺合法輸入**（人名 `O'Brien`、座標/label 含引號 → 全 422）。XSS 正解 = sink 改 `textContent`，不是擴張輸入過濾。
2. **RT-M4 別收緊成 /32**：現場 iPad 同網段換 IP 會被**誤踢登出**。設計取捨要保留。
3. **RT-H1/L4 別夾帶進 feature PR**：這兩個與任何 feature item 無關，硬塞進 P2 PR 違反 PROCESS.md 一 task 一 PR 紀律（這本身就是管理面的「補一個生一個」）→ 開**獨立 hardening issue/PR**。

---

## 安全執行計劃（2026-06-08，dogfood 安全策略對話衍生）

> 待辦 security work 的**排程 / 並行計劃**，與〈紅隊審查發現待辦〉（點狀 findings）、`threat_model.md`（分析）互補。RT-* 多已修（#153）或刻意取捨 / 休眠 → 不在此（見該節）。

### 待辦項（已排除「接受的殘餘」）
| 項目 | 類型 | 撞 TAK 熱區？ | 依賴 / 對接 |
|---|---|---|---|
| **P1-12** key 管理 + LUKS/SQLCipher + backup（+ §8.4 `disk-v1`）| 後端 + ops | ❌ 零撞 | FIDO2 token（真驗收）；部署 at-rest 策略 |
| **#136** TAK ingest XSS | code | render 在新 panel | **併 P2-12**（panel `textContent` = 主防線；勿 escape-at-ingest，見 [#136 comment](https://github.com/winson3QQ/ICS_COMMAND/issues/136)）|
| **mTLS 憑證撤銷 — 網路層 CRL/OCSP（正解，[#232](https://github.com/winson3QQ/ICS_COMMAND/issues/232) / [#280](https://github.com/winson3QQ/ICS_COMMAND/issues/280)-F）** | 後端 + nginx + step-ca（**含 code，非純 ops**）| ❌ | **App 層撤銷已落地（#275，撤銷即時失效）**；**網路層 CRL/OCSP = 完整解**（被撤證 TLS 握手即拒、連 app 都到不了）＝ step-ca 簽 CRL + 撤銷連動 step-ca revoke（存 cert serial）+ nginx `ssl_crl` + 定期刷新。連 TAK 端被擄裝置撤銷（#232 原範圍，偏 TAK）。step-ca、P2-15 |
| **PII retention**（track 90 天 TTL，缺口 #13）| 後端 | 🟡 碰 track 寫入 | 可併 P2-20 |
| **TAK-D** `visible_to`/`access` 映射 | code | — | 併 P2-12 |
| **Audit 簽章 key**（HKDF `child[2]` 預留）| 後端 | ❌ | P1-12 之後 |

### 並行軸（依能否與 active TAK sessions（P2-13/14/#161）並行排）
- **🟢 軸 1 — P1-12（現可開，零熱區撞，丟專屬後端 session）**：`12a`（key 基建 + `disk-v1` child，mock/env 全綠）→ `12b`（backup 加密 + GUI，掛既有演習面板）→ `12c`（SQLCipher）。前置：① 訂部署 at-rest 策略（threat_model §8.4）② FIDO2 token 採購（真驗收用，mock 先行）。
- **🟢 軸 2 — 部署硬化（現可開，純 ops/config，不碰 app code）**：TAK PG 強密碼（汰 `takdevpass123`）+ 只綁 localhost；被擄裝置撤銷 SOP；部署目標機 LUKS 整碟。可與任何 code session 並行。
  - ⚠ **例外（含 code，非純 ops）**：mTLS **網路層憑證撤銷（CRL/OCSP）= 正解完整解**，見上表「mTLS 憑證撤銷」列（#232/#280-F）；App 層撤銷已落地（#275）。
- **🟡 序列 — #136**：併 P2-12（panel `textContent`）。修完連帶解 **TAK-D**（`visible_to`）+ COP poison 縱深（來源標註 / 異常偵測，→ P2-12/P2-19）。
- **⚪ 後段（依賴前項）**：PII TTL（併 P2-20）→ Audit 簽章（P1-12 後，用 key 階層 `child[2]`）。

### 建議起手
**現開軸 1（P1-12）+ 軸 2（部署硬化）並行**——皆在 TAK 熱區外、與 P2-13/14/#161 零撞。**#136 / TAK-D / poison 縱深隨 P2-12**（#136 框架已釘，不會修錯層）。

---

## 交付 / 產品化（manned C2 密封盒，2026-06-22 拍板）

> 把「ICS 怎麼交到業主手上」收成一條可追蹤的線。商用化議題，橫切 P1-12 安全基建。**SoT = [`threat_model.md` §8.8](compliance/threat_model.md)**（威脅/控制分析）；本節 = 工作串接。

### 交付模型（使用者拍板）
**交付物 = 整顆可開機 Linux 加密碟（LUKS + FIDO2 解鎖）+ 2 把實體金鑰（CTAP2，PIN+touch）。**
- **形態優先序**：① 交**整台 mini-PC**（碟 + 2 鑰裝機內）＝最乾淨密封盒，零硬體相容/OS 信任邊界問題；② 僅交碟需業主備機 → 附〈備機條件〉，並承擔 UEFI/Secure Boot/驅動相容 + LUKS enroll 交接。
- **TAK**：業主**自行下載**（散布責任在業主，避 tak.gov redistribution）；ICS 端**提供整合服務**（設 ICS-TAK-SVC-CA、簽 server/device 證、串接）。⚠ TAK 若跑 appliance 外 → TAK PostgreSQL（位置/mission/GeoChat）落業主機、**不在 LUKS 加密邊界內**（§8.4 同碟明文搬到業主側），須交付文件告知或收進 appliance。
- **不可逆殘留**：盒子「開機運行中被實體奪取」＝記憶體明文、加密失效 → manned C2 本質接受，靠運行看管 + 閒置斷電緩解。

### 已串接的單（核心在軌，地基大半已落地）
| 單 | 在交付鏈的角色 | 狀態 |
|---|---|---|
| [#226](https://github.com/winson3QQ/ICS_COMMAND/issues/226) P1-12 umbrella | key 階層 + at-rest + backup（密封盒地基）| ⏳ 12a/12b merged、12c code-complete |
| [#231](https://github.com/winson3QQ/ICS_COMMAND/issues/231) LUKS/BitLocker 整碟 | **「加密碟」本體**（appliance 定 Linux LUKS）| pending（待硬體）|
| [#230](https://github.com/winson3QQ/ICS_COMMAND/issues/230) FIDO2 + Pi 驗收 | **「2 把實體金鑰」**驗收（最少硬體：Pi500+2鑰，免買量產機）+ 失鑰救援 | pending（待 token）|
| [#279](https://github.com/winson3QQ/ICS_COMMAND/issues/279) 證 23h→90d | 場域長放、不天天換證 | pending |
| [#232](https://github.com/winson3QQ/ICS_COMMAND/issues/232) mTLS 撤銷 / [#318](https://github.com/winson3QQ/ICS_COMMAND/issues/318) TAK 真撤銷 | 交付後憑證撤銷能力（出貨強制）| #318 ⏳ 機制 live（Slice 1+2）但**未完成**：真機 dogfood 揭快取（在線證需重啟）+ backfill/盤點缺口 → Slice 3；#232 pending |
| [#278](https://github.com/winson3QQ/ICS_COMMAND/issues/278) 裝置證 enrollment 產品化 | 業主現場發裝置證 | pending |
| [#306](https://github.com/winson3QQ/ICS_COMMAND/issues/306) first-admin bootstrap | **首次安裝 onboarding**（業主首開綁 admin）| pending |
| [#321](https://github.com/winson3QQ/ICS_COMMAND/issues/321) server cert SAN 衛生 | 首站用業主 global IP 簽證、不洩內網 | ✅ merged |
| [#302](https://github.com/winson3QQ/ICS_COMMAND/issues/302) 前端 IP 保護 | 交付 container 內前端值錢邏輯保護 | ⏳ layer-1 merged |
| [#255](https://github.com/winson3QQ/ICS_COMMAND/issues/255) TAK 連線設定 DB 化 | 業主可配置連線（key 卡 P1-12）| pending |
| [#323](https://github.com/winson3QQ/ICS_COMMAND/issues/323) CA 同機 / 密封盒決策 | 架構決策記錄（CA 同機於密封盒可接受）| 決策已記 |
| [#351](https://github.com/winson3QQ/ICS_COMMAND/issues/351) 授權宣告 + SBOM | 商用交付的智財邊界（LICENSE/NOTICE/SPDX）+ 物料清單（投標/盡職調查）| ✅ LICENSE/COPYRIGHT/NOTICE + licenses/ 全文（[#352](https://github.com/winson3QQ/ICS_COMMAND/pull/352)）、SPDX 檔頭 + 根 ruff.toml（[#413](https://github.com/winson3QQ/ICS_COMMAND/pull/413)）、著作權人對齊（[#414](https://github.com/winson3QQ/ICS_COMMAND/pull/414)）、SBOM CycloneDX（[#415](https://github.com/winson3QQ/ICS_COMMAND/pull/415)）、release SBOM 流程（`scripts/gen_release_sbom.sh` + PROCESS 9.5）；⏳ 地址/email/管轄欄、鎖 3 個 `>=` 版、IP 律師覆核 pending |

### 缺口（密封盒當「可交付產品」的封裝層，**目前無單**，待開）
- **G1 封盒工序**：build 產物 → 可交付的「Linux LUKS 碟 image + FIDO2 enroll + 封存」end-to-end 工序（#231 是加密機制、#230 是硬體驗收，但「產出一個密封盒」沒人追）。
- **G2 appliance/container 硬化**：single-purpose 最小服務 + container escape 緩解（§8.8 殘留：escape → 摸到盒內 CA 鑰）。
- **G3 運行/交接 SOP + 備機條件**：閒置斷電 / 碟鑰分離運送 / tamper / 運行看管 + onboarding runbook（first-run、global IP 固定或 DNS）+〈不交主機時業主備機規格〉。
- **G4 dev→deliver build 線**：dev 在 Windows、交付物是 Linux appliance → 需 Linux build/CI 封裝線（SQLCipher 等 Linux-native 已 CI 驗）。

> **下一步（不急，待使用者定）**：把 G1–G4 收成一張 **「密封盒交付產品化」umbrella issue**，連 #226/#230/#231/#323，交付線即完整可追蹤。

---

## Phase 之後（未規劃，意見區）

- Wave 6 時間軸回放 UI（COP 快照已在 P2 預埋）
- Wave 7+：**Medical / Shelter PWA 重新對接**——P1-04 保留的 `pi_*_repo` + `sync_repo` federation 介面可直接承接，無需架構翻修。⚠️ **啟用 `server/` Node relay 前必先解 RT-C1/H2/L3**（見〈紅隊審查發現待辦〉）：現休眠所以無害，一通電 `HMAC_SECRET` WS 明文下發即成現役破口
- TAK Federation 大網部署（跨機關互通）
- 多上游節點中樞：ICS_Command 同時對接多個 Pi 站台 / 友軍 TAK Server / 多個 WaveInk 錄音站

---

## 與 ICS_DMAS 的關係

- 共用元件（`server/`、`command-dashboard/`）的修正若對 ICS_DMAS 也適用，應評估回饋上游 PR
- ICS_DMAS 仍維持三組件完整架構（shelter / medical / command），本 repo 為 Command 單體交付線

---

> **本文件為 Stage 2 起草版**，Phase 細項在實作開始前可能依據演練回饋與 WaveInk Phase 進度調整。每次調整以 PR 形式修訂並在 commit message 標示 `docs(roadmap):`。
